# 阶段验收记录

## Phase 0：基线修复

### CHARTDB-P0-001：修复测试环境 localStorage 失败

状态：已完成本地验证。

变更摘要：

- 在 `src/test/setup.ts` 中为 Vitest/happy-dom 测试环境提供符合浏览器 Storage API 的内存版 `localStorage` 和 `sessionStorage`。
- 新增 `src/lib/utils/__tests__/utils.test.ts`，覆盖 `getWorkspaceId` 创建并复用 workspace id 的行为。
- 未修改浏览器生产运行时代码。

红灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：失败，错误为 `localStorage.getItem is not a function`。

绿灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：通过，`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。

下一项建议：

- 进入 `CHARTDB-P0-002`：升级 critical/high 生产依赖并记录剩余 advisory。

### CHARTDB-P0-002：建立安全依赖基线

状态：已完成本地验证。

变更摘要：

- 运行 `npm audit fix`，仅更新 `package-lock.json` 中可安全提升的传递依赖版本，未改动生产源码。
- 生产依赖 high/critical advisory 已清零，避免使用 `npm audit fix --force` 引入 `ai`、`@ai-sdk/openai` 或 `monaco-editor` 的破坏性迁移。
- 剩余生产 advisory：AI SDK 依赖链保留 low 风险，`monaco-editor` 依赖 `dompurify` 保留 moderate 风险；当前 npm 建议的修复路径需要破坏性升级或降级，转入 Phase 1 安全评估。

红灯验证：

```bash
npm audit --omit=dev --audit-level=high
```

结果：失败，存在 `react-router`、`react-use/js-cookie`、`lodash`、`lodash-es`、`picomatch` 等 high advisory，并有 AI SDK 链相关风险。

绿灯验证：

```bash
npm audit --omit=dev --audit-level=high
```

结果：通过，生产依赖 high/critical advisory 清零；输出仍列出 5 个 low 和 1 个 moderate。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P0-003`：在 CI 中加入 audit gate 和构建门禁。

### CHARTDB-P0-003：补齐 CI 安全门禁

状态：已完成本地验证。

变更摘要：

- 在 `.github/workflows/ci.yaml` 的 `npm ci` 后新增 `npm audit --omit=dev --audit-level=high`，PR CI 顺序变为 install、audit、lint、build、test。
- 在 `.github/workflows/publish.yaml` 的 Docker 构建前新增 production audit 和 `npm run test:ci`，tag publish 发布前会运行 audit、lint、test、build。
- workflow 未新增本机私有路径或私有凭据依赖。

红灯验证：

```bash
node -e "const fs=require('fs'); const ci=fs.readFileSync('.github/workflows/ci.yaml','utf8'); const pub=fs.readFileSync('.github/workflows/publish.yaml','utf8'); if (!ci.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing ci production audit gate'); process.exit(1); } if (!pub.includes('npm run test:ci')) { console.error('missing publish test gate'); process.exit(1); } if (!pub.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing publish production audit gate'); process.exit(1); }"
```

结果：失败，错误为 `missing ci production audit gate`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const ci=fs.readFileSync('.github/workflows/ci.yaml','utf8'); const pub=fs.readFileSync('.github/workflows/publish.yaml','utf8'); if (!ci.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing ci production audit gate'); process.exit(1); } if (!pub.includes('npm run test:ci')) { console.error('missing publish test gate'); process.exit(1); } if (!pub.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing publish production audit gate'); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P0-004`：完成 Phase 0 总体验收记录。

### CHARTDB-P0-004：完成 Phase 0 验收记录

状态：已完成本地验证。

变更摘要：

- 汇总 `CHARTDB-P0-001`、`CHARTDB-P0-002`、`CHARTDB-P0-003` 的红灯、绿灯、完整验证和剩余风险。
- 明确 Phase 0 退出门槛：测试、构建、生产依赖 high audit、CI gate 已具备可重复验证记录。
- 明确 Phase 0 不关闭的风险面：浏览器端 OpenAI key 暴露、`window.env` 运行时配置、`rehype-raw` Markdown 渲染和 low/moderate advisory，全部转入 Phase 1 安全重构或后续安全治理。

红灯验证：

```bash
rg -n "Phase 0|npm run test:ci|npm audit|npm run build" docs/阶段验收记录.md
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const missing=[]; for (const token of ['Phase 0 总体验收','退出门槛','CHARTDB-P0-004']) if (!s.includes(token)) missing.push(token); if (missing.length) { console.error('missing acceptance closure: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing acceptance closure: Phase 0 总体验收, 退出门槛`。

绿灯验证：

```bash
rg -n "Phase 0|npm run test:ci|npm audit|npm run build" docs/阶段验收记录.md
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const missing=[]; for (const token of ['Phase 0 总体验收','退出门槛','CHARTDB-P0-004']) if (!s.includes(token)) missing.push(token); if (missing.length) { console.error('missing acceptance closure: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

## Phase 0 总体验收

退出门槛：

- `npm run lint`：已在 P0-001、P0-002、P0-003 完整验证中通过。
- `npm run test:ci`：已在 P0-001、P0-002、P0-003 完整验证中通过，记录结果为 `108` 个测试文件、`834` 个测试通过。
- `npm run build`：已在 P0-001、P0-002、P0-003 完整验证中通过，仍有既有 Vite chunk 过大警告，转入 Phase 6 性能优化。
- `npm audit --omit=dev --audit-level=high`：已在 P0-002、P0-003 完整验证中通过，生产依赖 high/critical advisory 清零。
- CI gate：PR workflow 已包含 production audit、lint、build、test；tag publish workflow 已在 Docker 构建前包含 production audit、lint、test、build。
- 文档记录：P0-001 到 P0-004 均已记录实际命令、结果、失败证据和剩余风险，没有未填写占位项。

剩余风险：

- 安全扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围。
- 生产依赖仍有 5 个 low 和 1 个 moderate advisory；当前 npm 建议的修复路径涉及 AI SDK / Monaco 破坏性变化，转入 Phase 1 安全评估和后续依赖治理。
- 发布 workflow 已增加本地可验证门禁，但 Docker smoke 仍属于 Phase 7 发布治理增强项。

结论：

- Phase 0：通过。
- 允许进入下一项 `CHARTDB-P1-000`：编写 Phase 1 安全实施清单。

## 全方位优化手册任务验收

### CHARTDB-F-004：window.open noopener 防护

状态：已完成本地验证。

变更摘要：

- 新增 `safeOpenUrl(url)` 工具函数，统一使用 `window.open(url, '_blank', 'noopener,noreferrer')` 打开外链。
- 将顶部菜单、编辑侧栏和 Star Us 对话框中的 6 处外链打开入口切换到 `safeOpenUrl`。
- 本任务按 `docs/ChartDB全方位评估与后续优化手册.md` 执行；文档同步属于 dispatcher done 定义，范围最小化为手册状态与本验收记录。

红灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：失败，错误为 `safeOpenUrl is not a function`。

绿灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
bash -lc '! (rg -n "window\\.open\\(" src | rg -v "noopener")'
```

结果：通过，`safeOpenUrl` 测试通过，全项目无缺少 `noopener` 的 `window.open` 调用。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`923` 个测试通过。
- `npm run build` 通过，仍有既有 Vite 大 chunk 警告，继续归入性能批次。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，为既有 AI SDK / Monaco 依赖链风险。
- 安全扫描仅命中 `default.conf.template` 的非敏感 `window.env` 运行时配置入口。

## Phase 1：安全重构

### CHARTDB-P1-000：编写 Phase 1 安全实施清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/安全模型与AI边界.md`，明确 Phase 1 的密钥、AI、Markdown、Docker/Nginx、CSP 和剩余 advisory 边界。
- 明确三种 AI mode：Disabled、BYOK Session、Self-hosted Gateway。
- 明确本地数据默认不出浏览器，AI-assisted export 需要用户显式启用和隐私确认。
- 明确 BYOK 密钥只允许保存在当前浏览器会话内，不写入 localStorage、sessionStorage、IndexedDB、URL、日志、backup 或导出文件。
- 明确 Note Markdown 首轮禁用 raw HTML，链接协议只允许 `http`、`https` 和 `mailto`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/安全模型与AI边界.md'; if (!fs.existsSync(p)) { console.error('missing docs/安全模型与AI边界.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['AI','BYOK','Gateway','Markdown','CSP','密钥']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing security plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/安全模型与AI边界.md`。

绿灯验证：

```bash
rg -n "AI|BYOK|Gateway|Markdown|CSP|密钥" docs/安全模型与AI边界.md
node -e "const fs=require('fs'); const p='docs/安全模型与AI边界.md'; const s=fs.readFileSync(p,'utf8'); const required=['AI','BYOK','Gateway','Markdown','CSP','密钥','Disabled','Self-hosted Gateway']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing security plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

本轮合并前结果：

- `npm run lint` 首次失败于新增测试文件的 JSX import、Prettier 格式和 `NodeProps` 类型约束，修复后通过。
- `npm run test:ci` 通过，`111` 个测试文件、`845` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。

本轮合并前结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P1-001`：删除 Dockerfile 和 config.js 中的 OPENAI key 注入。

### CHARTDB-P1-001：移除构建期和运行时 API key 暴露

状态：已完成本地验证。

变更摘要：

- 删除 `Dockerfile` 中的 `VITE_OPENAI_API_KEY` build arg 和 `.env` 写入。
- 删除 `default.conf.template` 的 `/config.js` 中 `OPENAI_API_KEY` 输出，仅保留 endpoint、model 和非敏感开关。
- 删除 `src/lib/env.ts` 和 SQL export 中对浏览器端长期 API key 的读取。
- 暂停非 deterministic 的 AI-assisted SQL export fallback，避免在 `CHARTDB-P1-002` 前把 OpenAI SDK 和 key fallback 打入浏览器产物。
- 新增 `src/lib/security/__tests__/browser-key-exposure.test.ts`，防止 Docker、Nginx runtime config 和 env module 重新暴露 API key。

红灯验证：

```bash
npm run test -- src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：失败，错误为 `expected Dockerfile not to contain VITE_OPENAI_API_KEY`。

绿灯验证：

```bash
npm run test -- src/lib/security/__tests__/browser-key-exposure.test.ts
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY" Dockerfile default.conf.template src || true
```

结果：通过；安全扫描无命中。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`113` 个测试文件、`851` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

本轮合并前结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`109` 个测试文件、`835` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；仍命中 `window.env` runtime object 和 `rehype-raw`，分别属于 `CHARTDB-P1-002`/`CHARTDB-P1-003` 后续范围。
- `dist` 产物不包含 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；`sk-` 仅命中 `sk-background-color-*`、`sk-stroke-*` 等 CSS token，不是真实 secret。

下一项建议：

- 进入 `CHARTDB-P1-002`：实现 AI Disabled / BYOK Session / Self-hosted Gateway mode gating。

### CHARTDB-P1-002：实现 AI mode gating

状态：已完成本地验证。

变更摘要：

- 新增 `src/lib/ai/ai-mode.ts`，定义 Disabled、BYOK Session、Self-hosted Gateway 三种 AI mode 的前端安全 gate。
- BYOK key 只保存在 module memory，提供 set/get/clear API，不写入 localStorage、sessionStorage、IndexedDB、URL、日志、backup 或导出文件。
- Self-hosted Gateway 只接受 endpoint 和 model name 等非敏感配置，不接收浏览器端长期密钥。
- `exportSQL()` 对非 deterministic export 接入 AI mode gate：默认 Disabled；BYOK/Gateway 在未确认 schema transfer 前拒绝继续；真实模型客户端仍不启用。
- README 删除旧的 `VITE_OPENAI_API_KEY` / `OPENAI_API_KEY` 构建和 Docker 示例，改为说明浏览器构建不得接收长期模型 provider key。

红灯验证：

```bash
npm run test:ci -- src/lib/ai/__tests__/ai-mode.test.ts
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- `src/lib/ai/__tests__/ai-mode.test.ts` 初始失败，错误为 `Failed to resolve import "../ai-mode"`。
- `src/lib/data/sql-export/__tests__/export-sql.test.ts` 初始失败，BYOK gate 仍返回旧错误 `AI-assisted SQL export is disabled until AI mode gating is implemented.`。

绿灯验证：

```bash
npm run test:ci -- src/lib/ai/__tests__/ai-mode.test.ts
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- `src/lib/ai/__tests__/ai-mode.test.ts` 通过，`5` 个测试通过。
- `src/lib/data/sql-export/__tests__/export-sql.test.ts` 通过，`17` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
rg -n "localStorage.*OPENAI|indexedDB.*OPENAI|OPENAI_API_KEY" src
```

本轮合并前结果：

- `npm run lint` 首次失败于本轮新增文件的 Prettier 换行，修复格式后通过。
- `npm run test:ci` 通过，`110` 个测试文件、`843` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 首次因 npm registry TLS 连接失败重试；重试后通过 high gate，剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；仍命中 `window.env` runtime object 和 `rehype-raw`，分别属于运行时非敏感配置解释和 `CHARTDB-P1-003` 范围。
- `rg -n "localStorage.*OPENAI|indexedDB.*OPENAI|OPENAI_API_KEY" src` 无命中。

下一项建议：

- 进入 `CHARTDB-P1-003`：Note Markdown 安全渲染。

### CHARTDB-P1-003：Note Markdown 安全渲染

状态：已完成本地验证。

变更摘要：

- 移除 `src/pages/editor-page/canvas/note-node/note-node.tsx` 对 `rehype-raw` 的使用，Note Markdown 预览不再把 raw HTML 转为真实 DOM 元素。
- 卸载 `rehype-raw` 生产依赖，减少 Markdown 渲染供应面。
- 为 Note 链接增加协议 gate，仅允许 `http`、`https`、`mailto`；危险链接降级为不可点击文本。
- 新增 `src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx`，覆盖 `<script>`、`iframe`、`img onerror` 和 `javascript:` 链接 fixture。

红灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：失败，错误为 `expected <script></script> to be null`；同时 happy-dom 尝试加载恶意 `iframe`，证明 raw HTML 已进入 DOM。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：通过，`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`113` 个测试文件、`851` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

本轮合并前结果：

- `npm run lint` 首次因独立 worktree 尚未安装依赖失败，错误为 `eslint: command not found`；执行 `npm install` 后重跑通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P1-004`：Docker 和 Nginx 安全头。

### CHARTDB-P1-004：Docker 和 Nginx 安全头

状态：已完成本地验证。

变更摘要：

- `default.conf.template` 为静态页面增加 `Content-Security-Policy`、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`。
- `/config.js` 重复声明安全头并增加 `Cache-Control: no-store`，避免 Nginx location 级 `add_header` 继承规则导致安全头丢失，也避免 runtime config 跨部署缓存。
- `Dockerfile` builder 阶段设置 `NODE_OPTIONS=--max-old-space-size=4096`，避免镜像内生产构建在 Vite transform 阶段触发 Node heap OOM。
- CSP 默认同源加载脚本、字体、worker 和静态资源；`connect-src` 允许 `self`、`http:` 和 `https:`，兼容本地开发、内网 gateway 和 HTTPS 自托管 gateway。
- 新增 `docs/部署与安全配置.md`，说明 self-hosted gateway endpoint、非敏感 runtime config 和 CSP 调整边界。
- 新增 `src/lib/security/__tests__/nginx-security-headers.test.ts`，覆盖基础响应头、Vite 静态资源兼容性和 `/config.js` 缓存策略。

红灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：失败，错误为 `expected ... to contain 'add_header Content-Security-Policy'`。

绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：通过，`4` 个安全测试通过。

Docker OOM 红灯验证：

```bash
docker build -t chartdb-security-smoke .
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：`docker build` 首次失败于 `JavaScript heap out of memory`；补充回归测试失败，错误为 `expected ... to contain 'ENV NODE_OPTIONS=--max-old-space-size=4096'`。

Docker OOM 绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts src/lib/security/__tests__/browser-key-exposure.test.ts
docker build -t chartdb-security-smoke .
```

结果：聚焦安全测试通过，Docker 镜像构建通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
docker build -t chartdb-security-smoke .
docker run --rm -d -p 8080:80 --name chartdb-security-smoke chartdb-security-smoke
curl -I http://localhost:8080
curl -I http://localhost:8080/config.js
docker stop chartdb-security-smoke
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过 high gate，剩余 5 个 low 和 1 个 moderate；强制修复仍涉及 AI SDK/Monaco 破坏性变化。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。
- `docker build -t chartdb-security-smoke .` 通过。
- `curl -I http://localhost:8080` 返回 CSP、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`。
- `curl -I http://localhost:8080/config.js` 返回同样安全头，并额外返回 `Cache-Control: no-store`。

下一项建议：

- 进入 `CHARTDB-P1-005`：完成密钥、Markdown、Docker 安全审查。

### CHARTDB-P1-005：Phase 1 安全审查

状态：已完成本地验证。

变更摘要：

- 新增 `docs/安全风险登记.md`，按 High、Medium、Low 记录 Phase 1 剩余风险、缓解措施、暂不修复原因和后续处理路径。
- 确认浏览器端长期 OpenAI API key 暴露、Note Markdown raw HTML/XSS 风险、Docker/Nginx 缺少基础安全头这三类 high 风险均已关闭。
- 确认剩余 moderate 风险为 `monaco-editor -> dompurify` 生产依赖 advisory；当前 Note Markdown 不依赖 DOMPurify 执行 raw HTML 清洗，破坏性 Monaco 版本变更延后到依赖治理或 Phase 6。
- 确认剩余 low 风险为 AI SDK provider-utils 资源消耗 advisory 和 `/config.js` 的非敏感 `window.env` runtime config；当前 AI 默认 Disabled，真实模型调用未恢复，`window.env` 不输出 secret 且 no-store。

红灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const required=['CHARTDB-P1-005','Phase 1 总体验收','High 风险','Medium 风险','Low 风险']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing phase 1 security review tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing phase 1 security review tokens: Phase 1 总体验收, High 风险, Medium 风险, Low 风险`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const required=['CHARTDB-P1-005','Phase 1 总体验收','High 风险','Medium 风险','Low 风险']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing phase 1 security review tokens: '+missing.join(', ')); process.exit(1); }"
rg -n "High 风险|Medium 风险|Low 风险|Phase 2 准入结论" docs/安全风险登记.md
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过 high gate；`npm audit --omit=dev --json` 显示剩余 `5` 个 low 和 `1` 个 moderate，已记录在 `docs/安全风险登记.md`。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。

## Phase 1 总体验收

退出门槛：

- `npm run lint`：已在 P1-005 完整验证中通过。
- `npm run test:ci`：已在 P1-005 完整验证中通过，记录结果为 `112` 个测试文件、`849` 个测试通过。
- `npm run build`：已在 P1-005 完整验证中通过，仍有既有 Vite chunk 过大警告，转入 Phase 6 性能优化。
- `npm audit --omit=dev --audit-level=high`：已在 P1-005 完整验证中通过，生产依赖 high/critical advisory 为 0。
- 浏览器端 API key：`Dockerfile` 和 `/config.js` 不再输出 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`。
- Markdown XSS：Note Markdown raw HTML 风险已关闭，并有恶意 Markdown 回归测试。
- Docker/Nginx：静态页面和 `/config.js` 已有基础 CSP 与安全响应头；Docker smoke 已在 P1-004 通过。
- 风险登记：High 风险无开放项；Medium 风险为 Monaco/DOMPurify advisory；Low 风险为 AI SDK provider-utils advisory 和非敏感 `window.env` runtime config。

结论：

- Phase 1：通过。
- 允许进入下一项 `CHARTDB-P2-000`：编写 Phase 2 Schema Core 与 Command 架构执行清单。

## Phase 2：Schema Core 与 Command 架构

### CHARTDB-P2-000：Phase 2 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/schema-core设计.md`，定义 Phase 2 的 schema-core 边界、目标目录、旧类型到新 domain type 的映射、command contract、validator、diff 和 undo/redo 兼容层。
- 明确 `CHARTDB-P2-001` 到 `CHARTDB-P2-006` 的自动开发顺序：先建立 model re-export，再定义 command contract，之后按 table、field/index/relationship、area/note/custom type、command history 分批迁移。
- 明确兼容层保留周期：从 `CHARTDB-P2-001` 到 `CHARTDB-P2-006` 全程保留旧 import path 和旧 history API；Phase 2 退出验收后再评估删除旧兼容层。
- 同步产品计划、工程实施计划和自动开发任务计划，下一项切到 `CHARTDB-P2-001`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/schema-core设计.md'; if(!fs.existsSync(p)){ console.error('missing docs/schema-core设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['schema-core','command','validator','diff','undo','redo','兼容层','CHARTDB-P2-001']; const missing=required.filter((token)=>!s.includes(token)); if(missing.length){ console.error('missing schema-core design tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/schema-core设计.md`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/schema-core设计.md'; if(!fs.existsSync(p)){ console.error('missing docs/schema-core设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['schema-core','command','validator','diff','undo','redo','兼容层','CHARTDB-P2-001']; const missing=required.filter((token)=>!s.includes(token)); if(missing.length){ console.error('missing schema-core design tokens: '+missing.join(', ')); process.exit(1); }"
rg -n "schema-core|command|validator|diff|undo|redo" docs/schema-core设计.md
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：合并前验证见下方本轮结果。

下一项建议：

- 进入 `CHARTDB-P2-001`：建立 `src/schema-core/model` 目录和领域模型出口，旧 import 路径通过 re-export 兼容，不修改业务行为。

### CHARTDB-P2-001：建立 schema-core 目录和领域模型出口

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/model` re-export 层，为 Diagram、Table、Field、Index、Relationship、Area、Note、CustomType、Dependency、Database 等领域对象提供新入口。
- 新增 `src/schema-core/model/__tests__/model-exports.test.ts`，验证新入口可继续使用既有 domain schema，并检查 `schema-core` 文件不直接引入 React、Dexie、Monaco、DOM 或浏览器 storage。
- 旧 `src/lib/domain` import path 保持不变，本轮未搬迁旧类型定义，未修改业务逻辑。

红灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/schema-core/model"`。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

下一项建议：

- 进入 `CHARTDB-P2-002`：定义 DiagramCommand、CommandResult、CommandContext 和 risk metadata。

### CHARTDB-P2-002：定义 DiagramCommand 基础类型

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands` 基础 contract，包含 `DiagramCommand`、`CommandContext`、`CommandResult`、`CommandRisk` 和 `ValidationIssue`。
- 新增 `createDiagramCommand`、`createSuccessResult`、`createValidationErrorResult` 和 `createCommandRisk` helper，为后续 table/field/relationship command 提供稳定输入输出协议。
- 新增 `src/schema-core/commands/__tests__/diagram-command.test.ts`，覆盖 command 创建、success result、validation error result 和 destructive risk metadata。
- 本轮未接入 `ChartDBProvider`，未迁移业务行为，旧 UI 和旧 state flow 保持不变。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-command.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/schema-core/commands"`。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-command.test.ts src/schema-core/model/__tests__/model-exports.test.ts
```

结果：通过，`2` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

本轮合并前结果：

- `npm run lint` 首次失败于 `diagram-command.ts` 的 Prettier 泛型换行格式，修复后通过。
- `npm run test:ci` 通过，`114` 个测试文件、`855` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-003`：迁移 AddTable、UpdateTable、DeleteTable command。

### CHARTDB-P2-003：迁移 Table command

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands/table-commands.ts`，定义 `table.add`、`table.update`、`table.delete` 和 `table.restore` command 创建 helper。
- 新增 `src/schema-core/commands/apply-table-command.ts`，用纯函数根据当前 diagram table state 输出下一状态、undo command、affected entity ids、validation error 与 cascade risk。
- 新增 `src/schema-core/commands/__tests__/table-commands.test.ts`，覆盖 Add table、Update table name、missing table validation 和 Delete table cascade risk。
- `ChartDBProvider` 的 `addTables`、`updateTable` 和 `removeTables` 已改为通过 table command 计算下一状态，同时保留既有 Dexie 写入、事件和旧 undo/redo 数据结构。
- 删除 table 会报告并移除关联 relationship/dependency；当前 Note 模型没有 table anchor 字段，因此本轮不改变 notes。字段和索引继续随 table payload 保留，细粒度 field/index 影响进入 `CHARTDB-P2-004`。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/table-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createAddTableCommand is not a function`，符合缺少 table command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/table-commands.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

本轮合并前结果：

- `npm run lint` 首次失败于 Prettier 格式和一个未使用 import，修复后通过；补充 undo command 类型守卫后再次通过。
- `npm run test:ci` 通过，`115` 个测试文件、`859` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-004`：迁移 Field、Index、Relationship command。

### CHARTDB-P2-004：迁移 Field、Index、Relationship command

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands/field-commands.ts`、`index-commands.ts`、`relationship-commands.ts`，定义 field、index、relationship command 创建 helper。
- 新增 `apply-field-command.ts`、`apply-index-command.ts`、`apply-relationship-command.ts`，用纯函数根据当前 diagram state 输出下一状态、undo command、affected entity ids、validation error 与 cascade risk。
- 新增 `src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts`，覆盖删除 field 清理 relationship 和 index 引用、relationship 引用校验、cardinality 更新、index 语义保留和 add field undo。
- `ChartDBProvider` 的 field、index、relationship 入口已接入 command，同时保留既有 Dexie 写入、事件和 undo/redo action。删除 field 的 undo payload 会恢复被级联移除的 relationship。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createDeleteFieldCommand is not a function`，符合缺少 field/index/relationship command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`116` 个测试文件、`864` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-005`：迁移 Area、Note、CustomType command。

### CHARTDB-P2-005：迁移 Area、Note、CustomType command

提交：本轮提交，最终 SHA 见自动化 run log。

范围：

- 新增 `src/schema-core/commands/area-commands.ts`、`note-commands.ts`、`custom-type-commands.ts`，定义 area、note、custom type command 创建 helper。
- 新增 `apply-area-command.ts`、`apply-note-command.ts`、`apply-custom-type-command.ts`，用纯函数根据当前 diagram state 输出下一状态、undo command、affected entity ids 与 validation error。
- 新增 `src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts`，覆盖 Area add/delete undo、Note markdown 内容透明更新和 CustomType 被字段引用时禁止删除。
- `ChartDBProvider` 的 area、note、custom type 入口已接入 command，同时保留既有 Dexie 写入和 undo/redo action。删除 custom type 若发现字段引用，会停在 validation error，不写入 storage 或 history。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createAddAreaCommand is not a function`，符合缺少 area/note/custom type command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：通过，`2` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

- `npm run lint` 通过。
- `npm run test:ci` 通过，`118` 个测试文件、`869` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

- `npm run lint` 通过。
- `npm run test:ci` 通过，`117` 个测试文件、`867` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过，生产依赖 high/critical 为 0；剩余 5 low + 1 moderate 继续沿用 `docs/安全风险登记.md` 的记录。
- 安全关键词扫描只命中 `default.conf.template` 的非敏感 `window.env` runtime object，未重新引入 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`。

下一项建议：

- 进入 `CHARTDB-P2-006`：接入统一 undo/redo command history。

### CHARTDB-P2-006：接入统一 undo/redo command history

状态：已完成本地验证。

提交：本轮提交，最终 SHA 见自动化 run log。

范围：

- 新增 `src/schema-core/commands/command-history.ts`，将成功 command result 转换为包含 redo command、undo command、affected entity ids 和 risk snapshot 的 command history entry。
- 新增 `src/schema-core/commands/__tests__/command-history.test.ts`，覆盖成功结果生成 history entry，以及 validation error 不写入 command history。
- `RedoUndoAction` 新增可选 `commandHistory` batch，旧 handler 继续按 `redoData` / `undoData` 执行。
- `ChartDBProvider` 中已迁移的 table、field、index、relationship、area、note 和 custom type 操作会在写入旧 undo stack 的同时附带 command metadata。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/command-history.test.ts
```

结果：失败，错误为 `createCommandHistoryEntry is not a function`，符合缺少 command history adapter 的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/command-history.test.ts src/schema-core/commands/__tests__/table-commands.test.ts src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts
```

结果：通过，`4` 个测试文件、`14` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`119` 个测试文件、`872` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`118` 个测试文件、`869` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-000`：定义 storage repository 和 migration 计划。

### CHARTDB-P3-000：定义 storage repository 和 migration 计划

状态：已完成本地验证。

范围：

- 新增 `docs/storage设计.md`，记录当前 Dexie 数据库名、版本、表结构、既有 migration 和 Provider 耦合风险。
- 明确 Phase 3 目标目录：`src/storage/db`、`src/storage/repositories`、`src/storage/transactions`、`src/storage/backup` 和 `src/storage/migrations`。
- 明确 repository、diagram transaction、backup/restore 的职责边界和禁止范围。
- 明确 `CHARTDB-P3-001` 到 `CHARTDB-P3-004` 的自动开发顺序。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/storage设计.md'; if (!fs.existsSync(p)) { console.error('missing docs/storage设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['Dexie','repository','transaction','backup','restore','migration']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing storage plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/storage设计.md`。

绿灯验证：

```bash
rg -n "Dexie|repository|transaction|backup|restore|migration" docs/storage设计.md
node -e "const fs=require('fs'); const p='docs/storage设计.md'; const s=fs.readFileSync(p,'utf8'); const required=['Dexie','repository','transaction','backup','restore','migration']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing storage plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮完整验证后回填。

下一项建议：

- 进入 `CHARTDB-P3-001`：创建 `src/storage/db` 并集中 Dexie schema。

### CHARTDB-P3-001：抽离 Dexie 数据库定义

状态：已完成本地验证。

范围：

- 新增 `src/storage/db/chartdb-dexie.ts`，统一创建 typed `ChartDB` Dexie instance。
- 新增 `src/storage/db/schema-versions.ts`，集中当前 Dexie 版本 `13`、store 声明和既有 upgrade migration。
- `StorageProvider` 不再内联声明完整 Dexie schema，改为通过 `createChartDBDexie()` 获取 db instance；现有 CRUD、diagram 读取和写入逻辑保持不变。
- 新增 `src/storage/db/__tests__/chartdb-dexie.test.ts`，覆盖数据库名、当前版本和当前 store 名称。

红灯验证：

```bash
npm run test:ci -- src/storage/db/__tests__/chartdb-dexie.test.ts
```

结果：失败，错误为 `Failed to resolve import "../chartdb-dexie"`，符合缺少 `src/storage/db` 模块的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/db/__tests__/chartdb-dexie.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`119` 个测试文件、`872` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-002`：抽 Repository API。

### CHARTDB-P3-002：抽 Repository API

状态：已完成本地验证。

范围：

- 新增 `src/storage/repositories/chartdb-repositories.ts`，集中 config、diagram filter、diagram、table、relationship、dependency、area、custom type 和 note repository API。
- `StorageProvider` 改为通过 `createChartDBRepositories()` 组合 repository 方法，不再直接访问 `db.diagrams`、`db_tables`、`notes` 等 Dexie table。
- 保留既有 StorageContext 行为：diagram 组合读取、relationship/custom type 名称排序、diagram id 更新级联到子实体、delete diagram 删除既有子实体。
- 新增 `src/storage/repositories/__tests__/chartdb-repositories.test.ts`，覆盖 repository 读写、组合读取、排序、缺失实体返回 `undefined` 和 Provider 边界。

红灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts
```

结果：安装依赖前先失败于 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `Failed to resolve import "../chartdb-repositories"`，符合缺少 repository 模块的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`120` 个测试文件、`876` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-003`：实现 diagram transaction service，覆盖 diagram 删除、替换和创建的一致性。

### CHARTDB-P3-003：实现 diagram transaction service

状态：已完成本地验证。

范围：

- 新增 `src/storage/transactions/diagram-transaction-service.ts`，统一封装 diagram 创建、删除和替换的 Dexie transaction。
- `createChartDBRepositories()` 的 diagram add/delete 已改为通过 transaction service 执行，删除 diagram 时同步清理 tables、relationships、dependencies、areas、custom types、notes 和 diagram filter。
- 新增 `src/storage/transactions/__tests__/diagram-transaction-service.test.ts`，覆盖 child entity 写入失败时回滚和 delete diagram 清理 filter。
- 扩展 `src/storage/repositories/__tests__/chartdb-repositories.test.ts`，确认 repository 对外 deleteDiagram 会清理 diagram filter。

红灯验证：

```bash
npm run test:ci -- src/storage/transactions/__tests__/diagram-transaction-service.test.ts
```

结果：安装依赖前先失败于 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `Failed to resolve import "../diagram-transaction-service"`，符合缺少 transaction service 的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts src/storage/transactions/__tests__/diagram-transaction-service.test.ts
```

结果：通过，`2` 个测试文件、`7` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`121` 个测试文件、`879` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-004`：备份恢复版本化。

### CHARTDB-P3-004：备份恢复版本化

状态：已完成本地验证。

变更摘要：

- 新增 `src/storage/backup/backup-format.ts` 和 `src/storage/backup/index.ts`，定义 `chartdb.backup` + `schemaVersion: 1` 的 backup 文件格式。
- backup 导出写入 `createdAt`、`source`、`appVersion`、`diagramCount` 和完整 `diagrams` payload。
- restore 入口在恢复前校验 JSON、format、schemaVersion、diagram count 和 diagram 基础字段；不兼容版本会给出明确错误。
- `src/lib/export-import-utils.ts` 的现有 JSON 导出入口改用 versioned backup，导入入口保留旧单 diagram JSON 兼容路径。
- 新增 `docs/备份恢复格式.md` 记录格式字段、恢复规则和后续 migration 边界。

红灯验证：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/storage/backup"`。

绿灯验证：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`122` 个测试文件、`883` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-000`：定义 dialect contract 和迁移顺序。

### CHARTDB-P4-000：定义 dialect contract 和迁移顺序

状态：已完成本地验证。

变更摘要：

- 新增 `docs/方言能力矩阵.md`，作为 Phase 4 importer / exporter 插件化执行清单。
- 矩阵覆盖 PostgreSQL、MySQL、MariaDB、SQLite、SQL Server、Oracle、CockroachDB、ClickHouse 和 DBML。
- 每个方言记录 import 状态、export 状态、主要支持、unsupported syntax 和 warning 规则。
- 明确统一 contract 要求：`ImportResult`、`ExportResult`、`warnings`、`unsupportedObjects`、`sourceMap` 和 `riskLevel`。
- 明确后续迁移顺序：先做 `CHARTDB-P4-001` common contract，再迁移 PostgreSQL importer，之后迁移其它 dialect wrapper 和 exporter result。

红灯验证：

本任务为 PLAN 切片，不涉及运行时代码行为；以缺失文档作为红灯基线：

```bash
test -f docs/方言能力矩阵.md
```

结果：失败，`docs/方言能力矩阵.md` 不存在。

绿灯验证：

```bash
rg -n "PostgreSQL|MySQL|SQLite|SQL Server|Oracle|DBML|capability|warning" docs/方言能力矩阵.md
```

结果：通过，文档包含验收要求中的方言、capability 和 warning 规则。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`122` 个测试文件、`883` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-001`：创建 dialect contract 和 result type。

### CHARTDB-P4-001：创建 dialect contract 和 result type

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/common/importer.ts`，定义 `SchemaImporter`、`ImportResult`、`DialectWarning`、`UnsupportedDialectObject`、`DialectSourceMap` 和旧 importer adapter。
- 新增 `src/dialects/common/exporter.ts`，定义 `SchemaExporter`、`ExportResult`、`riskLevel` 和旧 exporter adapter。
- 新增 `src/dialects/common/capability-matrix.ts`，定义 `SupportLevel`、capability map、缺省 unsupported helper。
- 新增 `src/dialects/common/__tests__/capability-matrix.test.ts`，覆盖 capability 缺省值、unsupported syntax/warning rules、import/export result 字段和 legacy adapter 兼容。

红灯验证：

```bash
npm run test:ci -- src/dialects/common/__tests__/capability-matrix.test.ts
```

结果：失败，错误为 `Failed to resolve import "../capability-matrix"`。

绿灯验证：

```bash
npm run test:ci -- src/dialects/common/__tests__/capability-matrix.test.ts
```

结果：通过，`1` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`123` 个测试文件、`889` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-002`：迁移 PostgreSQL importer。

### CHARTDB-P4-002：迁移 PostgreSQL importer

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/postgresql/importer.ts`，提供 `postgresqlSchemaImporter.importSchema()`，把 PostgreSQL importer 接入统一 `ImportResult` contract。
- 新增 `src/dialects/postgresql/capabilities.ts`，声明 PostgreSQL import/export capability、unsupported syntax 和 warning rules。
- 新增 `src/dialects/postgresql/parser/legacy-parser.ts`，以 wrapper 方式复用旧 `fromPostgres` parser，并保持旧 diagram normalize、排序和 primary key index 行为。
- 新增 `src/dialects/postgresql/warnings.ts`，将 RLS、policy、extension、trigger 和 function 输出为结构化 `warnings` 与 `unsupportedObjects`。
- 新增 `src/dialects/postgresql/__tests__/importer.test.ts`，覆盖 wrapper fixture、capability levels 和 unsupported warning。

红灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/importer.test.ts
```

结果：失败，错误为 `Failed to resolve import "../importer"`。

绿灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/importer.test.ts
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

PostgreSQL regression 验证：

```bash
npm run test:ci -- src/lib/data/sql-import/dialect-importers/postgresql src/dialects/postgresql/__tests__/importer.test.ts
```

结果：通过，`50` 个测试文件、`171` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`124` 个测试文件、`891` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；生产依赖仍报告既有 `5` 个 low 和 `1` 个 moderate。
- 安全关键词扫描仅命中 `default.conf.template` 中既有非敏感 `window.env` runtime object，本轮未新增 API key、`rehype-raw` 或 `dangerouslySetInnerHTML` 风险面。

下一项建议：

- 进入 `CHARTDB-P4-003`：迁移 MySQL、MariaDB、SQLite、SQL Server 和 Oracle wrapper。

### CHARTDB-P4-003：迁移 MySQL、MariaDB、SQLite、SQL Server、Oracle

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/common/legacy-sql-importer.ts`，统一旧 SQL parser 到 `Diagram` 的转换、表排序和 primary key index normalize。
- 新增 `src/dialects/mysql`、`src/dialects/mariadb`、`src/dialects/sqlite`、`src/dialects/sqlserver` 和 `src/dialects/oracle`，每个方言提供 `SchemaImporter` wrapper、capabilities 和 warning 提取。
- MariaDB 明确使用 MySQL parser fallback，并用 `experimental` capability 与 `mariadb.mysql_fallback` warning 暴露产品风险。
- MySQL ENGINE/charset、MariaDB sequence/engine、SQLite virtual table、SQL Server procedure、Oracle sequence/package 等 unsupported 或降级语义进入结构化 `warnings` / `unsupportedObjects`。
- 新增 `src/dialects/__tests__/sql-dialect-importers.test.ts`，覆盖五个 wrapper 的统一 `ImportResult` contract、capability metadata 和 warning 输出。

红灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；安装依赖后重跑，按预期失败于 `Failed to resolve import "../mysql"`，确认 wrapper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

SQL dialect regression 验证：

```bash
npm run test:ci -- src/lib/data/sql-import/dialect-importers/mysql src/lib/data/sql-import/dialect-importers/sqlite src/lib/data/sql-import/dialect-importers/sqlserver src/lib/data/sql-import/dialect-importers/oracle src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：通过，`23` 个测试文件、`122` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P4-004`：DBML 进入 dialect pipeline。

### CHARTDB-P4-004：DBML 进入 dialect pipeline

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/dbml/importer.ts` 和 `src/dialects/dbml/exporter.ts`，让 DBML import/export 复用既有逻辑并输出统一 `ImportResult` / `ExportResult`。
- 新增 `src/dialects/dbml/capabilities.ts`，声明 DBML import/export capability、unsupported syntax 和 warning rules。
- 新增 `src/dialects/dbml/warnings.ts`，将 `TableGroup`、standalone `Note`、空表和重复表输出为结构化 `warnings` 与 `unsupportedObjects`。
- 将 dialect contract 类型扩展为 `DatabaseType | 'dbml'`，DBML 只作为 pipeline dialect 标识，不进入数据库选择和元数据系统。
- 新增 `src/dialects/dbml/__tests__/dbml-pipeline.test.ts`，覆盖 DBML round-trip wrapper、capability levels 和 unsupported warning。

红灯验证：

```bash
npm run test:ci -- src/dialects/dbml/__tests__/dbml-pipeline.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；安装依赖后重跑，按预期失败于 `Failed to resolve import "../index"`，确认 DBML dialect wrapper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/dialects/dbml/__tests__/dbml-pipeline.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P4-005`：导入前展示 preview、warnings 和 object counts。

### CHARTDB-P4-005：导入 Preview Flow

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/import/import-preview.ts`，将统一 `ImportResult` 汇总为导入 preview summary。
- 新增 `src/features/import/import-preview-panel.tsx`，在导入对话框中展示 tables、relationships、customTypes、warnings 和 unsupportedObjects。
- `ImportDatabaseDialog` 改为两步确认：首次点击只解析并暂存 preview，不写入 IndexedDB；用户确认后才写入 tables、relationships 和 customTypes。
- DBML 与 SQL DDL preview 走 Phase 4 dialect wrapper；Smart Query JSON 保持既有 metadata import 兼容路径。

红灯验证：

```bash
npm run test:ci -- src/features/import/__tests__/import-preview.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，按预期失败于 `Failed to resolve import "../import-preview"`，确认 preview helper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/features/import/__tests__/import-preview.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-000`：定义 UX 和可访问性验收矩阵。

### CHARTDB-P5-000：Phase 5 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/可访问性与核心流程验收.md`，定义首次进入、Smart Query、导入 Preview、编辑画布和设置中心的验收矩阵。
- 矩阵覆盖桌面、移动、键盘、可访问性和失败状态，作为 `CHARTDB-P5-001` 到 `CHARTDB-P5-004` 的统一验收依据。
- 明确 Phase 5 仍禁止账号登录、云端 diagram 存储、团队 workspace、持久化 AI secret 和远端 schema 上传。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P5-001`。

红灯验证：

```bash
rg -n "首次进入|Smart Query|aria|键盘|设置" docs/可访问性与核心流程验收.md
```

结果：按预期失败，目标文档尚不存在。

绿灯验证：

```bash
rg -n "首次进入|Smart Query|aria|键盘|设置" docs/可访问性与核心流程验收.md
```

结果：通过，命中核心流程、可访问性和设置验收条目。

下一项建议：

- 进入 `CHARTDB-P5-001`：重做首次进入入口。

### CHARTDB-P5-001：重做首次进入入口

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/onboarding/onboarding-dialog.tsx`，在首次无本地 diagram 时提供数据库选择、导入现有数据库、新建空白图、模板示例和 JSON backup 导入入口。
- `useDiagramLoader()` 在空状态下展示 onboarding dialog，已有 diagram、默认 diagram 和直接 URL 加载路径保持不变。
- 新建空白图只在选择数据库和启动方式后执行；若 `addDiagram` 或 `updateConfig` 失败，会删除已创建 diagram 并显示失败提示。
- 新增 `src/features/onboarding/__tests__/onboarding-dialog.test.tsx`，覆盖三入口可见、未选库提示、新建空白图、导入和模板路径。

红灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试先失败于导入路径缺少数据库选择，确认未选库 gating 生效；修正测试交互后再失败于模板入口需点击 Continue，确认流程是“选择入口 -> 继续”。

绿灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-002`：Smart Query Wizard。

### CHARTDB-P5-002：Smart Query Wizard

状态：已完成本地验证。

变更摘要：

- `SmartQueryInstructions` 新增 `Smart Query Wizard` 结构化说明，按选择数据库、复制 Smart Query、粘贴 JSON、Preview tables/relationships/warnings、Confirm import 五步呈现。
- Smart Query 说明明确 ChartDB 不要求数据库密码，用户不应粘贴连接串或 secret。
- `ImportDatabaseDialog` 捕获 preview 解析失败和无可导入对象结果，并在导入粘贴区显示可操作错误，指向 Smart Query JSON、SQL syntax 或 dialect limitations。
- 新建 diagram 的首次导入路径也接入 Preview/Confirm；大 Smart Query 结果在确认后仍保留进入 SelectTables 的旧分支。
- 新增 `smart-query-instructions.test.tsx`，覆盖无密码原则和五步向导文案。

红灯验证：

```bash
npm run test:ci -- src/dialogs/common/import-database/instructions-section/instructions/__tests__/smart-query-instructions.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于找不到 `Smart Query Wizard` heading，确认旧 Smart Query 说明仍是两步文本。

绿灯验证：

```bash
npm run test:ci -- src/dialogs/common/import-database/instructions-section/instructions/__tests__/smart-query-instructions.test.tsx
```

结果：通过，`1` 个测试文件、`1` 个测试通过；测试输出无 i18n 初始化警告。

下一项建议：

- 进入 `CHARTDB-P5-003`：全局 aria-label 和键盘路径修复。

### CHARTDB-P5-003：全局 aria-label 和键盘路径修复

状态：已完成本地验证。

变更摘要：

- 新增 `src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts`，覆盖 Phase 5 核心 icon-only controls 和 Monaco 编辑器用途名称契约。
- `CodeSnippet` 的复制按钮和额外 action 按钮补充可访问名称，复制按钮沿用当前 copied / copy tooltip 文案。
- `DialogContent` 的 back button 补充 `Go back` 名称。
- 画布 Toolbar 的 filter、show all、zoom、reorder、undo、redo 等图标按钮补充明确 `aria-label`。
- Smart Query output、SQL query 和 DBML Monaco 编辑器补充用途名称，便于屏幕阅读器区分输入区和代码展示区。

红灯验证：

```bash
npm run test:ci -- src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `CodeSnippet` 缺少 `aria-label={copyButtonLabel}`，确认核心复制按钮尚未命名。

绿灯验证：

```bash
npm run test:ci -- src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-004`：设置中心。

### CHARTDB-P5-004：设置中心

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/settings/settings-dialog.tsx`、`display-settings.tsx`、`privacy-settings.tsx` 和 `keyboard-shortcuts-settings.tsx`，通过左侧 Sidebar 的 `Settings` 入口打开。
- 设置中心集中管理 theme、language、minimap、field attributes、scroll action、AI mode、Self-hosted Gateway endpoint/model hint、导出 backup、恢复 backup 和清理本地 diagram 的危险操作入口。
- `LocalConfigProvider` 为 localStorage 读写增加 try/catch 容错；存储不可用时设置中心显示 session-only 降级说明。
- BYOK key 继续保持 session-only，不写入 localStorage、sessionStorage 或 settings state。

红灯验证：

```bash
npm run test -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于缺少 `src/features/settings/settings-dialog.tsx`，确认设置中心尚未实现。

绿灯验证：

```bash
npm run test -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P6-000`：定义 Phase 6 性能优化执行清单。

### CHARTDB-P6-000：Phase 6 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/性能基线与优化计划.md`，定义首屏 bundle、Monaco chunk、模板数据 chunk、large schema import、layout worker 和 CI 性能预算指标。
- 明确 Phase 6 执行顺序：Monaco 懒加载、模板 lazy registry、Parser/layout worker 化、Bundle budget 和性能回归记录。
- 约束性能优化不得破坏 Phase 1 安全边界和 Phase 5 可访问性边界。

红灯验证：

```bash
rg -n "bundle|Monaco|worker|large schema|首屏" docs/性能基线与优化计划.md
```

结果：失败，`docs/性能基线与优化计划.md` 不存在，确认 Phase 6 执行清单尚未建立。

绿灯验证：

```bash
rg -n "bundle|Monaco|worker|large schema|首屏" docs/性能基线与优化计划.md
```

结果：通过，文档包含 bundle、Monaco、worker、large schema 和首屏指标。

下一项建议：

- 进入 `CHARTDB-P6-001`：仅在代码编辑器路径加载 Monaco，并记录 `npm run build` chunk 变化。

### CHARTDB-P6-001：Monaco 懒加载

状态：已完成本地验证。

变更摘要：

- 新增 `src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts`，防止 `CodeSnippet` 外层模块和 DBML highlight helper 静态加载 Monaco runtime。
- 新增 `src/components/code-snippet/code-snippet-editor.tsx`，把 `useMonaco`、theme setup、worker config 和实际 Monaco editor 渲染移入懒加载子组件。
- `src/components/code-snippet/code-snippet.tsx` 外层只保留复制按钮、附加动作和 Suspense 壳，继续导出懒加载 `Editor` / `DiffEditor` 供现有代码编辑路径使用。
- `src/components/code-snippet/dbml/utils.ts` 改为 type-only Monaco 引用，错误高亮使用 Monaco 兼容的 plain range object。

红灯验证：

```bash
npm run test -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：先失败于 `CodeSnippet` 外层静态 `@monaco-editor/react` / `./config.ts` / theme import；扩展断言后再次失败于 `dbml/utils.ts` 的 `import * as monaco from 'monaco-editor'`。

绿灯验证：

```bash
npm run test -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：通过，`2` 个懒加载契约测试通过。

完整验证：

```bash
npm run build
```

结果：通过，仍有既有大 chunk 警告。chunk 变化：

- 基线：`code-editor-DNXSDyTK.js` 为 `15,279.59 kB` / gzip `2,787.98 kB`，入口 `index-Ca7JE0_R.js` 为 `2,609.63 kB` / gzip `513.31 kB`。
- 本轮后：`code-editor-BHvScKzX.js` 为 `3,788.30 kB` / gzip `976.92 kB`，入口 `index-C-ERHBci.js` 为 `2,609.63 kB` / gzip `513.31 kB`。
- 剩余：`editor-page-T0odRZ2l.js` 为 `11,472.77 kB` / gzip `1,806.40 kB`，主要转入 DBML/parser 与后续 worker 化治理。

下一项建议：

- 进入 `CHARTDB-P6-002`：模板 lazy registry。

### CHARTDB-P6-002：模板 lazy registry

状态：已完成本地验证。

变更摘要：

- 新增 `src/templates-data/template-manifest.ts`，将模板列表所需 metadata 与完整 diagram 数据拆分。
- 新增 `src/templates-data/__tests__/template-lazy-registry.test.ts`，防止列表路径重新静态加载完整模板聚合模块。
- `src/templates-data/templates-data.ts` 移除全部模板模块静态 import，改为通过 `loadTemplateBySlug()` 和 per-template dynamic import 加载完整模板。
- `src/templates-data/template-utils.ts`、`src/pages/templates-page/templates-page.tsx` 和 `TemplateCard` 改为消费 `TemplateManifest`，模板列表不访问 `diagram`。
- `src/router.tsx` 的模板详情和 clone loader 改为按 slug 动态加载完整模板。

红灯验证：

```bash
npm run test -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `template-utils.ts` 仍动态导入 `templates-data` 且 `template-manifest.ts` 不存在，确认当前列表路径仍会加载完整模板。

绿灯验证：

```bash
npm run test -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：通过，`2` 个模板 lazy registry 契约测试通过。

完整验证：

```bash
npm run build
```

结果：通过，仍有既有大 chunk 警告。chunk 变化：

- 本轮后：`employee-db-romprqJV.js`、`wordpress-db-B8pCH5_j.js`、`monica-db-Bj9lUCcR.js` 等模板数据已拆成独立 per-template JS chunk。
- 入口：`index-BOUqD14g.js` 为 `2,609.63 kB` / gzip `513.32 kB`。
- 剩余：`editor-page-NpB-GzH7.js` 为 `11,472.80 kB` / gzip `1,806.46 kB`，主要转入 DBML/parser 与后续 worker 化治理。

下一项建议：

- 进入 `CHARTDB-P6-003`：Parser 和 layout worker 化。

### CHARTDB-P6-003：Parser 和 layout worker 化

状态：已完成本地验证。

变更摘要：

- 新增 `src/workers/worker-client.ts`，统一 module worker 调用、结构化错误和无 Worker fallback。
- 新增 `src/workers/import-worker/import-worker.ts` 与 `src/workers/import-worker/import-preview-client.ts`，导入 Preview parse 默认经 worker 执行，主线程实现拆到 `src/features/import/import-preview-core.ts` 作为 fallback。
- 新增 `src/workers/layout-worker/layout-worker.ts` 与 `src/workers/layout-worker/layout-client.ts`，Area auto arrange 和 Move to Area 改为异步 worker client 计算布局。
- 新增 `src/workers/__tests__/worker-client.test.ts`、`src/features/import/__tests__/import-preview-worker-routing.test.ts` 和 `src/workers/layout-worker/__tests__/layout-client.test.ts`。
- 补充 `src/workers/__tests__/worker-client-progress.test.ts` 和 `src/features/import/__tests__/import-preview-progress-panel.test.tsx`，覆盖 worker progress、cancel 和导入 preview 运行中 UI。

聚焦验证：

```bash
npm run test -- src/workers/__tests__/worker-client.test.ts src/features/import/__tests__/import-preview-worker-routing.test.ts src/features/import/__tests__/import-preview.test.ts src/workers/layout-worker/__tests__/layout-client.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑通过，`4` 个文件 / `5` 个测试通过。

完整验证：

```bash
npm run lint
npm run build
```

结果：通过。`npm run build` 输出仍有既有大 chunk 警告；入口 `index` 为 `2,609.63 kB` / gzip `513.31 kB`，`code-editor` 为 `3,788.30 kB` / gzip `976.91 kB`，`editor-page` 为 `11,474.96 kB` / gzip `1,807.52 kB`。本轮主要降低 large schema import 和 area layout 的主线程阻塞风险，parser 依赖体积仍留给后续 bundle budget 治理。导入 preview 运行中显示 progress，并可 cancel 当前 worker task；100 tables / 500 tables smoke 方法已记录在 `docs/性能基线与优化计划.md`。

下一项建议：

- 进入 `CHARTDB-P7-000`：定义发布门禁和文档补齐范围。

## Phase 7：发布治理与文档

### CHARTDB-P7-000：Phase 7 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/发布检查清单.md`，定义 release gate、Docker smoke、文档 gate、回滚与失败处理、自托管 AI Gateway 边界。
- 发布前检查覆盖 production audit、lint、test、build、`git diff --check`、安全 rg 扫描和 Docker smoke。
- 明确 Docker daemon 不可用时不得自动放行发布，必须记录替代验证和人工补跑要求。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-001`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/发布检查清单.md'; if (!fs.existsSync(p)) { console.error('missing docs/发布检查清单.md'); process.exit(1); }"
```

结果：失败，错误为 `missing docs/发布检查清单.md`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/发布检查清单.md'; const s=fs.readFileSync(p,'utf8'); const required=['release','Docker','audit','test','build','smoke','回滚','自托管']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing release checklist tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
rg -n "release|Docker|audit|test|build|smoke" docs/发布检查清单.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P7-001`：发布 workflow gate。

### CHARTDB-P7-001：发布 workflow gate

状态：已完成本地验证。

变更摘要：

- 在 `.github/workflows/publish.yaml` 的正式 multi-arch 镜像 push 前增加 Docker smoke build/run gate。
- Docker smoke 使用 buildx `push: false`、`load: true` 构建本地 `linux/amd64` 镜像，启动容器后检查 `/` 和 `/config.js`。
- smoke gate 验证静态页面响应包含 CSP、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`，并验证 `/config.js` 返回 `Cache-Control: no-store`。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-002`。

红灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('.github/workflows/publish.yaml','utf8'); const required=['Docker smoke','push: false','load: true']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing publish docker smoke gate: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing publish docker smoke gate: Docker smoke, push: false, load: true`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('.github/workflows/publish.yaml','utf8'); const required=['Docker smoke','push: false','load: true','curl --fail','cache-control: no-store','content-security-policy']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing publish docker smoke gate: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过；安全扫描仅命中 `default.conf.template` 中非敏感 `window.env` runtime object。

下一项建议：

- 进入 `CHARTDB-P7-002`：补齐工程文档。

### CHARTDB-P7-002：补齐工程文档

状态：已完成本地验证。

变更摘要：

- README 新增文档导航，明确默认本地优先、无账号、无数据库密码，并链接架构、部署安全、AI 隐私、备份恢复、方言能力、导入导出和测试策略文档。
- 新增 `docs/架构说明.md`，记录 React 入口、schema-core、storage、dialects、workers、AI 与发布边界。
- 新增 `docs/导入导出接口约定.md`，记录 `ImportResult`、`ExportResult`、Import Preview、backup 和 AI-assisted export 约束。
- 新增 `docs/测试策略.md`，记录本地门禁、TDD、核心测试范围、UI smoke、发布 Docker smoke、安全扫描和性能记录要求。
- 新增 `.github/dependabot.yml`，每周检查 npm 和 GitHub Actions 依赖，生产安全更新独立分组。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-003`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingFiles = requiredFiles.filter((file) => !fs.existsSync(file));
const readme = fs.readFileSync('README.md', 'utf8');
const requiredReadmeTokens = [
  '本地优先',
  '无账号',
  '无数据库密码',
  'docs/部署与安全配置.md',
  'docs/备份恢复格式.md',
  'docs/方言能力矩阵.md',
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingTokens = requiredReadmeTokens.filter((token) => !readme.includes(token));
if (missingFiles.length || missingTokens.length) {
  console.error('missing P7-002 docs gate');
  if (missingFiles.length) console.error(`files: ${missingFiles.join(', ')}`);
  if (missingTokens.length) console.error(`README tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，缺少 `docs/架构说明.md`、`docs/导入导出接口约定.md`、`docs/测试策略.md`，README 缺少本地优先、无账号、无数据库密码和文档链接 token。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingFiles = requiredFiles.filter((file) => !fs.existsSync(file));
const readme = fs.readFileSync('README.md', 'utf8');
const requiredReadmeTokens = [
  '本地优先',
  '无账号',
  '无数据库密码',
  'docs/部署与安全配置.md',
  'docs/备份恢复格式.md',
  'docs/方言能力矩阵.md',
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingTokens = requiredReadmeTokens.filter((token) => !readme.includes(token));
if (missingFiles.length || missingTokens.length) {
  console.error('missing P7-002 docs gate');
  if (missingFiles.length) console.error(`files: ${missingFiles.join(', ')}`);
  if (missingTokens.length) console.error(`README tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "本地优先|安全|部署|备份|恢复|方言|AI" README.md docs
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P7-003`：Issue template 和贡献规则。

### CHARTDB-P7-003：Issue template 和贡献规则

状态：已完成本地验证。

变更摘要：

- 新增 `.github/ISSUE_TEMPLATE/bug_report.yml`，要求 bug 报告包含复现步骤、版本环境和可选脱敏 fixture。
- 新增 `.github/ISSUE_TEMPLATE/dialect_regression.yml`，要求方言 import/export regression 附数据库类型、版本、流程、SQL/DBML/Smart Query fixture、预期对象数量和实际结果。
- 新增 `.github/ISSUE_TEMPLATE/feature_request.yml`，要求说明本地优先影响，并确认不默认引入登录或自动上传。
- 新增 `.github/ISSUE_TEMPLATE/security_report.md` 和 `config.yml`，关闭空 issue 并引导安全问题走 GitHub private vulnerability reporting。
- 更新 `CONTRIBUTING.md`，补充本地优先边界、PR 验证、dialect regression fixture、security private report 和测试要求。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P8-000`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  '.github/ISSUE_TEMPLATE/bug_report.yml',
  '.github/ISSUE_TEMPLATE/feature_request.yml',
  '.github/ISSUE_TEMPLATE/dialect_regression.yml',
  '.github/ISSUE_TEMPLATE/security_report.md',
  '.github/ISSUE_TEMPLATE/config.yml',
];
const missing = requiredFiles.filter((file) => !fs.existsSync(file));
const contributing = fs.existsSync('CONTRIBUTING.md') ? fs.readFileSync('CONTRIBUTING.md', 'utf8') : '';
const requiredTokens = ['security', 'dialect', 'regression', 'reproduction', 'fixture', 'local-first', 'private report'];
const missingTokens = requiredTokens.filter((token) => !contributing.toLowerCase().includes(token));
if (missing.length || missingTokens.length) {
  console.error('missing P7-003 governance gate');
  if (missing.length) console.error(`files: ${missing.join(', ')}`);
  if (missingTokens.length) console.error(`CONTRIBUTING tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，缺少 `.github/ISSUE_TEMPLATE/bug_report.yml`、`feature_request.yml`、`dialect_regression.yml`、`security_report.md`、`config.yml`，`CONTRIBUTING.md` 缺少 security、dialect、regression、reproduction、fixture、local-first、private report token。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  '.github/ISSUE_TEMPLATE/bug_report.yml',
  '.github/ISSUE_TEMPLATE/feature_request.yml',
  '.github/ISSUE_TEMPLATE/dialect_regression.yml',
  '.github/ISSUE_TEMPLATE/security_report.md',
  '.github/ISSUE_TEMPLATE/config.yml',
];
const missing = requiredFiles.filter((file) => !fs.existsSync(file));
const contributing = fs.existsSync('CONTRIBUTING.md') ? fs.readFileSync('CONTRIBUTING.md', 'utf8') : '';
const requiredTokens = ['security', 'dialect', 'regression', 'reproduction', 'fixture', 'local-first', 'private report'];
const missingTokens = requiredTokens.filter((token) => !contributing.toLowerCase().includes(token));
if (missing.length || missingTokens.length) {
  console.error('missing P7-003 governance gate');
  if (missing.length) console.error(`files: ${missing.join(', ')}`);
  if (missingTokens.length) console.error(`CONTRIBUTING tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "security|dialect|regression|reproduction|fixture|local-first|private report" .github CONTRIBUTING.md
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过。

补充说明：production audit 仍保留既有 5 low + 1 moderate advisory，高危门槛通过；安全关键词扫描仅命中 `default.conf.template` 的非敏感 `window.env` runtime object。

下一项建议：

- 进入 `CHARTDB-P8-000`：Cloud/Team 边界计划。

## Phase 8：可选 Cloud/Team 预研

### CHARTDB-P8-000：Cloud/Team 边界计划

状态：已完成本地验证。

变更摘要：

- 新增 `docs/可选账号登录与团队协作预研.md`，明确 Phase 8 只做预研文档和接口边界，不新增登录代码。
- 明确不登录仍可完整使用 OSS Core，本地 diagram 不自动上传，云同步必须用户主动开启。
- 定义可选 Workspace、Team、权限、AuditLog、租户隔离、数据导出、数据删除和权限 negative tests。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P8-001`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
if (!fs.existsSync(p)) {
  console.error(`missing ${p}`);
  process.exit(1);
}
const s = fs.readFileSync(p, 'utf8');
const required = [
  '不登录仍可完整使用 OSS Core',
  '本地 diagram 不自动上传',
  '用户主动开启云同步',
  'Workspace',
  'Team',
  '权限',
  'AuditLog',
  '租户隔离',
  '数据导出',
  '数据删除',
  'negative tests',
  '不新增登录代码',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing Cloud/Team boundary tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，错误为 `missing docs/可选账号登录与团队协作预研.md`。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
if (!fs.existsSync(p)) {
  console.error(`missing ${p}`);
  process.exit(1);
}
const s = fs.readFileSync(p, 'utf8');
const required = [
  '不登录仍可完整使用 OSS Core',
  '本地 diagram 不自动上传',
  '用户主动开启云同步',
  'Workspace',
  'Team',
  '权限',
  'AuditLog',
  '租户隔离',
  '数据导出',
  '数据删除',
  'negative tests',
  '不新增登录代码',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing Cloud/Team boundary tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "账号|登录|Workspace|Team|权限|审计|本地模式|云同步|AuditLog|negative tests" docs/可选账号登录与团队协作预研.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P8-001`：Cloud/Team 技术栈选型记录。

### CHARTDB-P8-001：Cloud/Team 技术栈选型记录

状态：已完成本地验证。

变更摘要：

- 在 `docs/可选账号登录与团队协作预研.md` 补齐技术栈选型记录。
- 推荐方案为 `NestJS + PostgreSQL + Auth.js/OIDC adapter + REST API + append-only sync log`。
- 对比方案为 Fastify 轻量 API，并明确不推荐把 BaaS SDK 作为 OSS Core 默认依赖。
- 明确 OSS Core 隔离方式、Cloud/Team 不作为默认依赖、sync/push-pull 和 conflict 处理边界。
- 同步产品计划、工程实施计划和自动任务计划；本轮未新增登录代码。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
const s = fs.readFileSync(p, 'utf8');
const required = [
  '推荐方案',
  '方案 A',
  '方案 B',
  'NestJS',
  'Fastify',
  'PostgreSQL',
  'Auth.js',
  'OAuth',
  'SSO',
  'sync',
  'conflict',
  'OSS Core 隔离',
  'Cloud/Team 不作为默认依赖',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing P8-001 tech spike tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，错误为 `missing P8-001 tech spike tokens: 推荐方案, 方案 A, 方案 B, Auth.js, sync, conflict, OSS Core 隔离, Cloud/Team 不作为默认依赖`。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
const s = fs.readFileSync(p, 'utf8');
const required = [
  '推荐方案',
  '方案 A',
  '方案 B',
  'NestJS',
  'Fastify',
  'PostgreSQL',
  'Auth.js',
  'OAuth',
  'SSO',
  'sync',
  'conflict',
  'OSS Core 隔离',
  'Cloud/Team 不作为默认依赖',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing P8-001 tech spike tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "NestJS|Fastify|PostgreSQL|Auth|OAuth|SSO|sync|conflict" docs/可选账号登录与团队协作预研.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- Phase 8 预研队列已完成边界计划和技术栈选型；若继续推进，需要另开独立 Cloud/Team 工程计划，不直接进入账号登录实现。

## 后续优化批次 F：快速修复

### CHARTDB-F-003：entrypoint.sh 移除 OPENAI_API_KEY

状态：已完成本地验证。

变更摘要：

- 从 `entrypoint.sh` 的 `envsubst` 白名单中移除 `${OPENAI_API_KEY}`。
- 保留 `${OPENAI_API_ENDPOINT}`、`${LLM_MODEL_NAME}`、`${HIDE_CHARTDB_CLOUD}` 和 `${DISABLE_ANALYTICS}` 运行时配置替换。
- 在 `src/lib/security/__tests__/browser-key-exposure.test.ts` 增加 `entrypoint.sh` 断言，防止 OpenAI API key 重新进入运行时配置白名单。

红灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：失败，错误为 `expected '#!/bin/sh...' not to contain 'OPENAI_API_KEY'`。

绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/browser-key-exposure.test.ts
rg -n "OPENAI_API_KEY" entrypoint.sh
```

结果：

- 目标安全测试通过，`1` 个测试通过。
- `rg -n "OPENAI_API_KEY" entrypoint.sh` 无命中，命令按 ripgrep 语义返回 `1`。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过；安全扫描仍可能命中文档化的非 entrypoint 风险面时，以命中上下文判定是否属于后续任务范围。

实际结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`922` 个测试通过。
- `npm run build` 通过，仍有既有 Vite large chunk warning，继续归入后续性能优化任务。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；仍报告 5 个 low 和 1 个 moderate，属于 AI SDK / Monaco 后续治理范围。
- 安全扫描仅命中 `default.conf.template` 的既有 `window.env` 运行时配置入口；`entrypoint.sh` 不再命中 `OPENAI_API_KEY`。

下一项建议：

- 继续批次 F 的独立安全切片 `CHARTDB-F-004`：为所有 `window.open` 调用添加 `noopener noreferrer` 防护。
