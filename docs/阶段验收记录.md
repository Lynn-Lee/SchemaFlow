# 阶段验收记录

## Phase 0：基线修复

### CHARTDB-P0-001：修复测试环境 localStorage 失败

状态：已完成本地验证。

变更摘要：

- 在 `src/test/setup.ts` 中为 Vitest/happy-dom 测试环境提供符合浏览器 Storage API 的内存版 `localStorage` 和 `sessionStorage`。
- 新增 `src/lib/utils/__tests__/utils.test.ts`，覆盖 `getWorkspaceId` 创建并复用 workspace id 的行为。
- 未修改浏览器生产运行时代码。

红灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：失败，错误为 `localStorage.getItem is not a function`。

绿灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：通过，`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。

下一项建议：

- 进入 `CHARTDB-P0-002`：升级 critical/high 生产依赖并记录剩余 advisory。

### CHARTDB-P0-002：建立安全依赖基线

状态：已完成本地验证。

变更摘要：

- 运行 `npm audit fix`，仅更新 `package-lock.json` 中可安全提升的传递依赖版本，未改动生产源码。
- 生产依赖 high/critical advisory 已清零；后续 `CHARTDB-F-002a` 已删除未使用的 `ai` / `@ai-sdk/openai` 依赖链，避免继续保留 AI SDK low advisory。
- 剩余生产 advisory：`monaco-editor` 依赖 `dompurify` 保留 1 个 low 和 1 个 moderate 风险；当前 npm 建议的修复路径需要 Monaco 破坏性版本变更，转入 `CHARTDB-S-003` 评估。

红灯验证：

```bash
npm audit --omit=dev --audit-level=high
```

结果：失败，存在 `react-router`、`react-use/js-cookie`、`lodash`、`lodash-es`、`picomatch` 等 high advisory，并有 AI SDK 链相关风险。

绿灯验证：

```bash
npm audit --omit=dev --audit-level=high
```

结果：通过，生产依赖 high/critical advisory 清零；输出仍列出 5 个 low 和 1 个 moderate。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P0-003`：在 CI 中加入 audit gate 和构建门禁。

### CHARTDB-P0-003：补齐 CI 安全门禁

状态：已完成本地验证。

变更摘要：

- 在 `.github/workflows/ci.yaml` 的 `npm ci` 后新增 `npm audit --omit=dev --audit-level=high`，PR CI 顺序变为 install、audit、lint、build、test。
- 在 `.github/workflows/publish.yaml` 的 Docker 构建前新增 production audit 和 `npm run test:ci`，tag publish 发布前会运行 audit、lint、test、build。
- workflow 未新增本机私有路径或私有凭据依赖。

红灯验证：

```bash
node -e "const fs=require('fs'); const ci=fs.readFileSync('.github/workflows/ci.yaml','utf8'); const pub=fs.readFileSync('.github/workflows/publish.yaml','utf8'); if (!ci.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing ci production audit gate'); process.exit(1); } if (!pub.includes('npm run test:ci')) { console.error('missing publish test gate'); process.exit(1); } if (!pub.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing publish production audit gate'); process.exit(1); }"
```

结果：失败，错误为 `missing ci production audit gate`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const ci=fs.readFileSync('.github/workflows/ci.yaml','utf8'); const pub=fs.readFileSync('.github/workflows/publish.yaml','utf8'); if (!ci.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing ci production audit gate'); process.exit(1); } if (!pub.includes('npm run test:ci')) { console.error('missing publish test gate'); process.exit(1); } if (!pub.includes('npm audit --omit=dev --audit-level=high')) { console.error('missing publish production audit gate'); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有的 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P0-004`：完成 Phase 0 总体验收记录。

### CHARTDB-P0-004：完成 Phase 0 验收记录

状态：已完成本地验证。

变更摘要：

- 汇总 `CHARTDB-P0-001`、`CHARTDB-P0-002`、`CHARTDB-P0-003` 的红灯、绿灯、完整验证和剩余风险。
- 明确 Phase 0 退出门槛：测试、构建、生产依赖 high audit、CI gate 已具备可重复验证记录。
- 明确 Phase 0 不关闭的风险面：浏览器端 OpenAI key 暴露、`window.env` 运行时配置、`rehype-raw` Markdown 渲染和 low/moderate advisory，全部转入 Phase 1 安全重构或后续安全治理。

红灯验证：

```bash
rg -n "Phase 0|npm run test:ci|npm audit|npm run build" docs/阶段验收记录.md
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const missing=[]; for (const token of ['Phase 0 总体验收','退出门槛','CHARTDB-P0-004']) if (!s.includes(token)) missing.push(token); if (missing.length) { console.error('missing acceptance closure: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing acceptance closure: Phase 0 总体验收, 退出门槛`。

绿灯验证：

```bash
rg -n "Phase 0|npm run test:ci|npm audit|npm run build" docs/阶段验收记录.md
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const missing=[]; for (const token of ['Phase 0 总体验收','退出门槛','CHARTDB-P0-004']) if (!s.includes(token)) missing.push(token); if (missing.length) { console.error('missing acceptance closure: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

## Phase 0 总体验收

退出门槛：

- `npm run lint`：已在 P0-001、P0-002、P0-003 完整验证中通过。
- `npm run test:ci`：已在 P0-001、P0-002、P0-003 完整验证中通过，记录结果为 `108` 个测试文件、`834` 个测试通过。
- `npm run build`：已在 P0-001、P0-002、P0-003 完整验证中通过，仍有既有 Vite chunk 过大警告，转入 Phase 6 性能优化。
- `npm audit --omit=dev --audit-level=high`：已在 P0-002、P0-003 完整验证中通过，生产依赖 high/critical advisory 清零。
- CI gate：PR workflow 已包含 production audit、lint、build、test；tag publish workflow 已在 Docker 构建前包含 production audit、lint、test、build。
- 文档记录：P0-001 到 P0-004 均已记录实际命令、结果、失败证据和剩余风险，没有未填写占位项。

剩余风险：

- 安全扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 Phase 1 的 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围。
- 生产依赖仍有 1 个 low 和 1 个 moderate advisory，均来自 `monaco-editor -> dompurify`；当前 npm 建议的修复路径涉及 Monaco 破坏性版本变更，转入 `CHARTDB-S-003` 和后续依赖治理。
- 发布 workflow 已增加本地可验证门禁，但 Docker smoke 仍属于 Phase 7 发布治理增强项。

结论：

- Phase 0：通过。
- 允许进入下一项 `CHARTDB-P1-000`：编写 Phase 1 安全实施清单。

## 全方位优化手册任务验收

### CHARTDB-T-007：canvas.tsx 拆分（第五段 continuation）

状态：进行中，本轮完成第五段最小拆分切片，尚未达到任务完整验收线。

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-floating-edge.ts`，抽出临时 cursor node 与 floating edge 的纯数据组装逻辑。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-floating-edge.test.ts`，覆盖无 cursor 时保持原节点/边引用、浮动 cursor node、cursor target、hover target 与 source table fallback。
- `src/pages/editor-page/canvas/canvas.tsx` 改为调用该 helper，移除局部浮动边组装和不再使用的 import。
- `canvas.tsx` 从 `1490` 行降到 `1454` 行；`CHARTDB-T-007` 仍保持 `in_progress`，后续继续拆 `eventConsumer`、`CanvasViewport`、drag/zoom hooks 或其它事件 handler，直到主体低于 `500` 行。

红灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-floating-edge.test.ts
```

结果：先因 fresh worktree 缺少依赖失败于 `vitest: command not found`；执行 `npm install` 后，测试按预期失败于 `Failed to resolve import "../canvas-floating-edge"`。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-floating-edge.test.ts
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-floating-edge.test.ts src/pages/editor-page/canvas/__tests__/canvas-node-changes.test.ts src/pages/editor-page/canvas/__tests__/canvas-node-storage-updates.test.ts
```

结果：通过，聚焦测试 `4` 个通过；相邻 canvas node changes/storage updates 回归共 `10` 个测试通过。

### CHARTDB-A-009：Diagram version 字段与迁移钩子

状态：已完成本地验证。

变更摘要：

- 在 `src/schema-core/model/diagram.ts` 新增 `CURRENT_DIAGRAM_VERSION = 1`，并让 `diagramSchema` 对缺失 version 的历史数据补全当前版本。
- `diagramFromJSONInput()` 新增裸 diagram JSON 迁移入口：无 version 的旧数据按 v0 兼容并补全；高于当前支持版本的输入会给出包含 detected/expected version 的错误。
- 新建空图、onboarding、metadata import、SQL import 和当前 context diagram 构造路径写入当前 version。
- 为兼容历史模板和大量既有测试 fixture，`Diagram.version` 在 TypeScript 类型层保持可选；运行时 schema 与真实新建入口负责补全。

范围说明：

- 任务卡 allowed_files 未列出 `src/context/chartdb-context/**`、`src/dialogs/create-diagram-dialog/**`、`src/features/onboarding/**`、metadata/SQL import 创建入口，但验收要求新建 diagram 写入当前版本号；本轮只对真实 diagram 构造路径做最小越界。

红灯验证：

```bash
npm run test:ci -- src/lib/__tests__/export-import-utils.test.ts
```

结果：失败，错误为 `expected undefined to be 1`，证明当前缺少 `CURRENT_DIAGRAM_VERSION` 与版本补全。

绿灯验证：

```bash
npm run test:ci -- src/lib/__tests__/export-import-utils.test.ts
```

结果：通过，`2` 个测试通过。

构建验证：

```bash
npm run build
```

结果：通过；仍有既有 chunk 过大警告和 Browserslist 数据过期提示，未由本任务引入。

下一项建议：

- 继续批次 A 的 `CHARTDB-A-011`：Worker 任务增加超时/deadline；或进入批次 P 的 `CHARTDB-P-001`。

### CHARTDB-F-004：window.open noopener 防护

状态：已完成本地验证。

变更摘要：

- 新增 `safeOpenUrl(url)` 工具函数，统一使用 `window.open(url, '_blank', 'noopener,noreferrer')` 打开外链。
- 将顶部菜单、编辑侧栏和 Star Us 对话框中的 6 处外链打开入口切换到 `safeOpenUrl`。
- 本任务按 `docs/ChartDB全方位评估与后续优化手册.md` 执行；文档同步属于 dispatcher done 定义，范围最小化为手册状态与本验收记录。

红灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
```

结果：失败，错误为 `safeOpenUrl is not a function`。

绿灯验证：

```bash
npm run test:ci -- src/lib/utils/__tests__/utils.test.ts
bash -lc '! (rg -n "window\\.open\\(" src | rg -v "noopener")'
```

结果：通过，`safeOpenUrl` 测试通过，全项目无缺少 `noopener` 的 `window.open` 调用。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`923` 个测试通过。
- `npm run build` 通过，仍有既有 Vite 大 chunk 警告，继续归入性能批次。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，为既有 AI SDK / Monaco 依赖链风险。
- 安全扫描仅命中 `default.conf.template` 的非敏感 `window.env` 运行时配置入口。

### CHARTDB-F-001a：移除 @uidotdev/usehooks 依赖

状态：已完成本地验证。

变更摘要：

- 从 `package.json` dependencies 中删除零引用依赖 `@uidotdev/usehooks`。
- 通过 `npm uninstall @uidotdev/usehooks` 同步更新 `package-lock.json`，移除 lockfile 中对应依赖声明和 `node_modules/@uidotdev/usehooks` 节点。
- 未修改源码行为；`src/` 中无任何 `@uidotdev/usehooks` 引用。

红灯验证：

```bash
node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if (pkg.dependencies?.['@uidotdev/usehooks']) { console.error('dependency still present: @uidotdev/usehooks'); process.exit(1); }"
```

结果：失败，错误为 `dependency still present: @uidotdev/usehooks`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if (pkg.dependencies?.['@uidotdev/usehooks']) { console.error('dependency still present: @uidotdev/usehooks'); process.exit(1); }"
rg -n "@uidotdev/usehooks" src package.json package-lock.json
```

结果：通过；`rg` 无命中。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`923` 个测试通过。
- `npm run build` 通过，仍有既有 Vite 大 chunk 警告，继续归入性能批次。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，为既有 AI SDK / Monaco 依赖链风险。
- 安全扫描仅命中 `default.conf.template` 的非敏感 `window.env` 运行时配置入口。

下一项建议：

- 继续执行 `CHARTDB-F-001b`：将 TreeView 动画依赖从 `motion` 切换为显式 `framer-motion`。

### CHARTDB-F-001b：TreeView motion 依赖显式化

状态：已完成本地验证。

变更摘要：

- 从 `package.json` dependencies 中删除 `motion`。
- 将 `framer-motion` 作为直接依赖声明，匹配 `src/components/tree-view/tree-view.tsx` 中已有的 `framer-motion` import。
- 通过 `package-lock.json` 删除 `motion` wrapper 节点，保留原先经 `motion` 拉入的 `framer-motion` 12.23.x 依赖树；未修改 TreeView 动画实现。

红灯验证：

```bash
node -e "const p=require('./package.json'); const errors=[]; if (p.dependencies?.motion) errors.push('motion dependency should be removed'); if (!p.dependencies?.['framer-motion']) errors.push('framer-motion should be a direct dependency'); if (errors.length) { console.error(errors.join('\n')); process.exit(1); }"
```

结果：失败，错误为 `motion dependency should be removed` 和 `framer-motion should be a direct dependency`。

绿灯验证：

```bash
node -e "const p=require('./package.json'); const errors=[]; if (p.dependencies?.motion) errors.push('motion dependency should be removed'); if (p.dependencies?.['framer-motion'] !== '^12.23.6') errors.push('framer-motion should use the previous motion version constraint'); if (errors.length) { console.error(errors.join('\n')); process.exit(1); }"
bash -lc '! rg -n "\"motion\"" package.json'
rg -n "framer-motion" package.json src/components/tree-view/tree-view.tsx
```

结果：通过；`package.json` 不再声明 `motion`，并直接声明 `framer-motion`。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`923` 个测试通过。
- `npm run build` 通过，仍有既有 Vite 大 chunk 警告，继续归入性能批次。
- `git diff --check` 通过。

下一项建议：

- 继续执行 `CHARTDB-F-005`：修复 ClickHouse onboarding 入口。

### CHARTDB-F-002a：移除未使用的 AI SDK 依赖

状态：已完成本地验证。

变更摘要：

- 从 `package.json` dependencies 中删除 `@ai-sdk/openai` 和 `ai`。
- 通过 `package-lock.json` 移除 `@ai-sdk/openai`、`ai`、`@ai-sdk/provider`、`@ai-sdk/provider-utils`、`@ai-sdk/gateway`、`@vercel/oidc`、`@opentelemetry/api` 等仅由 AI SDK 拉入的依赖节点。
- AI 默认 Disabled / BYOK / Gateway 的本地 gate 代码未改动；本轮只移除未被源码引用的 SDK 包。
- 同步更新 P0-002 依赖 advisory 结论：AI SDK low advisory 已清除，剩余 production advisory 为 `monaco-editor -> dompurify` 的 1 low + 1 moderate。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
const lock = JSON.parse(fs.readFileSync('package-lock.json', 'utf8'));
const forbidden = ['@ai-sdk/openai', 'ai'];
const offenders = [];
for (const name of forbidden) {
  if (pkg.dependencies?.[name]) offenders.push(`package.json dependencies contains ${name}`);
  if (lock.packages?.['']?.dependencies?.[name]) offenders.push(`package-lock root dependencies contains ${name}`);
  if (lock.packages?.[`node_modules/${name}`]) offenders.push(`package-lock packages contains node_modules/${name}`);
}
if (offenders.length) {
  console.error(offenders.join('\n'));
  process.exit(1);
}
NODE
```

结果：失败，错误包含 `package.json dependencies contains @ai-sdk/openai`、`package-lock packages contains node_modules/@ai-sdk/openai`、`package.json dependencies contains ai` 和 `package-lock packages contains node_modules/ai`。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
const lock = JSON.parse(fs.readFileSync('package-lock.json', 'utf8'));
for (const name of ['@ai-sdk/openai', 'ai']) {
  if (pkg.dependencies?.[name]) throw new Error(`${name} still exists in package.json`);
  if (lock.packages?.['']?.dependencies?.[name]) throw new Error(`${name} still exists in lockfile root`);
  if (lock.packages?.[`node_modules/${name}`]) throw new Error(`${name} still exists in lockfile packages`);
}
NODE
bash -lc '! rg -n "\"@ai-sdk/openai\"|\"ai\"" package.json package-lock.json'
```

结果：通过；`package.json` 和 `package-lock.json` 不再声明 AI SDK 包。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过。
- `npm run build` 通过，仍有既有 Vite large chunk warning，继续归入性能批次。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；production audit 只剩 `monaco-editor -> dompurify` 的 1 low + 1 moderate。
- 安全扫描仅命中 `default.conf.template` 的非敏感 `window.env` runtime object。

下一项建议：

- 继续批次 F 的独立产品修复 `CHARTDB-F-005`：修复 ClickHouse onboarding 入口。

## Phase 1：安全重构

### CHARTDB-P1-000：编写 Phase 1 安全实施清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/安全模型与AI边界.md`，明确 Phase 1 的密钥、AI、Markdown、Docker/Nginx、CSP 和剩余 advisory 边界。
- 明确三种 AI mode：Disabled、BYOK Session、Self-hosted Gateway。
- 明确本地数据默认不出浏览器，AI-assisted export 需要用户显式启用和隐私确认。
- 明确 BYOK 密钥只允许保存在当前浏览器会话内，不写入 localStorage、sessionStorage、IndexedDB、URL、日志、backup 或导出文件。
- 明确 Note Markdown 首轮禁用 raw HTML，链接协议只允许 `http`、`https` 和 `mailto`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/安全模型与AI边界.md'; if (!fs.existsSync(p)) { console.error('missing docs/安全模型与AI边界.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['AI','BYOK','Gateway','Markdown','CSP','密钥']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing security plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/安全模型与AI边界.md`。

绿灯验证：

```bash
rg -n "AI|BYOK|Gateway|Markdown|CSP|密钥" docs/安全模型与AI边界.md
node -e "const fs=require('fs'); const p='docs/安全模型与AI边界.md'; const s=fs.readFileSync(p,'utf8'); const required=['AI','BYOK','Gateway','Markdown','CSP','密钥','Disabled','Self-hosted Gateway']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing security plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

本轮合并前结果：

- `npm run lint` 首次失败于新增测试文件的 JSX import、Prettier 格式和 `NodeProps` 类型约束，修复后通过。
- `npm run test:ci` 通过，`111` 个测试文件、`845` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。

本轮合并前结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`108` 个测试文件、`834` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描仍命中既有 `OPENAI_API_KEY` / `window.env` / `rehype-raw` 风险面，属于 `CHARTDB-P1-001` 和 `CHARTDB-P1-003` 范围，本轮未扩大风险面。

下一项建议：

- 进入 `CHARTDB-P1-001`：删除 Dockerfile 和 config.js 中的 OPENAI key 注入。

### CHARTDB-P1-001：移除构建期和运行时 API key 暴露

状态：已完成本地验证。

变更摘要：

- 删除 `Dockerfile` 中的 `VITE_OPENAI_API_KEY` build arg 和 `.env` 写入。
- 删除 `default.conf.template` 的 `/config.js` 中 `OPENAI_API_KEY` 输出，仅保留 endpoint、model 和非敏感开关。
- 删除 `src/lib/env.ts` 和 SQL export 中对浏览器端长期 API key 的读取。
- 暂停非 deterministic 的 AI-assisted SQL export fallback，避免在 `CHARTDB-P1-002` 前把 OpenAI SDK 和 key fallback 打入浏览器产物。
- 新增 `src/lib/security/__tests__/browser-key-exposure.test.ts`，防止 Docker、Nginx runtime config 和 env module 重新暴露 API key。

红灯验证：

```bash
npm run test -- src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：失败，错误为 `expected Dockerfile not to contain VITE_OPENAI_API_KEY`。

绿灯验证：

```bash
npm run test -- src/lib/security/__tests__/browser-key-exposure.test.ts
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY" Dockerfile default.conf.template src || true
```

结果：通过；安全扫描无命中。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`113` 个测试文件、`851` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

本轮合并前结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`109` 个测试文件、`835` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；剩余 5 个 low 和 1 个 moderate，继续按 Phase 1 安全评估和后续依赖治理处理。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；仍命中 `window.env` runtime object 和 `rehype-raw`，分别属于 `CHARTDB-P1-002`/`CHARTDB-P1-003` 后续范围。
- `dist` 产物不包含 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；`sk-` 仅命中 `sk-background-color-*`、`sk-stroke-*` 等 CSS token，不是真实 secret。

下一项建议：

- 进入 `CHARTDB-P1-002`：实现 AI Disabled / BYOK Session / Self-hosted Gateway mode gating。

### CHARTDB-P1-002：实现 AI mode gating

状态：已完成本地验证。

变更摘要：

- 新增 `src/lib/ai/ai-mode.ts`，定义 Disabled、BYOK Session、Self-hosted Gateway 三种 AI mode 的前端安全 gate。
- BYOK key 只保存在 module memory，提供 set/get/clear API，不写入 localStorage、sessionStorage、IndexedDB、URL、日志、backup 或导出文件。
- Self-hosted Gateway 只接受 endpoint 和 model name 等非敏感配置，不接收浏览器端长期密钥。
- `exportSQL()` 对非 deterministic export 接入 AI mode gate：默认 Disabled；BYOK/Gateway 在未确认 schema transfer 前拒绝继续；真实模型客户端仍不启用。
- README 删除旧的 `VITE_OPENAI_API_KEY` / `OPENAI_API_KEY` 构建和 Docker 示例，改为说明浏览器构建不得接收长期模型 provider key。

红灯验证：

```bash
npm run test:ci -- src/lib/ai/__tests__/ai-mode.test.ts
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- `src/lib/ai/__tests__/ai-mode.test.ts` 初始失败，错误为 `Failed to resolve import "../ai-mode"`。
- `src/lib/data/sql-export/__tests__/export-sql.test.ts` 初始失败，BYOK gate 仍返回旧错误 `AI-assisted SQL export is disabled until AI mode gating is implemented.`。

绿灯验证：

```bash
npm run test:ci -- src/lib/ai/__tests__/ai-mode.test.ts
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- `src/lib/ai/__tests__/ai-mode.test.ts` 通过，`5` 个测试通过。
- `src/lib/data/sql-export/__tests__/export-sql.test.ts` 通过，`17` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
rg -n "localStorage.*OPENAI|indexedDB.*OPENAI|OPENAI_API_KEY" src
```

本轮合并前结果：

- `npm run lint` 首次失败于本轮新增文件的 Prettier 换行，修复格式后通过。
- `npm run test:ci` 通过，`110` 个测试文件、`843` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 首次因 npm registry TLS 连接失败重试；重试后通过 high gate，剩余 5 个 low 和 1 个 moderate。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`；仍命中 `window.env` runtime object 和 `rehype-raw`，分别属于运行时非敏感配置解释和 `CHARTDB-P1-003` 范围。
- `rg -n "localStorage.*OPENAI|indexedDB.*OPENAI|OPENAI_API_KEY" src` 无命中。

下一项建议：

- 进入 `CHARTDB-P1-003`：Note Markdown 安全渲染。

### CHARTDB-P1-003：Note Markdown 安全渲染

状态：已完成本地验证。

变更摘要：

- 移除 `src/pages/editor-page/canvas/note-node/note-node.tsx` 对 `rehype-raw` 的使用，Note Markdown 预览不再把 raw HTML 转为真实 DOM 元素。
- 卸载 `rehype-raw` 生产依赖，减少 Markdown 渲染供应面。
- 为 Note 链接增加协议 gate，仅允许 `http`、`https`、`mailto`；危险链接降级为不可点击文本。
- 新增 `src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx`，覆盖 `<script>`、`iframe`、`img onerror` 和 `javascript:` 链接 fixture。

红灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：失败，错误为 `expected <script></script> to be null`；同时 happy-dom 尝试加载恶意 `iframe`，证明 raw HTML 已进入 DOM。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：通过，`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`113` 个测试文件、`851` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

本轮合并前结果：

- `npm run lint` 首次因独立 worktree 尚未安装依赖失败，错误为 `eslint: command not found`；执行 `npm install` 后重跑通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P1-004`：Docker 和 Nginx 安全头。

### CHARTDB-P1-004：Docker 和 Nginx 安全头

状态：已完成本地验证。

变更摘要：

- `default.conf.template` 为静态页面增加 `Content-Security-Policy`、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`。
- `/config.js` 重复声明安全头并增加 `Cache-Control: no-store`，避免 Nginx location 级 `add_header` 继承规则导致安全头丢失，也避免 runtime config 跨部署缓存。
- `Dockerfile` builder 阶段设置 `NODE_OPTIONS=--max-old-space-size=4096`，避免镜像内生产构建在 Vite transform 阶段触发 Node heap OOM。
- CSP 默认同源加载脚本、字体、worker 和静态资源；`connect-src` 允许 `self`、`http:` 和 `https:`，兼容本地开发、内网 gateway 和 HTTPS 自托管 gateway。
- 新增 `docs/部署与安全配置.md`，说明 self-hosted gateway endpoint、非敏感 runtime config 和 CSP 调整边界。
- 新增 `src/lib/security/__tests__/nginx-security-headers.test.ts`，覆盖基础响应头、Vite 静态资源兼容性和 `/config.js` 缓存策略。

红灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：失败，错误为 `expected ... to contain 'add_header Content-Security-Policy'`。

绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：通过，`4` 个安全测试通过。

Docker OOM 红灯验证：

```bash
docker build -t chartdb-security-smoke .
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：`docker build` 首次失败于 `JavaScript heap out of memory`；补充回归测试失败，错误为 `expected ... to contain 'ENV NODE_OPTIONS=--max-old-space-size=4096'`。

Docker OOM 绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts src/lib/security/__tests__/browser-key-exposure.test.ts
docker build -t chartdb-security-smoke .
```

结果：聚焦安全测试通过，Docker 镜像构建通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
docker build -t chartdb-security-smoke .
docker run --rm -d -p 8080:80 --name chartdb-security-smoke chartdb-security-smoke
curl -I http://localhost:8080
curl -I http://localhost:8080/config.js
docker stop chartdb-security-smoke
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过 high gate；后续 `CHARTDB-F-002a` 已移除 AI SDK 依赖链，当前剩余 1 个 low 和 1 个 moderate，强制修复仍涉及 Monaco 破坏性变化。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。
- `docker build -t chartdb-security-smoke .` 通过。
- `curl -I http://localhost:8080` 返回 CSP、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`。
- `curl -I http://localhost:8080/config.js` 返回同样安全头，并额外返回 `Cache-Control: no-store`。

下一项建议：

- 进入 `CHARTDB-P1-005`：完成密钥、Markdown、Docker 安全审查。

### CHARTDB-P1-005：Phase 1 安全审查

状态：已完成本地验证。

变更摘要：

- 新增 `docs/安全风险登记.md`，按 High、Medium、Low 记录 Phase 1 剩余风险、缓解措施、暂不修复原因和后续处理路径。
- 确认浏览器端长期 OpenAI API key 暴露、Note Markdown raw HTML/XSS 风险、Docker/Nginx 缺少基础安全头这三类 high 风险均已关闭。
- 确认剩余 moderate 风险为 `monaco-editor -> dompurify` 生产依赖 advisory；当前 Note Markdown 不依赖 DOMPurify 执行 raw HTML 清洗，破坏性 Monaco 版本变更延后到依赖治理或 Phase 6。
- 确认 `/config.js` 的非敏感 `window.env` runtime config 保留为 Low 风险；后续 `CHARTDB-F-002a` 已关闭 AI SDK provider-utils 资源消耗 advisory。

红灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const required=['CHARTDB-P1-005','Phase 1 总体验收','High 风险','Medium 风险','Low 风险']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing phase 1 security review tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing phase 1 security review tokens: Phase 1 总体验收, High 风险, Medium 风险, Low 风险`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('docs/阶段验收记录.md','utf8'); const required=['CHARTDB-P1-005','Phase 1 总体验收','High 风险','Medium 风险','Low 风险']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing phase 1 security review tokens: '+missing.join(', ')); process.exit(1); }"
rg -n "High 风险|Medium 风险|Low 风险|Phase 2 准入结论" docs/安全风险登记.md
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`112` 个测试文件、`849` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过 high gate；后续 `CHARTDB-F-002a` 已移除 AI SDK 依赖链，当前剩余 `1` 个 low 和 `1` 个 moderate，已记录在 `docs/安全风险登记.md`。
- 安全命中扫描不再命中 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`；仅剩 `default.conf.template` 的 `window.env` 非敏感 runtime object。

## Phase 1 总体验收

退出门槛：

- `npm run lint`：已在 P1-005 完整验证中通过。
- `npm run test:ci`：已在 P1-005 完整验证中通过，记录结果为 `112` 个测试文件、`849` 个测试通过。
- `npm run build`：已在 P1-005 完整验证中通过，仍有既有 Vite chunk 过大警告，转入 Phase 6 性能优化。
- `npm audit --omit=dev --audit-level=high`：已在 P1-005 完整验证中通过，生产依赖 high/critical advisory 为 0。
- 浏览器端 API key：`Dockerfile` 和 `/config.js` 不再输出 `VITE_OPENAI_API_KEY` 或 `OPENAI_API_KEY`。
- Markdown XSS：Note Markdown raw HTML 风险已关闭，并有恶意 Markdown 回归测试。
- Docker/Nginx：静态页面和 `/config.js` 已有基础 CSP 与安全响应头；Docker smoke 已在 P1-004 通过。
- 风险登记：High 风险无开放项；Medium 风险为 Monaco/DOMPurify advisory；Low 风险为非敏感 `window.env` runtime config；AI SDK provider-utils advisory 已由 `CHARTDB-F-002a` 关闭。

结论：

- Phase 1：通过。
- 允许进入下一项 `CHARTDB-P2-000`：编写 Phase 2 Schema Core 与 Command 架构执行清单。

## Phase 2：Schema Core 与 Command 架构

### CHARTDB-P2-000：Phase 2 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/schema-core设计.md`，定义 Phase 2 的 schema-core 边界、目标目录、旧类型到新 domain type 的映射、command contract、validator、diff 和 undo/redo 兼容层。
- 明确 `CHARTDB-P2-001` 到 `CHARTDB-P2-006` 的自动开发顺序：先建立 model re-export，再定义 command contract，之后按 table、field/index/relationship、area/note/custom type、command history 分批迁移。
- 明确兼容层保留周期：从 `CHARTDB-P2-001` 到 `CHARTDB-P2-006` 全程保留旧 import path 和旧 history API；Phase 2 退出验收后再评估删除旧兼容层。
- 同步产品计划、工程实施计划和自动开发任务计划，下一项切到 `CHARTDB-P2-001`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/schema-core设计.md'; if(!fs.existsSync(p)){ console.error('missing docs/schema-core设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['schema-core','command','validator','diff','undo','redo','兼容层','CHARTDB-P2-001']; const missing=required.filter((token)=>!s.includes(token)); if(missing.length){ console.error('missing schema-core design tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/schema-core设计.md`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/schema-core设计.md'; if(!fs.existsSync(p)){ console.error('missing docs/schema-core设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['schema-core','command','validator','diff','undo','redo','兼容层','CHARTDB-P2-001']; const missing=required.filter((token)=>!s.includes(token)); if(missing.length){ console.error('missing schema-core design tokens: '+missing.join(', ')); process.exit(1); }"
rg -n "schema-core|command|validator|diff|undo|redo" docs/schema-core设计.md
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：合并前验证见下方本轮结果。

下一项建议：

- 进入 `CHARTDB-P2-001`：建立 `src/schema-core/model` 目录和领域模型出口，旧 import 路径通过 re-export 兼容，不修改业务行为。

### CHARTDB-P2-001：建立 schema-core 目录和领域模型出口

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/model` re-export 层，为 Diagram、Table、Field、Index、Relationship、Area、Note、CustomType、Dependency、Database 等领域对象提供新入口。
- 新增 `src/schema-core/model/__tests__/model-exports.test.ts`，验证新入口可继续使用既有 domain schema，并检查 `schema-core` 文件不直接引入 React、Dexie、Monaco、DOM 或浏览器 storage。
- 旧 `src/lib/domain` import path 保持不变，本轮未搬迁旧类型定义，未修改业务逻辑。

红灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/schema-core/model"`。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

下一项建议：

- 进入 `CHARTDB-P2-002`：定义 DiagramCommand、CommandResult、CommandContext 和 risk metadata。

### CHARTDB-P2-002：定义 DiagramCommand 基础类型

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands` 基础 contract，包含 `DiagramCommand`、`CommandContext`、`CommandResult`、`CommandRisk` 和 `ValidationIssue`。
- 新增 `createDiagramCommand`、`createSuccessResult`、`createValidationErrorResult` 和 `createCommandRisk` helper，为后续 table/field/relationship command 提供稳定输入输出协议。
- 新增 `src/schema-core/commands/__tests__/diagram-command.test.ts`，覆盖 command 创建、success result、validation error result 和 destructive risk metadata。
- 本轮未接入 `ChartDBProvider`，未迁移业务行为，旧 UI 和旧 state flow 保持不变。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-command.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/schema-core/commands"`。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-command.test.ts src/schema-core/model/__tests__/model-exports.test.ts
```

结果：通过，`2` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

本轮合并前结果：

- `npm run lint` 首次失败于 `diagram-command.ts` 的 Prettier 泛型换行格式，修复后通过。
- `npm run test:ci` 通过，`114` 个测试文件、`855` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-003`：迁移 AddTable、UpdateTable、DeleteTable command。

### CHARTDB-P2-003：迁移 Table command

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands/table-commands.ts`，定义 `table.add`、`table.update`、`table.delete` 和 `table.restore` command 创建 helper。
- 新增 `src/schema-core/commands/apply-table-command.ts`，用纯函数根据当前 diagram table state 输出下一状态、undo command、affected entity ids、validation error 与 cascade risk。
- 新增 `src/schema-core/commands/__tests__/table-commands.test.ts`，覆盖 Add table、Update table name、missing table validation 和 Delete table cascade risk。
- `ChartDBProvider` 的 `addTables`、`updateTable` 和 `removeTables` 已改为通过 table command 计算下一状态，同时保留既有 Dexie 写入、事件和旧 undo/redo 数据结构。
- 删除 table 会报告并移除关联 relationship/dependency；当前 Note 模型没有 table anchor 字段，因此本轮不改变 notes。字段和索引继续随 table payload 保留，细粒度 field/index 影响进入 `CHARTDB-P2-004`。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/table-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createAddTableCommand is not a function`，符合缺少 table command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/table-commands.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮合并前验证。

本轮合并前结果：

- `npm run lint` 首次失败于 Prettier 格式和一个未使用 import，修复后通过；补充 undo command 类型守卫后再次通过。
- `npm run test:ci` 通过，`115` 个测试文件、`859` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.26 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-004`：迁移 Field、Index、Relationship command。

### CHARTDB-P2-004：迁移 Field、Index、Relationship command

状态：已完成本地验证。

变更摘要：

- 新增 `src/schema-core/commands/field-commands.ts`、`index-commands.ts`、`relationship-commands.ts`，定义 field、index、relationship command 创建 helper。
- 新增 `apply-field-command.ts`、`apply-index-command.ts`、`apply-relationship-command.ts`，用纯函数根据当前 diagram state 输出下一状态、undo command、affected entity ids、validation error 与 cascade risk。
- 新增 `src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts`，覆盖删除 field 清理 relationship 和 index 引用、relationship 引用校验、cardinality 更新、index 语义保留和 add field undo。
- `ChartDBProvider` 的 field、index、relationship 入口已接入 command，同时保留既有 Dexie 写入、事件和 undo/redo action。删除 field 的 undo payload 会恢复被级联移除的 relationship。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createDeleteFieldCommand is not a function`，符合缺少 field/index/relationship command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`116` 个测试文件、`864` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P2-005`：迁移 Area、Note、CustomType command。

### CHARTDB-P2-005：迁移 Area、Note、CustomType command

提交：本轮提交，最终 SHA 见自动化 run log。

范围：

- 新增 `src/schema-core/commands/area-commands.ts`、`note-commands.ts`、`custom-type-commands.ts`，定义 area、note、custom type command 创建 helper。
- 新增 `apply-area-command.ts`、`apply-note-command.ts`、`apply-custom-type-command.ts`，用纯函数根据当前 diagram state 输出下一状态、undo command、affected entity ids 与 validation error。
- 新增 `src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts`，覆盖 Area add/delete undo、Note markdown 内容透明更新和 CustomType 被字段引用时禁止删除。
- `ChartDBProvider` 的 area、note、custom type 入口已接入 command，同时保留既有 Dexie 写入和 undo/redo action。删除 custom type 若发现字段引用，会停在 validation error，不写入 storage 或 history。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts
```

结果：首次因独立 worktree 未安装依赖停在 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `createAddAreaCommand is not a function`，符合缺少 area/note/custom type command 实现的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts src/pages/editor-page/canvas/note-node/__tests__/note-markdown-safety.test.tsx
```

结果：通过，`2` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

- `npm run lint` 通过。
- `npm run test:ci` 通过，`118` 个测试文件、`869` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

- `npm run lint` 通过。
- `npm run test:ci` 通过，`117` 个测试文件、`867` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过，生产依赖 high/critical 为 0；后续 `CHARTDB-F-002a` 已移除 AI SDK 依赖链，当前剩余 1 low + 1 moderate 继续沿用 `docs/安全风险登记.md` 的记录。
- 安全关键词扫描只命中 `default.conf.template` 的非敏感 `window.env` runtime object，未重新引入 `VITE_OPENAI_API_KEY`、`OPENAI_API_KEY`、`rehype-raw` 或 `dangerouslySetInnerHTML`。

下一项建议：

- 进入 `CHARTDB-P2-006`：接入统一 undo/redo command history。

### CHARTDB-P2-006：接入统一 undo/redo command history

状态：已完成本地验证。

提交：本轮提交，最终 SHA 见自动化 run log。

范围：

- 新增 `src/schema-core/commands/command-history.ts`，将成功 command result 转换为包含 redo command、undo command、affected entity ids 和 risk snapshot 的 command history entry。
- 新增 `src/schema-core/commands/__tests__/command-history.test.ts`，覆盖成功结果生成 history entry，以及 validation error 不写入 command history。
- `RedoUndoAction` 新增可选 `commandHistory` batch，旧 handler 继续按 `redoData` / `undoData` 执行。
- `ChartDBProvider` 中已迁移的 table、field、index、relationship、area、note 和 custom type 操作会在写入旧 undo stack 的同时附带 command metadata。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/command-history.test.ts
```

结果：失败，错误为 `createCommandHistoryEntry is not a function`，符合缺少 command history adapter 的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/command-history.test.ts src/schema-core/commands/__tests__/table-commands.test.ts src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts src/schema-core/commands/__tests__/visual-custom-type-commands.test.ts
```

结果：通过，`4` 个测试文件、`14` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`119` 个测试文件、`872` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`118` 个测试文件、`869` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-000`：定义 storage repository 和 migration 计划。

### CHARTDB-P3-000：定义 storage repository 和 migration 计划

状态：已完成本地验证。

范围：

- 新增 `docs/storage设计.md`，记录当前 Dexie 数据库名、版本、表结构、既有 migration 和 Provider 耦合风险。
- 明确 Phase 3 目标目录：`src/storage/db`、`src/storage/repositories`、`src/storage/transactions`、`src/storage/backup` 和 `src/storage/migrations`。
- 明确 repository、diagram transaction、backup/restore 的职责边界和禁止范围。
- 明确 `CHARTDB-P3-001` 到 `CHARTDB-P3-004` 的自动开发顺序。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/storage设计.md'; if (!fs.existsSync(p)) { console.error('missing docs/storage设计.md'); process.exit(1); } const s=fs.readFileSync(p,'utf8'); const required=['Dexie','repository','transaction','backup','restore','migration']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing storage plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing docs/storage设计.md`。

绿灯验证：

```bash
rg -n "Dexie|repository|transaction|backup|restore|migration" docs/storage设计.md
node -e "const fs=require('fs'); const p='docs/storage设计.md'; const s=fs.readFileSync(p,'utf8'); const required=['Dexie','repository','transaction','backup','restore','migration']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing storage plan tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：待本轮完整验证后回填。

下一项建议：

- 进入 `CHARTDB-P3-001`：创建 `src/storage/db` 并集中 Dexie schema。

### CHARTDB-P3-001：抽离 Dexie 数据库定义

状态：已完成本地验证。

范围：

- 新增 `src/storage/db/chartdb-dexie.ts`，统一创建 typed `ChartDB` Dexie instance。
- 新增 `src/storage/db/schema-versions.ts`，集中当前 Dexie 版本 `13`、store 声明和既有 upgrade migration。
- `StorageProvider` 不再内联声明完整 Dexie schema，改为通过 `createChartDBDexie()` 获取 db instance；现有 CRUD、diagram 读取和写入逻辑保持不变。
- 新增 `src/storage/db/__tests__/chartdb-dexie.test.ts`，覆盖数据库名、当前版本和当前 store 名称。

红灯验证：

```bash
npm run test:ci -- src/storage/db/__tests__/chartdb-dexie.test.ts
```

结果：失败，错误为 `Failed to resolve import "../chartdb-dexie"`，符合缺少 `src/storage/db` 模块的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/db/__tests__/chartdb-dexie.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`119` 个测试文件、`872` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告，归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-002`：抽 Repository API。

### CHARTDB-P3-002：抽 Repository API

状态：已完成本地验证。

范围：

- 新增 `src/storage/repositories/chartdb-repositories.ts`，集中 config、diagram filter、diagram、table、relationship、dependency、area、custom type 和 note repository API。
- `StorageProvider` 改为通过 `createChartDBRepositories()` 组合 repository 方法，不再直接访问 `db.diagrams`、`db_tables`、`notes` 等 Dexie table。
- 保留既有 StorageContext 行为：diagram 组合读取、relationship/custom type 名称排序、diagram id 更新级联到子实体、delete diagram 删除既有子实体。
- 新增 `src/storage/repositories/__tests__/chartdb-repositories.test.ts`，覆盖 repository 读写、组合读取、排序、缺失实体返回 `undefined` 和 Provider 边界。

红灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts
```

结果：安装依赖前先失败于 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `Failed to resolve import "../chartdb-repositories"`，符合缺少 repository 模块的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`120` 个测试文件、`876` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-003`：实现 diagram transaction service，覆盖 diagram 删除、替换和创建的一致性。

### CHARTDB-P3-003：实现 diagram transaction service

状态：已完成本地验证。

范围：

- 新增 `src/storage/transactions/diagram-transaction-service.ts`，统一封装 diagram 创建、删除和替换的 Dexie transaction。
- `createChartDBRepositories()` 的 diagram add/delete 已改为通过 transaction service 执行，删除 diagram 时同步清理 tables、relationships、dependencies、areas、custom types、notes 和 diagram filter。
- 新增 `src/storage/transactions/__tests__/diagram-transaction-service.test.ts`，覆盖 child entity 写入失败时回滚和 delete diagram 清理 filter。
- 扩展 `src/storage/repositories/__tests__/chartdb-repositories.test.ts`，确认 repository 对外 deleteDiagram 会清理 diagram filter。

红灯验证：

```bash
npm run test:ci -- src/storage/transactions/__tests__/diagram-transaction-service.test.ts
```

结果：安装依赖前先失败于 `vitest: command not found`；执行 `npm install` 后重跑，失败于 `Failed to resolve import "../diagram-transaction-service"`，符合缺少 transaction service 的预期红灯。

绿灯验证：

```bash
npm run test:ci -- src/storage/repositories/__tests__/chartdb-repositories.test.ts src/storage/transactions/__tests__/diagram-transaction-service.test.ts
```

结果：通过，`2` 个测试文件、`7` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`121` 个测试文件、`879` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,242.33 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P3-004`：备份恢复版本化。

### CHARTDB-P3-004：备份恢复版本化

状态：已完成本地验证。

变更摘要：

- 新增 `src/storage/backup/backup-format.ts` 和 `src/storage/backup/index.ts`，定义 `chartdb.backup` + `schemaVersion: 1` 的 backup 文件格式。
- backup 导出写入 `createdAt`、`source`、`appVersion`、`diagramCount` 和完整 `diagrams` payload。
- restore 入口在恢复前校验 JSON、format、schemaVersion、diagram count 和 diagram 基础字段；不兼容版本会给出明确错误。
- `src/lib/export-import-utils.ts` 的现有 JSON 导出入口改用 versioned backup，导入入口保留旧单 diagram JSON 兼容路径。
- 新增 `docs/备份恢复格式.md` 记录格式字段、恢复规则和后续 migration 边界。

红灯验证：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts
```

结果：失败，错误为 `Failed to resolve import "@/storage/backup"`。

绿灯验证：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`122` 个测试文件、`883` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-000`：定义 dialect contract 和迁移顺序。

### CHARTDB-P4-000：定义 dialect contract 和迁移顺序

状态：已完成本地验证。

变更摘要：

- 新增 `docs/方言能力矩阵.md`，作为 Phase 4 importer / exporter 插件化执行清单。
- 矩阵覆盖 PostgreSQL、MySQL、MariaDB、SQLite、SQL Server、Oracle、CockroachDB、ClickHouse 和 DBML。
- 每个方言记录 import 状态、export 状态、主要支持、unsupported syntax 和 warning 规则。
- 明确统一 contract 要求：`ImportResult`、`ExportResult`、`warnings`、`unsupportedObjects`、`sourceMap` 和 `riskLevel`。
- 明确后续迁移顺序：先做 `CHARTDB-P4-001` common contract，再迁移 PostgreSQL importer，之后迁移其它 dialect wrapper 和 exporter result。

红灯验证：

本任务为 PLAN 切片，不涉及运行时代码行为；以缺失文档作为红灯基线：

```bash
test -f docs/方言能力矩阵.md
```

结果：失败，`docs/方言能力矩阵.md` 不存在。

绿灯验证：

```bash
rg -n "PostgreSQL|MySQL|SQLite|SQL Server|Oracle|DBML|capability|warning" docs/方言能力矩阵.md
```

结果：通过，文档包含验收要求中的方言、capability 和 warning 规则。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`122` 个测试文件、`883` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-001`：创建 dialect contract 和 result type。

### CHARTDB-P4-001：创建 dialect contract 和 result type

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/common/importer.ts`，定义 `SchemaImporter`、`ImportResult`、`DialectWarning`、`UnsupportedDialectObject`、`DialectSourceMap` 和旧 importer adapter。
- 新增 `src/dialects/common/exporter.ts`，定义 `SchemaExporter`、`ExportResult`、`riskLevel` 和旧 exporter adapter。
- 新增 `src/dialects/common/capability-matrix.ts`，定义 `SupportLevel`、capability map、缺省 unsupported helper。
- 新增 `src/dialects/common/__tests__/capability-matrix.test.ts`，覆盖 capability 缺省值、unsupported syntax/warning rules、import/export result 字段和 legacy adapter 兼容。

红灯验证：

```bash
npm run test:ci -- src/dialects/common/__tests__/capability-matrix.test.ts
```

结果：失败，错误为 `Failed to resolve import "../capability-matrix"`。

绿灯验证：

```bash
npm run test:ci -- src/dialects/common/__tests__/capability-matrix.test.ts
```

结果：通过，`1` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`123` 个测试文件、`889` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-P4-002`：迁移 PostgreSQL importer。

### CHARTDB-P4-002：迁移 PostgreSQL importer

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/postgresql/importer.ts`，提供 `postgresqlSchemaImporter.importSchema()`，把 PostgreSQL importer 接入统一 `ImportResult` contract。
- 新增 `src/dialects/postgresql/capabilities.ts`，声明 PostgreSQL import/export capability、unsupported syntax 和 warning rules。
- 新增 `src/dialects/postgresql/parser/legacy-parser.ts`，以 wrapper 方式复用旧 `fromPostgres` parser，并保持旧 diagram normalize、排序和 primary key index 行为。
- 新增 `src/dialects/postgresql/warnings.ts`，将 RLS、policy、extension、trigger 和 function 输出为结构化 `warnings` 与 `unsupportedObjects`。
- 新增 `src/dialects/postgresql/__tests__/importer.test.ts`，覆盖 wrapper fixture、capability levels 和 unsupported warning。

红灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/importer.test.ts
```

结果：失败，错误为 `Failed to resolve import "../importer"`。

绿灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/importer.test.ts
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

PostgreSQL regression 验证：

```bash
npm run test:ci -- src/lib/data/sql-import/dialect-importers/postgresql src/dialects/postgresql/__tests__/importer.test.ts
```

结果：通过，`50` 个测试文件、`171` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`124` 个测试文件、`891` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `code-editor` chunk 约 `15,243.94 kB`，继续归入 Phase 6 性能优化范围。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；生产依赖仍报告既有 `5` 个 low 和 `1` 个 moderate。
- 安全关键词扫描仅命中 `default.conf.template` 中既有非敏感 `window.env` runtime object，本轮未新增 API key、`rehype-raw` 或 `dangerouslySetInnerHTML` 风险面。

下一项建议：

- 进入 `CHARTDB-P4-003`：迁移 MySQL、MariaDB、SQLite、SQL Server 和 Oracle wrapper。

### CHARTDB-P4-003：迁移 MySQL、MariaDB、SQLite、SQL Server、Oracle

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/common/legacy-sql-importer.ts`，统一旧 SQL parser 到 `Diagram` 的转换、表排序和 primary key index normalize。
- 新增 `src/dialects/mysql`、`src/dialects/mariadb`、`src/dialects/sqlite`、`src/dialects/sqlserver` 和 `src/dialects/oracle`，每个方言提供 `SchemaImporter` wrapper、capabilities 和 warning 提取。
- MariaDB 明确使用 MySQL parser fallback，并用 `experimental` capability 与 `mariadb.mysql_fallback` warning 暴露产品风险。
- MySQL ENGINE/charset、MariaDB sequence/engine、SQLite virtual table、SQL Server procedure、Oracle sequence/package 等 unsupported 或降级语义进入结构化 `warnings` / `unsupportedObjects`。
- 新增 `src/dialects/__tests__/sql-dialect-importers.test.ts`，覆盖五个 wrapper 的统一 `ImportResult` contract、capability metadata 和 warning 输出。

红灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；安装依赖后重跑，按预期失败于 `Failed to resolve import "../mysql"`，确认 wrapper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

SQL dialect regression 验证：

```bash
npm run test:ci -- src/lib/data/sql-import/dialect-importers/mysql src/lib/data/sql-import/dialect-importers/sqlite src/lib/data/sql-import/dialect-importers/sqlserver src/lib/data/sql-import/dialect-importers/oracle src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：通过，`23` 个测试文件、`122` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P4-004`：DBML 进入 dialect pipeline。

### CHARTDB-P4-004：DBML 进入 dialect pipeline

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/dbml/importer.ts` 和 `src/dialects/dbml/exporter.ts`，让 DBML import/export 复用既有逻辑并输出统一 `ImportResult` / `ExportResult`。
- 新增 `src/dialects/dbml/capabilities.ts`，声明 DBML import/export capability、unsupported syntax 和 warning rules。
- 新增 `src/dialects/dbml/warnings.ts`，将 `TableGroup`、standalone `Note`、空表和重复表输出为结构化 `warnings` 与 `unsupportedObjects`。
- 将 dialect contract 类型扩展为 `DatabaseType | 'dbml'`，DBML 只作为 pipeline dialect 标识，不进入数据库选择和元数据系统。
- 新增 `src/dialects/dbml/__tests__/dbml-pipeline.test.ts`，覆盖 DBML round-trip wrapper、capability levels 和 unsupported warning。

红灯验证：

```bash
npm run test:ci -- src/dialects/dbml/__tests__/dbml-pipeline.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；安装依赖后重跑，按预期失败于 `Failed to resolve import "../index"`，确认 DBML dialect wrapper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/dialects/dbml/__tests__/dbml-pipeline.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P4-005`：导入前展示 preview、warnings 和 object counts。

### CHARTDB-P4-005：导入 Preview Flow

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/import/import-preview.ts`，将统一 `ImportResult` 汇总为导入 preview summary。
- 新增 `src/features/import/import-preview-panel.tsx`，在导入对话框中展示 tables、relationships、customTypes、warnings 和 unsupportedObjects。
- `ImportDatabaseDialog` 改为两步确认：首次点击只解析并暂存 preview，不写入 IndexedDB；用户确认后才写入 tables、relationships 和 customTypes。
- DBML 与 SQL DDL preview 走 Phase 4 dialect wrapper；Smart Query JSON 保持既有 metadata import 兼容路径。

红灯验证：

```bash
npm run test:ci -- src/features/import/__tests__/import-preview.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，按预期失败于 `Failed to resolve import "../import-preview"`，确认 preview helper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/features/import/__tests__/import-preview.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-000`：定义 UX 和可访问性验收矩阵。

### CHARTDB-P5-000：Phase 5 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/可访问性与核心流程验收.md`，定义首次进入、Smart Query、导入 Preview、编辑画布和设置中心的验收矩阵。
- 矩阵覆盖桌面、移动、键盘、可访问性和失败状态，作为 `CHARTDB-P5-001` 到 `CHARTDB-P5-004` 的统一验收依据。
- 明确 Phase 5 仍禁止账号登录、云端 diagram 存储、团队 workspace、持久化 AI secret 和远端 schema 上传。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P5-001`。

红灯验证：

```bash
rg -n "首次进入|Smart Query|aria|键盘|设置" docs/可访问性与核心流程验收.md
```

结果：按预期失败，目标文档尚不存在。

绿灯验证：

```bash
rg -n "首次进入|Smart Query|aria|键盘|设置" docs/可访问性与核心流程验收.md
```

结果：通过，命中核心流程、可访问性和设置验收条目。

下一项建议：

- 进入 `CHARTDB-P5-001`：重做首次进入入口。

### CHARTDB-P5-001：重做首次进入入口

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/onboarding/onboarding-dialog.tsx`，在首次无本地 diagram 时提供数据库选择、导入现有数据库、新建空白图、模板示例和 JSON backup 导入入口。
- `useDiagramLoader()` 在空状态下展示 onboarding dialog，已有 diagram、默认 diagram 和直接 URL 加载路径保持不变。
- 新建空白图只在选择数据库和启动方式后执行；若 `addDiagram` 或 `updateConfig` 失败，会删除已创建 diagram 并显示失败提示。
- 新增 `src/features/onboarding/__tests__/onboarding-dialog.test.tsx`，覆盖三入口可见、未选库提示、新建空白图、导入和模板路径。

红灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试先失败于导入路径缺少数据库选择，确认未选库 gating 生效；修正测试交互后再失败于模板入口需点击 Continue，确认流程是“选择入口 -> 继续”。

绿灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-002`：Smart Query Wizard。

### CHARTDB-P5-002：Smart Query Wizard

状态：已完成本地验证。

变更摘要：

- `SmartQueryInstructions` 新增 `Smart Query Wizard` 结构化说明，按选择数据库、复制 Smart Query、粘贴 JSON、Preview tables/relationships/warnings、Confirm import 五步呈现。
- Smart Query 说明明确 ChartDB 不要求数据库密码，用户不应粘贴连接串或 secret。
- `ImportDatabaseDialog` 捕获 preview 解析失败和无可导入对象结果，并在导入粘贴区显示可操作错误，指向 Smart Query JSON、SQL syntax 或 dialect limitations。
- 新建 diagram 的首次导入路径也接入 Preview/Confirm；大 Smart Query 结果在确认后仍保留进入 SelectTables 的旧分支。
- 新增 `smart-query-instructions.test.tsx`，覆盖无密码原则和五步向导文案。

红灯验证：

```bash
npm run test:ci -- src/dialogs/common/import-database/instructions-section/instructions/__tests__/smart-query-instructions.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于找不到 `Smart Query Wizard` heading，确认旧 Smart Query 说明仍是两步文本。

绿灯验证：

```bash
npm run test:ci -- src/dialogs/common/import-database/instructions-section/instructions/__tests__/smart-query-instructions.test.tsx
```

结果：通过，`1` 个测试文件、`1` 个测试通过；测试输出无 i18n 初始化警告。

下一项建议：

- 进入 `CHARTDB-P5-003`：全局 aria-label 和键盘路径修复。

### CHARTDB-P5-003：全局 aria-label 和键盘路径修复

状态：已完成本地验证。

变更摘要：

- 新增 `src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts`，覆盖 Phase 5 核心 icon-only controls 和 Monaco 编辑器用途名称契约。
- `CodeSnippet` 的复制按钮和额外 action 按钮补充可访问名称，复制按钮沿用当前 copied / copy tooltip 文案。
- `DialogContent` 的 back button 补充 `Go back` 名称。
- 画布 Toolbar 的 filter、show all、zoom、reorder、undo、redo 等图标按钮补充明确 `aria-label`。
- Smart Query output、SQL query 和 DBML Monaco 编辑器补充用途名称，便于屏幕阅读器区分输入区和代码展示区。

红灯验证：

```bash
npm run test:ci -- src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `CodeSnippet` 缺少 `aria-label={copyButtonLabel}`，确认核心复制按钮尚未命名。

绿灯验证：

```bash
npm run test:ci -- src/lib/accessibility/__tests__/phase5-a11y-contract.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P5-004`：设置中心。

### CHARTDB-P5-004：设置中心

状态：已完成本地验证。

变更摘要：

- 新增 `src/features/settings/settings-dialog.tsx`、`display-settings.tsx`、`privacy-settings.tsx` 和 `keyboard-shortcuts-settings.tsx`，通过左侧 Sidebar 的 `Settings` 入口打开。
- 设置中心集中管理 theme、language、minimap、field attributes、scroll action、AI mode、Self-hosted Gateway endpoint/model hint、导出 backup、恢复 backup 和清理本地 diagram 的危险操作入口。
- `LocalConfigProvider` 为 localStorage 读写增加 try/catch 容错；存储不可用时设置中心显示 session-only 降级说明。
- BYOK key 继续保持 session-only，不写入 localStorage、sessionStorage 或 settings state。

红灯验证：

```bash
npm run test -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：第一次因 fresh worktree 缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于缺少 `src/features/settings/settings-dialog.tsx`，确认设置中心尚未实现。

绿灯验证：

```bash
npm run test -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

下一项建议：

- 进入 `CHARTDB-P6-000`：定义 Phase 6 性能优化执行清单。

### CHARTDB-P6-000：Phase 6 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/性能基线与优化计划.md`，定义首屏 bundle、Monaco chunk、模板数据 chunk、large schema import、layout worker 和 CI 性能预算指标。
- 明确 Phase 6 执行顺序：Monaco 懒加载、模板 lazy registry、Parser/layout worker 化、Bundle budget 和性能回归记录。
- 约束性能优化不得破坏 Phase 1 安全边界和 Phase 5 可访问性边界。

红灯验证：

```bash
rg -n "bundle|Monaco|worker|large schema|首屏" docs/性能基线与优化计划.md
```

结果：失败，`docs/性能基线与优化计划.md` 不存在，确认 Phase 6 执行清单尚未建立。

绿灯验证：

```bash
rg -n "bundle|Monaco|worker|large schema|首屏" docs/性能基线与优化计划.md
```

结果：通过，文档包含 bundle、Monaco、worker、large schema 和首屏指标。

下一项建议：

- 进入 `CHARTDB-P6-001`：仅在代码编辑器路径加载 Monaco，并记录 `npm run build` chunk 变化。

### CHARTDB-P6-001：Monaco 懒加载

状态：已完成本地验证。

变更摘要：

- 新增 `src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts`，防止 `CodeSnippet` 外层模块和 DBML highlight helper 静态加载 Monaco runtime。
- 新增 `src/components/code-snippet/code-snippet-editor.tsx`，把 `useMonaco`、theme setup、worker config 和实际 Monaco editor 渲染移入懒加载子组件。
- `src/components/code-snippet/code-snippet.tsx` 外层只保留复制按钮、附加动作和 Suspense 壳，继续导出懒加载 `Editor` / `DiffEditor` 供现有代码编辑路径使用。
- `src/components/code-snippet/dbml/utils.ts` 改为 type-only Monaco 引用，错误高亮使用 Monaco 兼容的 plain range object。

红灯验证：

```bash
npm run test -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：先失败于 `CodeSnippet` 外层静态 `@monaco-editor/react` / `./config.ts` / theme import；扩展断言后再次失败于 `dbml/utils.ts` 的 `import * as monaco from 'monaco-editor'`。

绿灯验证：

```bash
npm run test -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：通过，`2` 个懒加载契约测试通过。

完整验证：

```bash
npm run build
```

结果：通过，仍有既有大 chunk 警告。chunk 变化：

- 基线：`code-editor-DNXSDyTK.js` 为 `15,279.59 kB` / gzip `2,787.98 kB`，入口 `index-Ca7JE0_R.js` 为 `2,609.63 kB` / gzip `513.31 kB`。
- 本轮后：`code-editor-BHvScKzX.js` 为 `3,788.30 kB` / gzip `976.92 kB`，入口 `index-C-ERHBci.js` 为 `2,609.63 kB` / gzip `513.31 kB`。
- 剩余：`editor-page-T0odRZ2l.js` 为 `11,472.77 kB` / gzip `1,806.40 kB`，主要转入 DBML/parser 与后续 worker 化治理。

下一项建议：

- 进入 `CHARTDB-P6-002`：模板 lazy registry。

### CHARTDB-P6-002：模板 lazy registry

状态：已完成本地验证。

变更摘要：

- 新增 `src/templates-data/template-manifest.ts`，将模板列表所需 metadata 与完整 diagram 数据拆分。
- 新增 `src/templates-data/__tests__/template-lazy-registry.test.ts`，防止列表路径重新静态加载完整模板聚合模块。
- `src/templates-data/templates-data.ts` 移除全部模板模块静态 import，改为通过 `loadTemplateBySlug()` 和 per-template dynamic import 加载完整模板。
- `src/templates-data/template-utils.ts`、`src/pages/templates-page/templates-page.tsx` 和 `TemplateCard` 改为消费 `TemplateManifest`，模板列表不访问 `diagram`。
- `src/router.tsx` 的模板详情和 clone loader 改为按 slug 动态加载完整模板。

红灯验证：

```bash
npm run test -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `template-utils.ts` 仍动态导入 `templates-data` 且 `template-manifest.ts` 不存在，确认当前列表路径仍会加载完整模板。

绿灯验证：

```bash
npm run test -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：通过，`2` 个模板 lazy registry 契约测试通过。

完整验证：

```bash
npm run build
```

结果：通过，仍有既有大 chunk 警告。chunk 变化：

- 本轮后：`employee-db-romprqJV.js`、`wordpress-db-B8pCH5_j.js`、`monica-db-Bj9lUCcR.js` 等模板数据已拆成独立 per-template JS chunk。
- 入口：`index-BOUqD14g.js` 为 `2,609.63 kB` / gzip `513.32 kB`。
- 剩余：`editor-page-NpB-GzH7.js` 为 `11,472.80 kB` / gzip `1,806.46 kB`，主要转入 DBML/parser 与后续 worker 化治理。

下一项建议：

- 进入 `CHARTDB-P6-003`：Parser 和 layout worker 化。

### CHARTDB-P6-003：Parser 和 layout worker 化

状态：已完成本地验证。

变更摘要：

- 新增 `src/workers/worker-client.ts`，统一 module worker 调用、结构化错误和无 Worker fallback。
- 新增 `src/workers/import-worker/import-worker.ts` 与 `src/workers/import-worker/import-preview-client.ts`，导入 Preview parse 默认经 worker 执行，主线程实现拆到 `src/features/import/import-preview-core.ts` 作为 fallback。
- 新增 `src/workers/layout-worker/layout-worker.ts` 与 `src/workers/layout-worker/layout-client.ts`，Area auto arrange 和 Move to Area 改为异步 worker client 计算布局。
- 新增 `src/workers/__tests__/worker-client.test.ts`、`src/features/import/__tests__/import-preview-worker-routing.test.ts` 和 `src/workers/layout-worker/__tests__/layout-client.test.ts`。
- 补充 `src/workers/__tests__/worker-client-progress.test.ts` 和 `src/features/import/__tests__/import-preview-progress-panel.test.tsx`，覆盖 worker progress、cancel 和导入 preview 运行中 UI。

聚焦验证：

```bash
npm run test -- src/workers/__tests__/worker-client.test.ts src/features/import/__tests__/import-preview-worker-routing.test.ts src/features/import/__tests__/import-preview.test.ts src/workers/layout-worker/__tests__/layout-client.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑通过，`4` 个文件 / `5` 个测试通过。

完整验证：

```bash
npm run lint
npm run build
```

结果：通过。`npm run build` 输出仍有既有大 chunk 警告；入口 `index` 为 `2,609.63 kB` / gzip `513.31 kB`，`code-editor` 为 `3,788.30 kB` / gzip `976.91 kB`，`editor-page` 为 `11,474.96 kB` / gzip `1,807.52 kB`。本轮主要降低 large schema import 和 area layout 的主线程阻塞风险，parser 依赖体积仍留给后续 bundle budget 治理。导入 preview 运行中显示 progress，并可 cancel 当前 worker task；100 tables / 500 tables smoke 方法已记录在 `docs/性能基线与优化计划.md`。

下一项建议：

- 进入 `CHARTDB-P7-000`：定义发布门禁和文档补齐范围。

## Phase 7：发布治理与文档

### CHARTDB-P7-000：Phase 7 执行清单

状态：已完成本地验证。

变更摘要：

- 新增 `docs/发布检查清单.md`，定义 release gate、Docker smoke、文档 gate、回滚与失败处理、自托管 AI Gateway 边界。
- 发布前检查覆盖 production audit、lint、test、build、`git diff --check`、安全 rg 扫描和 Docker smoke。
- 明确 Docker daemon 不可用时不得自动放行发布，必须记录替代验证和人工补跑要求。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-001`。

红灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/发布检查清单.md'; if (!fs.existsSync(p)) { console.error('missing docs/发布检查清单.md'); process.exit(1); }"
```

结果：失败，错误为 `missing docs/发布检查清单.md`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const p='docs/发布检查清单.md'; const s=fs.readFileSync(p,'utf8'); const required=['release','Docker','audit','test','build','smoke','回滚','自托管']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing release checklist tokens: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
rg -n "release|Docker|audit|test|build|smoke" docs/发布检查清单.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P7-001`：发布 workflow gate。

### CHARTDB-P7-001：发布 workflow gate

状态：已完成本地验证。

变更摘要：

- 在 `.github/workflows/publish.yaml` 的正式 multi-arch 镜像 push 前增加 Docker smoke build/run gate。
- Docker smoke 使用 buildx `push: false`、`load: true` 构建本地 `linux/amd64` 镜像，启动容器后检查 `/` 和 `/config.js`。
- smoke gate 验证静态页面响应包含 CSP、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`，并验证 `/config.js` 返回 `Cache-Control: no-store`。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-002`。

红灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('.github/workflows/publish.yaml','utf8'); const required=['Docker smoke','push: false','load: true']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing publish docker smoke gate: '+missing.join(', ')); process.exit(1); }"
```

结果：失败，错误为 `missing publish docker smoke gate: Docker smoke, push: false, load: true`。

绿灯验证：

```bash
node -e "const fs=require('fs'); const s=fs.readFileSync('.github/workflows/publish.yaml','utf8'); const required=['Docker smoke','push: false','load: true','curl --fail','cache-control: no-store','content-security-policy']; const missing=required.filter((token)=>!s.includes(token)); if (missing.length) { console.error('missing publish docker smoke gate: '+missing.join(', ')); process.exit(1); }"
```

结果：通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过；安全扫描仅命中 `default.conf.template` 中非敏感 `window.env` runtime object。

下一项建议：

- 进入 `CHARTDB-P7-002`：补齐工程文档。

### CHARTDB-P7-002：补齐工程文档

状态：已完成本地验证。

变更摘要：

- README 新增文档导航，明确默认本地优先、无账号、无数据库密码，并链接架构、部署安全、AI 隐私、备份恢复、方言能力、导入导出和测试策略文档。
- 新增 `docs/架构说明.md`，记录 React 入口、schema-core、storage、dialects、workers、AI 与发布边界。
- 新增 `docs/导入导出接口约定.md`，记录 `ImportResult`、`ExportResult`、Import Preview、backup 和 AI-assisted export 约束。
- 新增 `docs/测试策略.md`，记录本地门禁、TDD、核心测试范围、UI smoke、发布 Docker smoke、安全扫描和性能记录要求。
- 新增 `.github/dependabot.yml`，每周检查 npm 和 GitHub Actions 依赖，生产安全更新独立分组。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P7-003`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingFiles = requiredFiles.filter((file) => !fs.existsSync(file));
const readme = fs.readFileSync('README.md', 'utf8');
const requiredReadmeTokens = [
  '本地优先',
  '无账号',
  '无数据库密码',
  'docs/部署与安全配置.md',
  'docs/备份恢复格式.md',
  'docs/方言能力矩阵.md',
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingTokens = requiredReadmeTokens.filter((token) => !readme.includes(token));
if (missingFiles.length || missingTokens.length) {
  console.error('missing P7-002 docs gate');
  if (missingFiles.length) console.error(`files: ${missingFiles.join(', ')}`);
  if (missingTokens.length) console.error(`README tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，缺少 `docs/架构说明.md`、`docs/导入导出接口约定.md`、`docs/测试策略.md`，README 缺少本地优先、无账号、无数据库密码和文档链接 token。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingFiles = requiredFiles.filter((file) => !fs.existsSync(file));
const readme = fs.readFileSync('README.md', 'utf8');
const requiredReadmeTokens = [
  '本地优先',
  '无账号',
  '无数据库密码',
  'docs/部署与安全配置.md',
  'docs/备份恢复格式.md',
  'docs/方言能力矩阵.md',
  'docs/架构说明.md',
  'docs/导入导出接口约定.md',
  'docs/测试策略.md',
];
const missingTokens = requiredReadmeTokens.filter((token) => !readme.includes(token));
if (missingFiles.length || missingTokens.length) {
  console.error('missing P7-002 docs gate');
  if (missingFiles.length) console.error(`files: ${missingFiles.join(', ')}`);
  if (missingTokens.length) console.error(`README tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "本地优先|安全|部署|备份|恢复|方言|AI" README.md docs
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P7-003`：Issue template 和贡献规则。

### CHARTDB-P7-003：Issue template 和贡献规则

状态：已完成本地验证。

变更摘要：

- 新增 `.github/ISSUE_TEMPLATE/bug_report.yml`，要求 bug 报告包含复现步骤、版本环境和可选脱敏 fixture。
- 新增 `.github/ISSUE_TEMPLATE/dialect_regression.yml`，要求方言 import/export regression 附数据库类型、版本、流程、SQL/DBML/Smart Query fixture、预期对象数量和实际结果。
- 新增 `.github/ISSUE_TEMPLATE/feature_request.yml`，要求说明本地优先影响，并确认不默认引入登录或自动上传。
- 新增 `.github/ISSUE_TEMPLATE/security_report.md` 和 `config.yml`，关闭空 issue 并引导安全问题走 GitHub private vulnerability reporting。
- 更新 `CONTRIBUTING.md`，补充本地优先边界、PR 验证、dialect regression fixture、security private report 和测试要求。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P8-000`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  '.github/ISSUE_TEMPLATE/bug_report.yml',
  '.github/ISSUE_TEMPLATE/feature_request.yml',
  '.github/ISSUE_TEMPLATE/dialect_regression.yml',
  '.github/ISSUE_TEMPLATE/security_report.md',
  '.github/ISSUE_TEMPLATE/config.yml',
];
const missing = requiredFiles.filter((file) => !fs.existsSync(file));
const contributing = fs.existsSync('CONTRIBUTING.md') ? fs.readFileSync('CONTRIBUTING.md', 'utf8') : '';
const requiredTokens = ['security', 'dialect', 'regression', 'reproduction', 'fixture', 'local-first', 'private report'];
const missingTokens = requiredTokens.filter((token) => !contributing.toLowerCase().includes(token));
if (missing.length || missingTokens.length) {
  console.error('missing P7-003 governance gate');
  if (missing.length) console.error(`files: ${missing.join(', ')}`);
  if (missingTokens.length) console.error(`CONTRIBUTING tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，缺少 `.github/ISSUE_TEMPLATE/bug_report.yml`、`feature_request.yml`、`dialect_regression.yml`、`security_report.md`、`config.yml`，`CONTRIBUTING.md` 缺少 security、dialect、regression、reproduction、fixture、local-first、private report token。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const requiredFiles = [
  '.github/ISSUE_TEMPLATE/bug_report.yml',
  '.github/ISSUE_TEMPLATE/feature_request.yml',
  '.github/ISSUE_TEMPLATE/dialect_regression.yml',
  '.github/ISSUE_TEMPLATE/security_report.md',
  '.github/ISSUE_TEMPLATE/config.yml',
];
const missing = requiredFiles.filter((file) => !fs.existsSync(file));
const contributing = fs.existsSync('CONTRIBUTING.md') ? fs.readFileSync('CONTRIBUTING.md', 'utf8') : '';
const requiredTokens = ['security', 'dialect', 'regression', 'reproduction', 'fixture', 'local-first', 'private report'];
const missingTokens = requiredTokens.filter((token) => !contributing.toLowerCase().includes(token));
if (missing.length || missingTokens.length) {
  console.error('missing P7-003 governance gate');
  if (missing.length) console.error(`files: ${missing.join(', ')}`);
  if (missingTokens.length) console.error(`CONTRIBUTING tokens: ${missingTokens.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "security|dialect|regression|reproduction|fixture|local-first|private report" .github CONTRIBUTING.md
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过。

补充说明：后续 `CHARTDB-F-002a` 已移除 AI SDK 依赖链，production audit 当前保留 1 low + 1 moderate advisory，高危门槛通过；安全关键词扫描仅命中 `default.conf.template` 的非敏感 `window.env` runtime object。

下一项建议：

- 进入 `CHARTDB-P8-000`：Cloud/Team 边界计划。

## Phase 8：可选 Cloud/Team 预研

### CHARTDB-P8-000：Cloud/Team 边界计划

状态：已完成本地验证。

变更摘要：

- 新增 `docs/可选账号登录与团队协作预研.md`，明确 Phase 8 只做预研文档和接口边界，不新增登录代码。
- 明确不登录仍可完整使用 OSS Core，本地 diagram 不自动上传，云同步必须用户主动开启。
- 定义可选 Workspace、Team、权限、AuditLog、租户隔离、数据导出、数据删除和权限 negative tests。
- 同步产品计划、工程实施计划和自动任务计划，下一项切到 `CHARTDB-P8-001`。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
if (!fs.existsSync(p)) {
  console.error(`missing ${p}`);
  process.exit(1);
}
const s = fs.readFileSync(p, 'utf8');
const required = [
  '不登录仍可完整使用 OSS Core',
  '本地 diagram 不自动上传',
  '用户主动开启云同步',
  'Workspace',
  'Team',
  '权限',
  'AuditLog',
  '租户隔离',
  '数据导出',
  '数据删除',
  'negative tests',
  '不新增登录代码',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing Cloud/Team boundary tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，错误为 `missing docs/可选账号登录与团队协作预研.md`。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
if (!fs.existsSync(p)) {
  console.error(`missing ${p}`);
  process.exit(1);
}
const s = fs.readFileSync(p, 'utf8');
const required = [
  '不登录仍可完整使用 OSS Core',
  '本地 diagram 不自动上传',
  '用户主动开启云同步',
  'Workspace',
  'Team',
  '权限',
  'AuditLog',
  '租户隔离',
  '数据导出',
  '数据删除',
  'negative tests',
  '不新增登录代码',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing Cloud/Team boundary tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "账号|登录|Workspace|Team|权限|审计|本地模式|云同步|AuditLog|negative tests" docs/可选账号登录与团队协作预研.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-P8-001`：Cloud/Team 技术栈选型记录。

### CHARTDB-P8-001：Cloud/Team 技术栈选型记录

状态：已完成本地验证。

变更摘要：

- 在 `docs/可选账号登录与团队协作预研.md` 补齐技术栈选型记录。
- 推荐方案为 `NestJS + PostgreSQL + Auth.js/OIDC adapter + REST API + append-only sync log`。
- 对比方案为 Fastify 轻量 API，并明确不推荐把 BaaS SDK 作为 OSS Core 默认依赖。
- 明确 OSS Core 隔离方式、Cloud/Team 不作为默认依赖、sync/push-pull 和 conflict 处理边界。
- 同步产品计划、工程实施计划和自动任务计划；本轮未新增登录代码。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
const s = fs.readFileSync(p, 'utf8');
const required = [
  '推荐方案',
  '方案 A',
  '方案 B',
  'NestJS',
  'Fastify',
  'PostgreSQL',
  'Auth.js',
  'OAuth',
  'SSO',
  'sync',
  'conflict',
  'OSS Core 隔离',
  'Cloud/Team 不作为默认依赖',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing P8-001 tech spike tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，错误为 `missing P8-001 tech spike tokens: 推荐方案, 方案 A, 方案 B, Auth.js, sync, conflict, OSS Core 隔离, Cloud/Team 不作为默认依赖`。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const p = 'docs/可选账号登录与团队协作预研.md';
const s = fs.readFileSync(p, 'utf8');
const required = [
  '推荐方案',
  '方案 A',
  '方案 B',
  'NestJS',
  'Fastify',
  'PostgreSQL',
  'Auth.js',
  'OAuth',
  'SSO',
  'sync',
  'conflict',
  'OSS Core 隔离',
  'Cloud/Team 不作为默认依赖',
];
const missing = required.filter((token) => !s.includes(token));
if (missing.length) {
  console.error(`missing P8-001 tech spike tokens: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：通过。

完整验证：

```bash
rg -n "NestJS|Fastify|PostgreSQL|Auth|OAuth|SSO|sync|conflict" docs/可选账号登录与团队协作预研.md
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- Phase 8 预研队列已完成边界计划和技术栈选型；若继续推进，需要另开独立 Cloud/Team 工程计划，不直接进入账号登录实现。

## 后续优化批次 F：快速修复

### CHARTDB-F-003：entrypoint.sh 移除 OPENAI_API_KEY

状态：已完成本地验证。

变更摘要：

- 从 `entrypoint.sh` 的 `envsubst` 白名单中移除 `${OPENAI_API_KEY}`。
- 保留 `${OPENAI_API_ENDPOINT}`、`${LLM_MODEL_NAME}`、`${HIDE_CHARTDB_CLOUD}` 和 `${DISABLE_ANALYTICS}` 运行时配置替换。
- 在 `src/lib/security/__tests__/browser-key-exposure.test.ts` 增加 `entrypoint.sh` 断言，防止 OpenAI API key 重新进入运行时配置白名单。

红灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/browser-key-exposure.test.ts
```

结果：失败，错误为 `expected '#!/bin/sh...' not to contain 'OPENAI_API_KEY'`。

绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/browser-key-exposure.test.ts
rg -n "OPENAI_API_KEY" entrypoint.sh
```

结果：

- 目标安全测试通过，`1` 个测试通过。
- `rg -n "OPENAI_API_KEY" entrypoint.sh` 无命中，命令按 ripgrep 语义返回 `1`。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：本轮执行并通过；安全扫描仍可能命中文档化的非 entrypoint 风险面时，以命中上下文判定是否属于后续任务范围。

实际结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`139` 个测试文件、`922` 个测试通过。
- `npm run build` 通过，仍有既有 Vite large chunk warning，继续归入后续性能优化任务。
- `git diff --check` 通过。
- `npm audit --omit=dev --audit-level=high` 通过；仍报告 5 个 low 和 1 个 moderate，属于 AI SDK / Monaco 后续治理范围。
- 安全扫描仅命中 `default.conf.template` 的既有 `window.env` 运行时配置入口；`entrypoint.sh` 不再命中 `OPENAI_API_KEY`。

下一项建议：

- 继续批次 F 的独立安全切片 `CHARTDB-F-004`：为所有 `window.open` 调用添加 `noopener noreferrer` 防护。

### CHARTDB-F-005：ClickHouse onboarding 入口修正

状态：已完成本地验证。

变更摘要：

- 采用 `docs/ChartDB全方位评估与后续优化手册.md` 推荐方案 A，从 onboarding 数据库选项中移除 ClickHouse。
- 保留 PostgreSQL、MySQL、SQLite、SQL Server 和 MariaDB 作为导入/新建入口，避免用户从首次进入流程进入当前 DDL import 不支持的 ClickHouse 路径。
- 新增 onboarding 契约测试，确认 ClickHouse 不再作为 DDL import 数据库选项展示。
- 同步全方位优化手册中 `CHARTDB-F-005` 的状态、completion 和任务状态总览；本轮未修改 i18n 文案。

红灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后测试失败，错误为 `expected document not to contain element`，命中 ClickHouse radio 仍存在。

绿灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

UI smoke：

- 桌面 `1280x720`：onboarding 标题可见，PostgreSQL/MySQL 可见，ClickHouse 不可见，无横向溢出，无 console error。
- 移动 `390x844`：onboarding 标题可见，PostgreSQL/MySQL 可见，ClickHouse 不可见，无横向溢出，无 console error。

下一项建议：

- 继续批次 F 的 `CHARTDB-F-006`：为 Vite 构建添加 manualChunks 分离大型依赖。

### CHARTDB-F-006：Vite manualChunks 分离大型依赖

状态：已完成本地验证。

变更摘要：

- 在 `vite.config.ts` 的 `build.rollupOptions.output` 中新增 `manualChunks` 函数。
- 按依赖 scope 分离 `vendor-react`、`vendor-monaco`、`vendor-sql-parser`、`vendor-dbml`、`vendor-ui`、`vendor-xyflow` 和 `vendor-i18n`。
- 保留既有 `external` 规则和 `assetFileNames` 输出规则。
- 同步全方位优化手册中 `CHARTDB-F-006` 的状态、completion 和任务状态总览。

红灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const source = fs.readFileSync('vite.config.ts', 'utf8');
const required = [
  'manualChunks',
  'vendor-react',
  'vendor-monaco',
  'vendor-sql-parser',
  'vendor-dbml',
  'vendor-ui',
  'vendor-xyflow',
  'vendor-i18n',
];
const missing = required.filter((token) => !source.includes(token));
if (missing.length) {
  console.error(`missing vite chunk config: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
```

结果：失败，错误为缺少 `manualChunks` 和全部目标 vendor chunk 名称。

绿灯验证：

```bash
node - <<'NODE'
const fs = require('fs');
const source = fs.readFileSync('vite.config.ts', 'utf8');
const required = [
  'manualChunks',
  'vendor-react',
  'vendor-monaco',
  'vendor-sql-parser',
  'vendor-dbml',
  'vendor-ui',
  'vendor-xyflow',
  'vendor-i18n',
];
const missing = required.filter((token) => !source.includes(token));
if (missing.length) {
  console.error(`missing vite chunk config: ${missing.join(', ')}`);
  process.exit(1);
}
NODE
npm run build
```

结果：通过。

性能结果：

- 修改前 `npm run build`：`editor-page-CrZ5nJPw.js` 11,477.69 kB / gzip 1,808.26 kB；`code-editor-DITLnTBm.js` 3,788.30 kB / gzip 976.91 kB；`index-I_21rQlA.js` 2,609.63 kB / gzip 513.31 kB。
- 修改后 `npm run build`：`editor-page-CLgWdvNu.js` 405.53 kB / gzip 111.31 kB；`code-editor-C0kdgjzX.js` 2.03 kB / gzip 1.01 kB；`index-cr1tvE-1.js` 507.60 kB / gzip 115.63 kB。
- 新增 vendor chunk：`vendor-react`、`vendor-monaco`、`vendor-sql-parser`、`vendor-dbml`、`vendor-ui`、`vendor-xyflow`、`vendor-i18n`。
- Vite 仍提示 `vendor-dbml`、`vendor-monaco` 和 `vendor-sql-parser` 超过 500 kB；本轮目标是把大型 vendor 依赖从业务 chunk 分离，后续如需继续降低 vendor 体积，应进入 DBML/parser 和 Monaco 进一步懒加载任务。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

浏览器 smoke：

- `npm run dev -- --host 127.0.0.1 --port 5173` 启动本地页面。
- 桌面 1280x720：自动进入本地图表，标题和工具栏可见，无横向溢出，console error 为空。
- 移动 390x844：主界面可见，无横向溢出，console error 为空。

下一项建议：

- F 批次 P0 任务已完成；按全方位优化手册第 10 节补充，下一项优先插入 `CHARTDB-A-007`：新增全局 ErrorBoundary。

### CHARTDB-A-007：全局 ErrorBoundary

状态：已完成本地验证。

变更摘要：

- 新增 `src/components/error-boundary/error-boundary.tsx`，使用 React class 组件实现 `getDerivedStateFromError` 与 `componentDidCatch`。
- 在 `src/App.tsx` 的 `RouterProvider` 外层接入根级 ErrorBoundary，渲染期未捕获异常会展示恢复页而不是整页白屏。
- 恢复页提供刷新入口，并明确错误详情和 schema 数据不会发送到远端服务。
- 新增 `src/components/error-boundary/__tests__/error-boundary.test.tsx`，覆盖子组件 render 抛错时 fallback UI 正常渲染。
- 同步全方位优化手册中 `CHARTDB-A-007` 的状态、completion 和任务状态总览。

红灯验证：

```bash
npm run test:ci -- src/components/error-boundary/__tests__/error-boundary.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `Failed to resolve import "../error-boundary"`，确认测试命中新缺失模块。

绿灯验证：

```bash
npm run test:ci -- src/components/error-boundary/__tests__/error-boundary.test.tsx
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
rg -n "ErrorBoundary" src/main.tsx src/App.tsx
```

结果：本轮执行并通过。

下一项建议：

- 按全方位优化手册第 10 节补充，继续优先插入 `CHARTDB-P-013`：应用启动与打开图表流程补充错误处理。

### CHARTDB-P-013：应用启动与打开图表流程补充错误处理

状态：已完成本地验证。

变更摘要：

- `useDiagramLoader()` 对 `loadDiagram` / `listDiagrams` 增加 try/catch；存储读取失败时会关闭已显示的全屏 loader，并暴露 `loadError` 与 `retryLoadDiagram`。
- 编辑页启动失败时展示错误说明和重试按钮，避免 IndexedDB 被阻塞、隐私模式限制或本地数据损坏时永久停留在加载动画。
- `OpenDiagramDialog` 对 `listDiagrams({ includeTables: true })` 增加 try/catch；读取失败时在列表区域展示错误态和重试按钮，区别于正常空列表。
- 新增 `src/pages/editor-page/__tests__/use-diagram-loader.test.tsx` 和 `src/dialogs/open-diagram-dialog/__tests__/open-diagram-dialog.test.tsx`，覆盖两处存储异常路径。
- 任务卡 allowed_files 中 `src/hooks/use-diagram-loader.tsx` 为旧路径；真实代码位于 `src/pages/editor-page/use-diagram-loader.tsx`，本轮按真实入口最小修改，并同步记录到全方位优化手册。

红灯验证：

```bash
npm run test:ci -- src/pages/editor-page/__tests__/use-diagram-loader.test.tsx src/dialogs/open-diagram-dialog/__tests__/open-diagram-dialog.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为未捕获的 `IndexedDB blocked` reject；`hideLoader()` 未被调用，OpenDiagramDialog 也无法找到 `Could not load local diagrams` 错误态，确认测试命中当前缺口。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/__tests__/use-diagram-loader.test.tsx src/dialogs/open-diagram-dialog/__tests__/open-diagram-dialog.test.tsx
```

结果：通过，`2` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 按全方位优化手册状态表继续选择下一项无依赖高优先级任务；候选包括 `CHARTDB-A-010` 元数据导入校验补齐或 `CHARTDB-P-011` 路由 errorElement，具体以下轮基线和手册顺序为准。

### CHARTDB-A-001：schema-core model 真正独立

状态：已完成本地验证。

变更摘要：

- 将 `area`、`table`、`field`、`index`、`relationship`、`diagram`、`database`、`customType`、`dependency`、`note`、`schema` 等纯模型定义迁移到 `src/schema-core/model/`。
- 将旧 `src/lib/domain/` 文件改为兼容 re-export，保持现有业务 import 路径可用，本轮不做大范围调用点迁移。
- `databaseEditionToImageMap` 继续留在 `src/lib/domain/database-edition.ts`，避免 schema-core 引入图片资源依赖；`DatabaseEdition`、label 和 edition map 已迁入 schema-core。
- `db-table` 在 schema-core model 内使用纯本地 helper，避免继续通过 `lib/utils` 间接依赖浏览器工具层。
- 更新 `src/schema-core/model/__tests__/model-exports.test.ts`，明确断言 schema-core model 文件不再从 `@/lib/domain` 重导出。
- 同步全方位优化手册中 `CHARTDB-A-001` 状态、completion 和任务状态总览，并修正 `CHARTDB-A-007` 任务卡残留 queued 状态。

红灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `src/schema-core/model/area.ts should own model definitions instead of re-exporting lib/domain`，确认测试命中 schema-core model 空壳重导出缺口。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/model/__tests__/model-exports.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
rg -n "from 'react'|from 'dexie'|from 'monaco|window\.|localStorage" src/schema-core/model/
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`142` 个测试文件、`928` 个测试通过。
- `npm run build` 通过，仍有既有的模板 manifest 动静态导入提示与大 chunk 警告，归入后续性能治理。
- `git diff --check` 通过。
- source-only 纯净性检查通过：`rg -n "from 'react'|from 'dexie'|from 'monaco|window\.|localStorage" src/schema-core/model/ --glob '!**/__tests__/**'` 无命中。

下一项建议：

- 进入 `CHARTDB-A-002`：统一 undo/redo 到 Command 驱动。

### CHARTDB-A-002：统一 undo/redo 到 Command 驱动

状态：已完成本地验证。

变更摘要：

- `HistoryProvider` 现在会优先读取 `RedoUndoAction.commandHistory`，通过 replay `redoCommand` / `undoCommand` 执行已迁移的 table、field、index、relationship、area、note 和 custom type command。
- undo replay 会反向执行 batch entries，避免批量命令回滚时破坏依赖顺序。
- 缺少 commandHistory 或遇到尚未支持 replay 的 command 时，继续回退旧 `redoData` / `undoData` handler，保持 checkConstraint 等未迁移路径兼容。
- 新增 `src/context/history-context/__tests__/history-provider.test.tsx`，验证 undo/redo 优先使用 commandHistory，并保留 legacy fallback。
- 同步全方位优化手册中 `CHARTDB-A-002` completion 和任务状态总览。

红灯验证：

```bash
npm test -- src/context/history-context/__tests__/history-provider.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，undo 调用 `removeTables(['legacy-undo'])`，redo 调用 `addTables([legacy-redo])`，确认测试命中 `history-provider` 未使用 commandHistory 的缺口。

绿灯验证：

```bash
npm test -- src/context/history-context/__tests__/history-provider.test.tsx
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-A-003`：拆分 ChartDBProvider。

### CHARTDB-A-003：ChartDBProvider 拆分（第一段结构切片）

状态：已完成本地验证，任务卡继续保持 `in_progress`。

变更摘要：

- `src/context/chartdb-context/chartdb-provider.tsx` 从 2864 行收敛为 21 行薄包装组件，只负责调用 `useChartDBProviderValue()` 并挂载 `chartDBContext.Provider`。
- 原 Provider 状态、命令和 context value 组装逻辑迁入 `src/context/chartdb-context/use-chartdb-provider-value.tsx`，保持 `ChartDBProvider` props 和消费方 `useChartDB()` API 不变。
- 新增 `src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts`，锁定 provider 主体不超过 800 行，并要求实现迁入 hook 文件。
- 本轮是 A-003 的最小结构切片，尚未继续把 table、field、relationship、area、note、custom type、check constraint 和 dependency 操作拆成各自领域 hook。

红灯验证：

```bash
npm test -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected 2864 to be less than or equal to 800`，确认测试命中 `chartdb-provider.tsx` 过大的结构问题。

绿灯验证：

```bash
npm test -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`144` 个测试文件、`932` 个测试通过。
- `npm run build` 通过，仍有既有 Browserslist 过期提示、template manifest 动静态导入提示和大 chunk 警告。
- `git diff --check` 通过。

下一项建议：

- 继续 `CHARTDB-A-003`，把 `use-chartdb-provider-value.tsx` 中的 Table / Field / Relationship / Area / Note / CustomType / CheckConstraint 操作继续拆成独立领域 hook。

### CHARTDB-A-003：ChartDBProvider 拆分（第二段 Dependency hook）

状态：已完成本地验证，任务卡继续保持 `in_progress`。

变更摘要：

- 新增 `src/context/chartdb-context/use-dependency-operations.ts`，把 `addDependency`、`addDependencies`、`createDependency`、`getDependency`、`removeDependency`、`removeDependencies`、`updateDependency` 从 `use-chartdb-provider-value.tsx` 迁出。
- `useChartDBProviderValue()` 继续保留 `useChartDB()` 对外 API 不变，只负责组装 dependency hook 返回的 context 方法。
- 扩展 `chartdb-provider-structure.test.ts`，要求 dependency 操作存在独立领域 hook，避免后续回退到 Provider value 内联实现。

红灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected false to be true`，确认测试命中缺少 `use-dependency-operations.ts` 的结构问题。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。

下一项建议：

- 继续 `CHARTDB-A-003`，优先把 Area / Note / CustomType 等 visual domain 操作迁入独立 hook，再处理 Table / Field / Relationship / CheckConstraint。

### CHARTDB-A-003：ChartDBProvider 拆分（第三段 Visual hook）

状态：已完成本地验证，任务卡继续保持 `in_progress`。

变更摘要：

- 新增 `src/context/chartdb-context/use-visual-operations.ts`，把 Area、Note、CustomType 及 custom type 高亮相关操作从 `use-chartdb-provider-value.tsx` 迁出。
- `useChartDBProviderValue()` 继续保留 `useChartDB()` 对外 API 不变，只负责把 visual hook 返回的方法组装进 context value。
- 扩展 `chartdb-provider-structure.test.ts`，要求 visual/custom type 操作存在独立领域 hook，避免后续回退到 Provider value 内联实现。

红灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected false to be true`，确认测试命中缺少 `use-visual-operations.ts` 的结构问题。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 继续 `CHARTDB-A-003`，把 Table / Field / Relationship / CheckConstraint 操作继续拆成独立领域 hook。

### CHARTDB-A-003：ChartDBProvider 拆分（第四段 Table / Field hook）

状态：已完成本地验证，任务卡继续保持 `in_progress`。

变更摘要：

- 新增 `src/context/chartdb-context/use-table-field-operations.ts`，把 Table / Field 相关操作从 `use-chartdb-provider-value.tsx` 迁出。
- 迁出范围包括 `addTable` / `addTables` / `createTable` / `getTable` / `removeTable` / `removeTables` / `updateTable` / `updateTablesState`，以及 `addField` / `createField` / `getField` / `removeField` / `updateField`。
- `useChartDBProviderValue()` 继续保留 `useChartDB()` 对外 API 不变，只负责把 table/field hook 返回的方法组装进 context value。
- 扩展 `chartdb-provider-structure.test.ts`，要求 Table / Field 操作存在独立领域 hook，避免后续回退到 Provider value 内联实现。

红灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected false to be true`，确认测试命中缺少 `use-table-field-operations.ts` 的结构问题。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 继续 `CHARTDB-A-003`，把 Relationship / CheckConstraint 操作继续拆成独立领域 hook。

### CHARTDB-A-003：ChartDBProvider 拆分（第五段 Relationship / CheckConstraint hook）

状态：已完成本地验证，任务卡继续保持 `in_progress`。

变更摘要：

- 新增 `src/context/chartdb-context/use-relationship-constraint-operations.ts`，把 Relationship / CheckConstraint 相关操作从 `use-chartdb-provider-value.tsx` 迁出。
- 迁出范围包括 `addRelationship` / `addRelationships` / `createRelationship` / `getRelationship` / `removeRelationship` / `removeRelationships` / `updateRelationship`，以及 `addCheckConstraint` / `createCheckConstraint` / `removeCheckConstraint` / `updateCheckConstraint`。
- `useChartDBProviderValue()` 继续保留 `useChartDB()` 对外 API 不变，只负责把 relationship/check constraint hook 返回的方法组装进 context value。
- 扩展 `chartdb-provider-structure.test.ts`，要求 Relationship / CheckConstraint 操作存在独立领域 hook，避免后续回退到 Provider value 内联实现。

红灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected false to be true`，确认测试命中缺少 `use-relationship-constraint-operations.ts` 的结构问题。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`144` 个测试文件、`936` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `vendor-dbml` chunk 约 `11,163.38 kB`，继续归入 Phase 6 / 后续性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 继续 `CHARTDB-A-003`，把 Index 操作继续拆成独立领域 hook；完成后再评估是否关闭 Provider 拆分任务。

### CHARTDB-A-003：ChartDBProvider 拆分（第六段 Index hook）

状态：已完成本地验证，`CHARTDB-A-003` 关闭为 `done`。

变更摘要：

- 新增 `src/context/chartdb-context/use-index-operations.ts`，把 Index 相关操作从 `use-chartdb-provider-value.tsx` 迁出。
- 迁出范围包括 `addIndex` / `createIndex` / `getIndex` / `removeIndex` / `updateIndex`。
- `useChartDBProviderValue()` 继续保留 `useChartDB()` 对外 API 不变，只负责把 index hook 返回的方法组装进 context value。
- 扩展 `chartdb-provider-structure.test.ts`，要求 Index 操作存在独立领域 hook，避免后续回退到 Provider value 内联实现。
- 同步全方位优化手册中 `CHARTDB-A-003` 的状态、completion、任务状态总览和下一项。

红灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected false to be true`，确认测试命中缺少 `use-index-operations.ts` 的结构问题。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：通过，`1` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：本轮执行并通过。

下一项建议：

- 进入 `CHARTDB-A-004`：diff 合并和 loadDiagram 走 Command 管道。

### CHARTDB-A-004：diff 合并和 loadDiagram 走 Command 管道

状态：已完成本地验证，`CHARTDB-A-004` 关闭为 `done`。

变更摘要：

- 新增 `src/schema-core/commands/diagram-commands.ts` 与 `apply-diagram-command.ts`，提供 `diagram.replace` 和 `diagram.mergeDiff` 两个 diagram 级命令。
- `diagram.replace` 会在替换整张图前校验重复实体 ID，成功时返回上一张 diagram 作为 undo payload；校验失败时保持原 state。
- `diagram.mergeDiff` 会通过命令合并新增 table、field、relationship、area，并在最终 diagram 通过校验后一次性应用。
- `useChartDBProviderValue()` 中的 diff 合并和 `loadDiagramFromData()` 改为调用 schema-core diagram command，不再直接用多组 setter 绕过 command 管道。
- `HistoryProvider` 支持 replay `diagram.replace` / `diagram.mergeDiff`，diff 合并写入 commandHistory 后可通过 undo/redo 恢复或重放，并避免 replay 时重置 undo/redo 栈。
- 扩展 `chartdb-provider-structure.test.ts`，防止 Provider value 回退到直接 setter 路径。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-commands.test.ts
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- diagram command 测试失败于 `createReplaceDiagramCommand is not a function`，确认命中缺少 diagram command API。
- Provider 结构测试失败于 `expected ... to contain 'applyDiagramCommand'`，确认 diff/load 尚未接入 command 管道。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/diagram-commands.test.ts src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
npm run test:ci -- src/context/history-context/__tests__/history-provider.test.tsx
```

结果：

- diagram command 与 Provider 结构聚焦测试通过，`2` 个测试文件、`10` 个测试通过。
- HistoryProvider command replay 聚焦测试通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：

- `npm run lint` 通过。
- `npm run test:ci` 通过，`145` 个测试文件、`941` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `vendor-dbml` chunk 约 `11,163.38 kB`，继续归入 Phase 6 / 后续性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-A-005`：checkConstraint 补齐 Command。

### CHARTDB-A-005：checkConstraint 补齐 Command

状态：已完成本地验证，`CHARTDB-A-005` 关闭为 `done`。

变更摘要：

- 新增 `src/schema-core/commands/check-constraint-commands.ts` 和 `apply-check-constraint-command.ts`，提供 `check_constraint.add`、`check_constraint.update`、`check_constraint.delete` command。
- `addCheckConstraint`、`removeCheckConstraint`、`updateCheckConstraint` 改为先执行 schema-core command，再写入 React state、IndexedDB table 和 diagram updatedAt。
- check constraint 操作的 undo action 附加 `commandHistory`，并让 `HistoryProvider` 支持 check constraint command replay，undo/redo 优先走 command。
- 本轮按任务卡 allowed_files 最小越界到 `src/context/history-context/history-provider.tsx`，原因是验收项要求 undo/redo 可用；未改其它 history 行为。

红灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
```

结果：先因 fresh worktree 未安装依赖失败于 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `createAddCheckConstraintCommand is not a function`，确认缺少 command API。

绿灯验证：

```bash
npm run test:ci -- src/schema-core/commands/__tests__/field-index-relationship-commands.test.ts
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
npm run test:ci -- src/context/history-context/__tests__/history-provider.test.tsx
```

结果：

- command 聚焦测试通过，`7` 个测试通过。
- Provider 结构测试通过，`7` 个测试通过。
- HistoryProvider command replay 测试通过，`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

- `npm run lint` 通过。
- `npm run test:ci` 通过，`145` 个测试文件、`943` 个测试通过。
- `npm run build` 通过，仍有既有 Vite chunk 过大警告；最大 `vendor-dbml` chunk 约 `11,163.38 kB`，继续归入 Phase 6 / 后续性能优化范围。
- `git diff --check` 通过。

下一项建议：

- 进入 `CHARTDB-A-006`：dialects 层迁移 parser 逻辑，先迁移 PostgreSQL 作为模板。

### CHARTDB-A-006：dialects 层迁移 parser 逻辑

状态：已完成本地验证，`CHARTDB-A-006` 关闭为 `done`。

变更摘要：

- 将 PostgreSQL parser 核心实现迁入 `src/dialects/postgresql/parser/`，包括 `postgresql.ts`、`postgresql-dump.ts` 和 `postgresql-common.ts`。
- `src/lib/data/sql-import/dialect-importers/postgresql/` 下同名文件改为兼容 re-export，保证旧路径测试和调用点继续可用。
- `src/dialects/postgresql/parser/legacy-parser.ts` 改为直接从 dialect 包内 parser 导入 `fromPostgres`，不再依赖旧 importer 实现。
- 新增 `src/dialects/postgresql/__tests__/parser-migration.test.ts`，锁定 parser 实现位置和旧路径兼容层。

红灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/parser-migration.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后失败，错误为 `expected ... not to contain '@/lib/data/sql-import/dialect-importers/postgresql/postgresql'`，确认 PostgreSQL dialect parser 仍依赖旧 importer 实现。

绿灯验证：

```bash
npm run test:ci -- src/dialects/postgresql/__tests__/parser-migration.test.ts
npm run test:ci -- src/dialects/postgresql/__tests__/importer.test.ts src/lib/data/sql-import/dialect-importers/postgresql/__tests__/postgresql-core.test.ts src/lib/data/sql-import/dialect-importers/postgresql/__tests__/postgresql-parser.test.ts
```

结果：

- parser migration 结构测试通过，`1` 个测试通过。
- PostgreSQL dialect importer 与旧路径 parser regression 通过，`3` 个测试文件、`37` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-001`：实现 Clear local diagrams 功能；也可按批次 A 继续独立插入 `CHARTDB-A-010` 元数据导入校验补齐。

## 2026-07-04：CHARTDB-A-010 元数据导入热路径 Zod 校验验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `loadDatabaseMetadata()` 改为对 `JSON.parse` 结果调用 `DatabaseMetadataSchema.safeParse()`，不再直接做 TypeScript 类型断言。
- schema 校验失败时抛出 `Invalid database metadata JSON` 开头的清晰错误，并包含字段路径摘要，避免畸形 Smart Query JSON 继续流入 diagram 构建深层逻辑。
- `import-preview-core.ts` 和 `create-diagram-dialog.tsx` 的 query 热路径复用同一个解析入口，因此 preview、最终导入和选择表前的 metadata 缓存路径均受同一校验保护。
- 新增元数据 schema 与 import preview 聚焦测试，覆盖畸形 Smart Query JSON 被提前拦截。

红灯记录：

```bash
npm run test:ci -- src/lib/data/import-metadata/__tests__/database-metadata-validation.test.ts src/lib/data/import-metadata/__tests__/import-preview-metadata-validation.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后 `database-metadata-validation.test.ts` 失败于 `expected [Function] to throw an error`，确认 parsed JSON 缺少 schema 校验。

绿灯验证：

```bash
npm run test:ci -- src/lib/data/import-metadata/__tests__/database-metadata-validation.test.ts src/lib/data/import-metadata/__tests__/import-preview-metadata-validation.test.ts
```

结果：

- 聚焦测试通过，`2` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续批次 A 中剩余 queued 项，优先在 `CHARTDB-A-008`、`CHARTDB-A-009`、`CHARTDB-A-011` 中按依赖和可验证范围选择下一个最小切片。

## 2026-07-04：CHARTDB-A-008 Context selector 基础验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- 在 `src/context/chartdb-context/chartdb-context.tsx` 中新增稳定 ChartDB store、`chartDBStoreContext` 和 `useChartDBSelector()`，为消费方提供按 slice 订阅的入口。
- 在 `ChartDBProvider` 中保留原 `chartDBContext.Provider`，继续兼容旧 `useChartDB()` API；同时提供稳定 store provider，并在 layout effect 中同步最新 context value。
- 新增 selector 渲染计数回归测试：只订阅 `diagramName` 的 memo 消费方在 `tables` 变化时不重渲染，证明后续侧栏/节点组件可逐步从整包 `useChartDB()` 迁移到 selector。
- 本轮不批量迁移 62 处既有消费方，避免跨页面大范围重构；旧消费方行为保持不变。

红灯记录：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-context-selector.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 补齐测试 Provider 后重跑失败，错误为 `useChartDBSelector is not a function`，确认缺少 selector 订阅入口。

绿灯验证：

```bash
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-context-selector.test.tsx
npm run test:ci -- src/context/chartdb-context/__tests__/chartdb-context-selector.test.tsx src/context/chartdb-context/__tests__/chartdb-provider-structure.test.ts
```

结果：

- selector 聚焦测试通过，`1` 个测试文件、`1` 个测试通过。
- context 相关聚焦测试通过，`2` 个测试文件、`8` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续批次 A 中剩余 queued 项，优先在 `CHARTDB-A-009`、`CHARTDB-A-011` 中按依赖和可验证范围选择下一个最小切片。

## 2026-07-04：CHARTDB-A-011 Worker 超时保护验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `runWorkerTask()` 新增默认 15 秒 timeout，并允许调用方通过 `timeoutMs` 覆盖。
- worker 长时间不响应时会清理 message/error/abort 监听、终止 worker，并走主线程 fallback，避免调用方永久 pending。
- fallback 新增可选 `WorkerTaskFallbackContext`，上层可区分 `worker-unavailable`、`worker-error` 和 `worker-timeout`，并在需要时标记 `timeout-fallback` 来源。
- 新增 stalled worker 聚焦测试，覆盖 timeout 后 fallback resolve 与 worker terminate。

范围说明：

- 任务卡 allowed_files 未列出文档，但 dispatcher done 定义要求同步全方位优化手册和阶段验收记录；本轮文档越界仅更新任务状态与验收结果。

红灯记录：

```bash
npm run test:ci -- src/workers/__tests__/
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增 stalled worker 测试在 5 秒测试超时处失败，确认旧实现会永久 pending。

绿灯验证：

```bash
npm run test:ci -- src/workers/__tests__/
```

结果：

- 聚焦测试通过，`2` 个测试文件、`5` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-001`：实现 Clear local diagrams 功能。

## 2026-07-04：CHARTDB-P-001 Clear local diagrams 验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `StorageContext` / `StorageProvider` 新增 `clearAllDiagrams()`，调用 repository 批量清除本地图表，并清空 `defaultDiagramId`。
- `DiagramTransactionService` 新增 `clearAllDiagramsWithChildren()`，在 transaction 内删除全部 diagram 及其 tables、relationships、dependencies、areas、customTypes、notes 和 filters。
- 设置中心 `Clear local diagrams` 改为 destructive 二次确认；确认后执行实际删除，并展示处理中、成功和失败反馈。
- 新增设置页交互测试与 repository 级联删除测试，覆盖确认、删除 API 调用、成功反馈和父/子记录清除。

红灯记录：

```bash
npm run test:ci -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于找不到 `Delete all local diagrams?` alertdialog，确认当前按钮只显示占位提示，未执行删除。

绿灯验证：

```bash
npm run test:ci -- src/features/settings/__tests__/settings-dialog.test.tsx src/storage/repositories/__tests__/chartdb-repositories.test.ts
```

结果：

- 聚焦测试通过，`2` 个测试文件、`10` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-002`：BYOK key 输入入口。

## 2026-07-04：CHARTDB-P-002 BYOK key 输入入口验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- 设置中心的 BYOK Session 模式新增 `Session API key` password 输入框，用户可在设置中心输入临时 key。
- 输入框通过 `setBYOKSessionKey()` 写入 module memory；不会写入 localStorage、sessionStorage、IndexedDB、URL 或导出文件。
- `setBYOKSessionKey()` 对空白输入执行清除，设置页清空输入会同步清空内存 key。
- 新增 `src/features/settings/__tests__/privacy-settings.test.tsx`，覆盖 key 输入、清除和不持久化；保留 `ai-mode` 测试覆盖 BYOK 请求构造。

红灯记录：

```bash
npm run test:ci -- src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于找不到 `Session API key` label，确认设置中心 BYOK 模式没有 key 输入入口。

绿灯验证：

```bash
npm run test:ci -- src/features/settings/__tests__/settings-dialog.test.tsx src/features/settings/__tests__/privacy-settings.test.tsx src/lib/ai/__tests__/ai-mode.test.ts
```

结果：

- 聚焦测试通过，`3` 个测试文件、`10` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-003`：不支持的方言导出时输出明确 warning 而非静默降级。

## 2026-07-04：CHARTDB-P-003 Oracle/ClickHouse 导出 warning 验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `exportBaseSQL()` 对 Oracle、ClickHouse、CockroachDB deterministic export 输出 PostgreSQL fallback SQL 前新增 SQL 注释 warning。
- warning 注释包含 `export.unsupported_dialect_fallback` code、warning severity 和 medium riskLevel，明确说明当前使用 PostgreSQL fallback 格式。
- 保持现有 string API 和导出对话框调用路径不变，避免扩大到通用 dialect contract 或 UI 改造。
- 新增 `export-sql.test.ts` 聚焦测试覆盖 Oracle、ClickHouse、CockroachDB 三种 unsupported target。

范围说明：

- 任务卡 allowed_files 未列出文档，但 dispatcher done 定义要求同步本手册和阶段验收记录；本轮文档越界仅更新任务状态与验收结果。

红灯记录：

```bash
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于 Oracle 导出 SQL 不包含 `WARNING: Oracle SQL export is not fully supported`，确认当前是静默 PostgreSQL fallback。

绿灯验证：

```bash
npm run test:ci -- src/lib/data/sql-export/__tests__/export-sql.test.ts
```

结果：

- 聚焦测试通过，`1` 个测试文件、`20` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-004`：为 ImportResult 补充 confidence 和 diagnostics 字段。

## 2026-07-04：CHARTDB-P-004 ImportResult confidence 验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `ImportResult` 新增 `confidence: 'high' | 'medium' | 'low'` 与 `diagnostics` 字段。
- `createImportResult()` 默认输出 `confidence: 'medium'` 和空 diagnostics，旧 wrapper 不需要立即改造也能得到稳定 contract。
- `buildImportPreview()` 透传 confidence 和 diagnostics；`ImportPreviewPanel` 在 preview 摘要中展示可信度等级，并在提示区展示 diagnostics message。
- `docs/导入导出接口约定.md` 同步记录新的 ImportResult 和 Import Preview 要求。

范围说明：

- 任务卡 allowed_files 未列出 `src/features/import/__tests__/**` 与文档，但 dispatcher TDD 和 done 定义要求同步聚焦测试、全方位优化手册、阶段验收记录与接口约定；本轮越界仅补测试 fixture 和对应中文文档。

红灯记录：

```bash
npm run test:ci -- src/dialects/__tests__/import-confidence.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于 `expected undefined to be 'medium'`，确认 `createImportResult()` 缺少 confidence 默认值。

绿灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/import-confidence.test.tsx src/features/import/__tests__/import-preview.test.ts src/features/import/__tests__/import-preview-worker-routing.test.ts
```

结果：

- 聚焦测试通过，`3` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-005`：Backup 恢复预览。

## 2026-07-04：CHARTDB-P-005 Backup 恢复预览验收

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

验收结论：通过。

变更范围：

- `src/storage/backup/backup-format.ts` 新增 `parseBackupSummary()`，恢复前可解析 backup metadata、diagram count、diagram 名称、table count 和 relationship count。
- 设置中心 `Restore from backup` 改为选择 JSON backup 后先展示摘要预览；用户点击 `Restore backup` 前不写入本地 storage。
- 确认恢复后按 backup payload 逐个创建新的本地图表，并跳转到第一个恢复出的 diagram。
- 不兼容或损坏 backup 在预览/恢复路径中展示可读错误，不进入写入流程。

红灯记录：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于 `parseBackupSummary is not a function`，确认缺少恢复摘要解析入口。

绿灯验证：

```bash
npm run test:ci -- src/storage/backup/__tests__/backup-restore.test.ts src/features/settings/__tests__/settings-dialog.test.tsx
```

结果：

- 聚焦测试通过，`2` 个测试文件、`10` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-P-006`：CockroachDB 和 ClickHouse dialect wrapper。

### CHARTDB-P-006：CockroachDB 和 ClickHouse dialect wrapper

状态：已完成本地验证。

变更摘要：

- 新增 `src/dialects/cockroachdb/`，复用 PostgreSQL parser fallback，声明 CockroachDB import/export capability，并对 locality、changefeed、interleave 和 zone config 输出结构化 warning / unsupportedObjects。
- 新增 `src/dialects/clickhouse/`，明确 ClickHouse DDL import 当前 unsupported / Smart Query only，返回空 diagram、low confidence、diagnostics 和 unsupportedObjects，不伪装为 PostgreSQL/MySQL 解析。
- 扩展 `src/dialects/__tests__/sql-dialect-importers.test.ts`，覆盖 CockroachDB fallback wrapper 和 ClickHouse unsupported wrapper。

红灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：失败，错误为 `Failed to resolve import "../cockroachdb"`，确认缺少 wrapper。

绿灯验证：

```bash
npm run test:ci -- src/dialects/__tests__/sql-dialect-importers.test.ts
```

结果：通过，`1` 个测试文件、`7` 个测试通过。

下一项建议：

- 进入 `CHARTDB-S-001`：CSP connect-src 收紧。

### CHARTDB-S-001：CSP connect-src 收紧

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `default.conf.template` 的静态页面与 `/config.js` CSP 均从 `connect-src 'self' http: https:` 收紧为 `connect-src 'self' https:`，不再默认允许任意明文 HTTP 连接。
- `src/lib/security/__tests__/nginx-security-headers.test.ts` 增加回归断言，要求 HTTPS self-hosted gateway 兼容，并禁止回退到 `connect-src 'self' http: https:`。
- `docs/部署与安全配置.md` 更新自托管边界：默认生产 gateway 应使用 HTTPS；明文 HTTP、本地开发端口、WebSocket 或专用域名需要自托管者显式审查并调整 CSP。

范围说明：

- 任务卡 allowed_files 未列出本手册和阶段验收记录，但 dispatcher done 定义要求同步 `docs/ChartDB全方位评估与后续优化手册.md` 和 `docs/阶段验收记录.md`；本轮文档越界仅更新任务状态、验收结果和下一步。

红灯记录：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于 `expected ... to contain "connect-src 'self' https:"`，确认当前 Nginx CSP 仍允许 `http:`.

绿灯验证：

```bash
npm run test:ci -- src/lib/security/__tests__/nginx-security-headers.test.ts
```

结果：

- 聚焦测试通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-S-002`：export-image-provider innerHTML 修复。

### CHARTDB-S-002：export-image-provider innerHTML 修复

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `src/context/export-image-context/export-image-provider.tsx` 不再使用 `defs.innerHTML = markerDefs.innerHTML` 复制 SVG marker 定义。
- 临时导出 SVG 的 defs 改为遍历 `markerDefs.childNodes` 并使用 `cloneNode(true)` 复制节点，保留 marker 结构且避免不受控 DOM 字符串赋值。
- 新增 `src/context/export-image-context/__tests__/export-image-provider.test.tsx`，mock SVG 导出流程并禁止 `innerHTML` setter，验证导出时 marker 已被复制到临时 defs。

红灯记录：

```bash
npm run test:ci -- src/context/export-image-context/__tests__/export-image-provider.test.tsx
```

结果：

- 独立 worktree 初次运行先因未安装依赖失败，错误为 `vitest: command not found`；执行 `npm install` 后重跑。
- 重跑后新增测试失败于 `innerHTML assignment is not allowed`，确认当前导出路径仍使用 `innerHTML` 赋值。

绿灯验证：

```bash
npm run test:ci -- src/context/export-image-context/__tests__/export-image-provider.test.tsx
```

结果：

- 聚焦测试通过，`1` 个测试文件、`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
npm audit --omit=dev --audit-level=high
rg -n "VITE_OPENAI_API_KEY|OPENAI_API_KEY|window\\.env|rehype-raw|dangerouslySetInnerHTML" src Dockerfile default.conf.template .github
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-S-003`：Monaco dompurify 升级评估，或按手册批次继续选择下一个可本地验证的 queued 项。

### CHARTDB-S-003：Monaco dompurify 升级评估

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `docs/安全风险登记.md` 已记录当前 Monaco / dompurify 事实：`monaco-editor@0.55.1` 仍声明 `dompurify@3.2.7`，当前 latest stable 未提供可直接升级的修复版本。
- `npm audit --omit=dev --json` 的自动修复建议是调整到 `monaco-editor@0.53.0`；该路径属于编辑器主版本回退，需要单独验证 Monaco worker、`@monaco-editor/react` runtime 和 SQL/DBML readOnly 高亮，不作为本轮 spike 直接执行。
- CodeMirror 6 已记录为长期替代方案，但需要重写 `CodeSnippet`、DBML language setup、completion provider、theme、worker 和可访问性契约。
- 本轮结论：暂不变更依赖；保留当前 Monaco，继续依赖 Markdown raw HTML 禁用、CSP、ErrorBoundary 和 Monaco lazy 边界降低实际可利用面。

范围说明：

- 任务卡 allowed_files 只列出 `docs/安全风险登记.md`；dispatcher done 定义要求同步手册和阶段验收记录，因此本轮文档越界仅更新任务状态、验收结果和下一步。

红灯记录：

```bash
rg -n "Monaco Editor 升级路径|monaco-editor@0\\.55\\.1|dompurify@3\\.2\\.7|CodeMirror 6|CHARTDB-S-003" docs/安全风险登记.md
```

结果：无命中，确认安全风险登记尚未记录本任务要求的升级/替代方案结论。

绿灯验证：

```bash
rg -n "Monaco|dompurify|CodeMirror|CHARTDB-S-003" docs/安全风险登记.md
npm audit --omit=dev --json
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-001`：i18n locale 文件改为按需动态加载。

### CHARTDB-T-001：i18n locale 懒加载

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `src/i18n/i18n.ts` 保留英文 fallback 同步资源，其余 locale 改由自定义 i18next backend 通过动态 import 按需加载。
- `languages` 元数据改为同步轻量列表，不再为了渲染语言选择器静态 import 21 个非英文翻译文件。
- 新增 `src/i18n/__tests__/i18n-lazy-loading.test.ts`，覆盖非 fallback locale 不再静态 import，以及 `i18n.changeLanguage('fr')` 时才加载法语 resource bundle。

红灯记录：

```bash
npm run test:ci -- src/i18n/__tests__/i18n-lazy-loading.test.ts
```

结果：失败，错误为 `expected ... not to contain "from './locales/fr'"`，确认当前 `i18n.ts` 仍静态加载非英文 locale。

绿灯验证：

```bash
npm run test:ci -- src/i18n/__tests__/i18n-lazy-loading.test.ts src/i18n/__tests__/language-metadata.test.ts
```

结果：通过，`2` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-002`：模板数据懒加载完善。

### CHARTDB-T-002：模板数据懒加载完善

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 强化 `src/templates-data/__tests__/template-lazy-registry.test.ts`，确认 50 个 `src/templates-data/templates/*.ts` 文件均通过 `template-manifest.ts` 的 per-template dynamic import 注册。
- `src/templates-data/templates-data.ts` 删除未使用的 `loadTemplates()` 全量加载出口和 `templateManifests` 运行时 import，仅保留 `Template` 类型与 `loadTemplateBySlug` re-export。
- `docs/ChartDB全方位评估与后续优化手册.md` 将 `CHARTDB-T-002` 标记为 done，并同步旧 Phase 6 文档中关于 `loadTemplates()` 的过期描述。

红灯记录：

```bash
npm run test:ci -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `templates-data.ts` 仍包含 `loadTemplates`，确认仍存在全量加载所有模板 diagram 的兼容出口。

绿灯验证：

```bash
npm run test:ci -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run test:ci -- src/templates-data/__tests__/
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-003`：模板图片 PNG 转 WebP。

### CHARTDB-T-003：模板图片 PNG 转 WebP

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 使用 `cwebp -q 82` 为 `src/assets/templates/` 下 100 张模板亮色/暗色 PNG 生成同名 WebP，并保留原 PNG 作为备份。
- `src/templates-data/template-manifest.ts` 改为引用 WebP 缩略图，模板列表和卡片首屏不再加载 PNG 缩略图。
- `src/templates-data/templates/*.ts` 的动态模板模块同步改为 WebP 引用，避免详情页和 clone loader 的 template chunk 继续携带 PNG。
- 强化 `src/templates-data/__tests__/template-lazy-registry.test.ts`，锁定 manifest 和完整模板模块都只引用存在的 WebP 模板缩略图。
- 模板 PNG 总体积 39,573,744 bytes；WebP 总体积 12,004,866 bytes；缩减 69.7%，超过任务卡 40% 要求。

范围说明：

- 任务卡 allowed_files 未列出 `src/templates-data/templates/*.ts`，但这些动态模板模块也 import PNG 作为模板 `image` / `imageDark`；本轮最小越界仅把图片 import 后缀改为 `.webp`，避免详情页和 clone loader chunk 继续携带 PNG。

红灯记录：

```bash
npm run test:ci -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于 `template-manifest.ts` 仍引用 100 个 PNG。

绿灯验证：

```bash
npm run test:ci -- src/templates-data/__tests__/template-lazy-registry.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run test:ci -- src/templates-data/__tests__/
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-004`：统一图标库。

### CHARTDB-T-004：统一图标库

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/components/__tests__/icon-library-contract.test.ts`，锁定 `package.json` 和源码 import 不再依赖 Radix icon 包。
- 将基础 UI 组件、菜单、分页、选择器、侧边栏等剩余 Radix icon import 替换为 `lucide-react` 对应图标或语义接近图标。
- 通过 `npm uninstall @radix-ui/react-icons` 从 `package.json` 和 `package-lock.json` 移除冗余图标依赖。

红灯记录：

```bash
npm run test:ci -- src/components/__tests__/icon-library-contract.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，测试失败于 `package.json` 仍包含 `@radix-ui/react-icons`。

绿灯验证：

```bash
npm run test:ci -- src/components/__tests__/icon-library-contract.test.ts
```

结果：通过，`1` 个测试文件、`1` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
rg -n "@radix-ui/react-icons" src
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-005`：精简 hooks 库。

### CHARTDB-T-005：精简 hooks 库

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/hooks/use-click-away.ts`、`src/hooks/use-key-press.ts`、`src/hooks/use-key-press-event.ts`，用本地最小 hooks 替代 `react-use` 的 click-away 和 key press event 能力。
- 将 Area、Note、Relationship、CustomType、diagram name、canvas popover 等编辑态调用点切换到本地 hooks。
- 从 `package.json` / `package-lock.json` 移除 `react-use` 及其传递依赖；`@uidotdev/usehooks` 继续保持无依赖、无引用。
- 保留 `ahooks`，原因是当前仍使用 `useEventEmitter` 和 `useDebounceFn`，其语义比本轮要内联的 DOM 事件 hooks 更复杂。
- 新增 `src/hooks/__tests__/local-hooks-contract.test.tsx`，锁定源码和 package 不再依赖目标 hooks 包，并覆盖 click-away、keydown/keyup 行为。

红灯记录：

```bash
npm run test:ci -- src/hooks/__tests__/local-hooks-contract.test.tsx
```

结果：新增测试先失败于缺少 `src/hooks/use-click-away`，确认本地 hooks 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/hooks/__tests__/local-hooks-contract.test.tsx
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
bash -lc '! rg -n "react-use|@uidotdev/usehooks" src'
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-006`：Monaco config 惰性初始化。

### CHARTDB-T-006：Monaco config 惰性初始化

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `src/components/code-snippet/config.ts` 改为导出缓存式 `ensureMonaco()`，只在首次实际渲染编辑器时动态 import `monaco-editor` 和 editor/json/css/html/typescript worker。
- `src/components/code-snippet/code-snippet-editor.tsx` 移除旧的配置副作用 import，在渲染 Monaco Editor 前等待 `ensureMonaco()` 完成，主题设置和 autoScroll 行为保持不变。
- `src/components/code-snippet/code-editor.ts` 不再加载配置模块，避免懒加载编辑器模块时立即触发 Monaco runtime setup。
- 扩展 `src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts`，锁定 `config.ts` 无静态 Monaco runtime / worker import，且编辑器组件通过 `ensureMonaco()` 初始化。

红灯记录：

```bash
npm run test:ci -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于 `config.ts` 仍缺少 `ensureMonaco`，且静态 import `monaco-editor` 与 worker。

绿灯验证：

```bash
npm run test:ci -- src/components/code-snippet/__tests__/monaco-lazy-loading.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-007`：canvas.tsx 拆分；若希望继续更小切片，可按手册并行策略选择 `CHARTDB-P-010`。

### CHARTDB-P-010：Canvas 键盘可访问性基线

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-keyboard-actions.ts`，将画布最小键盘动作抽为可测试纯函数。
- `src/pages/editor-page/canvas/canvas.tsx` 为画布容器补充可聚焦语义和可访问名称，并接入 Delete/Backspace 删除、方向键按 20px 微调选中 table/area/note 的键盘路径。
- 输入框、按钮、textarea、textbox 等交互控件内的按键不会被画布快捷键拦截；readonly 画布不执行节点变更。
- `docs/可访问性与核心流程验收.md` 明确画布节点/连线完整键盘等价操作仍为已知限制，本轮只交付选中节点后的最小可行键盘路径。
- 同步全方位优化手册中 `CHARTDB-P-010` 状态、completion 和任务状态总览；并修正 `CHARTDB-A-007` 任务卡、`CHARTDB-S-003` 状态总览中的滞后状态。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-keyboard-actions.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../canvas-keyboard-actions`，确认本轮键盘动作 helper 尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-keyboard-actions.test.ts
```

结果：通过，`1` 个测试文件、`3` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

UI smoke：

```bash
npm run dev -- --host 127.0.0.1
```

结果：见本轮 run log，覆盖桌面与移动视口。

下一项建议：

- 进入 `CHARTDB-P-011`：路由增加 errorElement；若优先技术债，可继续 `CHARTDB-T-007`：canvas.tsx 拆分。

### CHARTDB-P-011：路由 errorElement

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/components/route-error-boundary/route-error-boundary.tsx`，在 route loader 抛错或动态路由 chunk 加载失败时展示 ChartDB 风格恢复页。
- `src/router.tsx` 导出 `routes` 并为全部应用路由配置统一 `errorElement`，避免 React Router 默认错误页脱离产品视觉。
- 新增 `src/components/route-error-boundary/__tests__/route-error-boundary.test.tsx`，覆盖全部 route 配置和 loader 抛错后的自定义错误页、“返回首页”“重试”入口。
- 同步全方位优化手册中 `CHARTDB-P-011` 状态、completion 和任务状态总览。

红灯记录：

```bash
npm run test:ci -- src/components/route-error-boundary/__tests__/route-error-boundary.test.tsx
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../route-error-boundary`，确认路由错误边界尚未实现。

绿灯验证：

```bash
npm run test:ci -- src/components/route-error-boundary/__tests__/route-error-boundary.test.tsx
```

结果：通过，`1` 个测试文件、`2` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

UI smoke：

```bash
npm run dev -- --host 127.0.0.1
```

结果：见本轮 run log，覆盖桌面与移动视口的路由页面无横向溢出、无严重 console error；loader 抛错错误页由聚焦测试覆盖。

下一项建议：

- 进入 `CHARTDB-T-007`：canvas.tsx 拆分；若继续产品小切片，可选择 `CHARTDB-P-012`：Onboarding 数据库选择延续。

### CHARTDB-P-012：Onboarding 数据库选择延续

状态：已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- `CreateDiagramDialog` 新增 `initialDatabaseType` 与 `initialStep` 参数，并在对话框打开时按参数初始化数据库类型和步骤。
- `OnboardingDialog` 在用户选择数据库后进入“Import existing database”路径时，将已选数据库类型传入 create dialog，并直接进入导入步骤。
- 未经 onboarding 的普通新建入口不传初始参数，仍保持 `SELECT_DATABASE` / `DatabaseType.GENERIC` 默认行为。
- 扩展 onboarding 聚焦测试，覆盖 MySQL 等数据库选择在导入路径中的参数传递。

红灯记录：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增断言失败于 `openCreateDiagramDialog()` 未收到 `initialDatabaseType` / `initialStep` 参数。

绿灯验证：

```bash
npm run test:ci -- src/features/onboarding/__tests__/onboarding-dialog.test.tsx
```

结果：通过，`1` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 进入 `CHARTDB-T-007`：canvas.tsx 拆分；若继续独立小切片，可选择 `CHARTDB-P-014`、`CHARTDB-S-004` 或 `CHARTDB-Q-006`。

### CHARTDB-T-007：canvas.tsx 拆分（第一段：模型映射层）

状态：进行中，首段最小拆分切片已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-model.ts`，把 React Flow `nodeTypes` / `edgeTypes`、初始 edges、`tableToTableNode()`、`areaToAreaNode()`、`noteToNoteNode()` 从 `canvas.tsx` 抽出为独立模型映射层。
- `canvas.tsx` 改为消费 `canvas-model.ts` 的导出，保留画布对外行为不变。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-model.test.ts`，覆盖节点/边类型映射、table 过滤隐藏、forceShow、area/note zIndex 等现有行为。
- 本轮未把 `CHARTDB-T-007` 标记为 done：`canvas.tsx` 已从 1944 行降到 1796 行，但距离任务卡“主体不超过 500 行”的完整验收仍有后续拆分工作。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-model.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../canvas-model`，确认模型映射层尚未抽出。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-model.test.ts
```

结果：通过，`1` 个测试文件、`4` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续 `CHARTDB-T-007`：优先拆 `CanvasViewport` / selection / drag / zoom hooks，直到 `canvas.tsx` 主体低于 500 行并可将任务状态更新为 done。

### CHARTDB-T-007：canvas.tsx 拆分（第二段：edge 映射层）

状态：进行中，第二段最小拆分切片已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-edges.ts`，把关系边/依赖边生成、previous selected/animated 状态保留、选中节点/边高亮计算从 `canvas.tsx` 抽出为独立 edge 映射层。
- `canvas.tsx` 改为在边创建和高亮 effect 中调用 `buildCanvasEdges()` / `getHighlightedCanvasEdges()`，保留原有 React Flow handle、dependency visibility 和选中高亮行为。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-edges.test.ts`，覆盖 relationship/dependency handle 映射、showDBViews 隐藏依赖边、previous edge state 保留和高亮幂等返回。
- 本轮未把 `CHARTDB-T-007` 标记为 done：`canvas.tsx` 已从上一轮 1799 行降到 1677 行，但距离任务卡“主体不超过 500 行”的完整验收仍有后续拆分工作。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-edges.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../canvas-edges`，确认 edge 映射层尚未抽出。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-edges.test.ts src/pages/editor-page/canvas/__tests__/canvas-model.test.ts
```

结果：通过，`2` 个测试文件、`6` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续 `CHARTDB-T-007`：优先拆 `CanvasSelection` / drag / zoom hooks 或 `CanvasViewport`，直到 `canvas.tsx` 主体低于 500 行并可将任务状态更新为 done。

### CHARTDB-T-007：canvas.tsx 拆分（第三段：selection 与 node-change 映射层）

状态：进行中，第三段最小拆分切片已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-selection.ts`，把 React Flow nodes/edges 的 selected id 映射从 `canvas.tsx` 抽出为独立 selection 映射层。
- 新增 `src/pages/editor-page/canvas/canvas-node-changes.ts`，把节点变更分类和 area 拖拽时子表视觉 position changes 计算从 `canvas.tsx` 抽出为独立纯函数层。
- `canvas.tsx` 改为调用 `getSelectedCanvasNodeIds()`、`getSelectedCanvasEdgeIds()`、`getRelevantCanvasNodeChanges()` 和 `getAreaDragChildTablePositionChanges()`，保留原有选择同步、area 拖拽视觉反馈和 table/area/note 持久化行为。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-selection.test.ts` 和 `src/pages/editor-page/canvas/__tests__/canvas-node-changes.test.ts`，覆盖 selection 映射、节点变更分类和 area 拖拽子表位移。
- 本轮未把 `CHARTDB-T-007` 标记为 done：`canvas.tsx` 已从上一轮 1677 行降到 1604 行，但距离任务卡“主体不超过 500 行”的完整验收仍有后续拆分工作。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-selection.test.ts
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-node-changes.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试分别失败于缺少 `../canvas-selection` 和 `../canvas-node-changes`，确认 selection/node-change 映射层尚未抽出。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-selection.test.ts src/pages/editor-page/canvas/__tests__/canvas-node-changes.test.ts src/pages/editor-page/canvas/__tests__/canvas-edges.test.ts src/pages/editor-page/canvas/__tests__/canvas-model.test.ts
```

结果：通过，`4` 个测试文件、`9` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续 `CHARTDB-T-007`：优先拆 `onNodesChangeHandler` 的 table/area/note storage update 或 `CanvasViewport`，直到 `canvas.tsx` 主体低于 500 行并可将任务状态更新为 done。

### CHARTDB-T-007：canvas.tsx 拆分（第四段：node storage update 纯函数层）

状态：进行中，第四段最小拆分切片已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-node-storage-updates.ts`，把 `onNodesChangeHandler` 中 table/area/note 的持久化更新计算抽出为独立纯函数层。
- `canvas.tsx` 改为调用 `buildTableStorageChanges()`、`buildAreaStorageChanges()` 和 `buildNoteStorageChanges()`，保留原有 update/remove API 调用顺序与行为。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-node-storage-updates.test.ts`，覆盖 table position/width/remove、area remove 清理 `parentAreaId`、area/note update/remove 输出。
- 本轮未把 `CHARTDB-T-007` 标记为 done：`canvas.tsx` 已从上一轮 1604 行降到 1490 行，但距离任务卡“主体不超过 500 行”的完整验收仍有后续拆分工作。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-node-storage-updates.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../canvas-node-storage-updates`，确认 storage update helper 尚未抽出。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-node-storage-updates.test.ts src/pages/editor-page/canvas/__tests__/canvas-node-changes.test.ts src/pages/editor-page/canvas/__tests__/canvas-selection.test.ts src/pages/editor-page/canvas/__tests__/canvas-edges.test.ts src/pages/editor-page/canvas/__tests__/canvas-model.test.ts
```

结果：通过，`5` 个测试文件、`13` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续 `CHARTDB-T-007`：优先拆 `CanvasViewport` / drag / zoom hooks 或其它事件 handler，直到 `canvas.tsx` 主体低于 500 行并可将任务状态更新为 done。

### CHARTDB-T-007：canvas.tsx 拆分（第六段：ChartDB eventConsumer helper）

状态：进行中，第六段最小拆分切片已完成本地验证。

任务来源：`docs/ChartDB全方位评估与后续优化手册.md`

变更摘要：

- 新增 `src/pages/editor-page/canvas/canvas-chartdb-events.ts`，把 `eventConsumer` 中 ChartDB 事件到 overlap graph / measured node update 的计算逻辑抽出为独立 helper。
- `canvas.tsx` 改为调用 `buildCanvasEventUpdate()`，保留订阅、`setOverlapGraph()` 和 delayed measured node state update 的组件状态写入。
- 新增 `src/pages/editor-page/canvas/__tests__/canvas-chartdb-events.test.ts`，覆盖 `remove_tables` 事件清理 overlap graph 顶点和邻接引用。
- 本轮未把 `CHARTDB-T-007` 标记为 done：`canvas.tsx` 已从上一轮 1454 行降到 1364 行，但距离任务卡“主体不超过 500 行”的完整验收仍有后续拆分工作。

红灯记录：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-chartdb-events.test.ts
```

结果：第一次在 fresh worktree 因缺少 `node_modules` 停在 `vitest: command not found`；执行 `npm install` 后重跑，新增测试失败于缺少 `../canvas-chartdb-events`，确认 eventConsumer helper 尚未抽出。

绿灯验证：

```bash
npm run test:ci -- src/pages/editor-page/canvas/__tests__/canvas-chartdb-events.test.ts
npm run test:ci -- src/pages/editor-page/canvas/__tests__
```

结果：聚焦测试通过；canvas 目录测试通过，`8` 个测试文件、`21` 个测试通过。

完整验证：

```bash
npm run lint
npm run test:ci
npm run build
git diff --check
```

结果：见本轮 run log。

下一项建议：

- 继续 `CHARTDB-T-007`：优先拆 `CanvasViewport` / drag / zoom hooks / ReactFlow controls 或其它事件 handler，直到 `canvas.tsx` 主体低于 500 行并可将任务状态更新为 done。
